# 프로세스 회고 (orchestrator가 미션 설계 시 읽는다)

## 잘 작동한 패턴 (유지)
- [2026-07-17][process] **스펙(디자이너 hex 확정+대비 실측) → 구현 → qa+reviewer 병렬 → 수정 → 재검증** 파이프라인. reviewer/qa가 매 미션 실버그를 잡았다(토글 썸 소실, 차트 stale, 진행률 반올림 모순, 에러 무음 삼킴) — 검증 단계 생략 금지.
- [2026-07-17][process] 병렬 트랙은 **파일 경계로 분리**하고, 공유 파일(copy.ts, index.ts 배럴)은 순차 처리 또는 담당 지정. 착수 전 "누가 어떤 파일을 만지는지" 명시.
- [2026-07-17][process] 핸드오프에 **검증 로그 원문 수치**(테스트 개수·대비 값)를 포함하면 취합 신뢰도가 높아진다. "통과했다"만 쓰지 말 것.
- [2026-07-17][process] 대비(contrast)는 스펙 단계에서 **사전 실측**(presets-verify.mjs 패턴) — 구현 후 발견하면 연쇄 조정 비용이 커진다.
- [2026-07-17][process] 같은 도메인 후속 작업은 **이전 작업자를 재개**(컨텍스트 유지)하는 편이 새 에이전트보다 빠르고 정확했다.
- [2026-07-20][process] 시각 스타일 요청은 "1안 코드 적용 + 나머지 안은 **교체 지점 1곳 + CSS 스니펫**으로 설명"이 사용자 취향(여러 버전 비교)과 잘 맞았다 — 대안을 다 구현하지 않고도 비교·전환이 가능. 이때 활성/변형 스타일을 styled 파일 내 **단일 상수 블록**으로 모으는 게 전제(navItemActiveStyle 사례).
- [2026-07-21][process] 단일 UI 도메인 소기능(LoginModal 버튼 1개)도 **frontend → qa → reviewer 순차 위임**이 값을 했다: 브리핑에 pitfalls의 "vi.mock 전체치환 목 누락 크래시"를 발췌해 주입하니 frontend가 이를 명시적 리스크로 넘겼고 qa가 선제 차단 → 전 LoginModal 테스트 붕괴를 예방. 이벤트 핸들러에서만 불리는 새 배럴 import는 목에 없어도 **기존 테스트가 초록이라 무죄로 오인**하기 쉬운데, 브리핑 주입이 그 함정을 건너뛰게 했다.

- [2026-07-21][process] **진단-우선 미션(버그 원인 규명)은 "GA4 정량화 ∥ 코드 근본원인" 2트랙 병렬 → 판정 수렴 확인 → "구조적 한계"면 ui-ux-designer ∥ pm-po로 대응안 도출** 파이프라인이 잘 맞았다. 코드 진단(state-engineer, read-only)과 데이터 진단(analytics-analyst)이 독립이라 병렬로 띄워 시간 절약. **판정이 갈릴 수 있는 지점을 미리 계측 지문으로 정의**해두면(context_switched vs reason=no_session으로 가설 a/b 구분) 실기기·데이터 확인 항목이 명확해진다. ⚠ ui-ux-designer("지금 카피 A 배포")와 pm-po("측정 먼저, blind 빌드 금지")가 **상충 권고**를 냈는데, 이 상충 자체가 사용자에게 넘길 결정의 핵심이었다 — 오케스트레이터가 한쪽으로 성급히 봉합하지 말고 상충을 그대로 드러내 사용자 판단에 넘기는 게 옳다(특히 파괴적/전환영향 있는 로그인 플로우).
- [2026-07-21][process] **GA MCP 인증이 미설정이면 진단 미션의 정량화 트랙이 통째로 blind**가 된다 — analytics-analyst를 띄우기 전에 인증 상태를 알 수 없으니 일단 띄우되, 브리핑에 "인증 미설정이면 지어내지 말고 택소노미만 정리"를 명시하면 헛수고 없이 "무엇을 수집 중인지 + 인증 풀리면 돌릴 쿼리"가 남아 다음 세션이 즉시 재개 가능하다.

- [2026-07-21][process] **외부 심사(네이버 검수) 대응은 "코드는 보조, 제출자료·캡처가 실제 통과요인"임을 브리핑·최종보고 양쪽에 못박는다** — 확정 스펙(카피·배치·톤까지 사용자가 정함)이 오면 ui-ux-designer 왕복을 생략하고 frontend에 디자인결정을 주입해 A(게이트 flip)+B(상시 고지) 한 에이전트로 묶으면 빠르다. 문서 이관(C)은 파일 경계가 완전 분리라 프론트와 병렬. reviewer를 "blocking만, 없으면 통과"로 게이팅하니 대기 없이 커밋까지 완주했다. 최종 핸드오프에 **사용자가 직접 해야 하는 제출 체크리스트**(캡처 순서·상수 되돌리기 시점)를 포함하는 게 이런 "코드로 못 끝나는" 미션의 핵심 산출물이다.
- [2026-07-21][process] **OAuth/서버 연동 실패는 클라이언트 UX 를 만지기 전에 서버 콜백 상태코드·프로바이더 로그부터 본다** — "카카오만 로그인 실패" 증상이 "카톡 인앱 저장소 격리"라는 그럴듯한 가설과 맞아떨어져 여러 세션(선제 배너·외부열기 버튼·2FA 카피 3커밋)을 UI 쪽으로 태웠으나, 실원인은 Supabase Auth Logs 의 콜백 500(profiles avatar_url CHECK 위반)이었다 — 로그 한 줄이면 첫 세션에 끝났을 일. **위임 브리핑에 "확정된 근본원인 + 로그 원문"을 주니** frontend/qa/docs 가 헛다리 코드를 정확히 도려내고(선제 배너 제거) 실재하는 안전망(실패 후 배너)은 남기는 경계를 한 번에 맞췄다 — 진단이 끝난 정리 미션은 "무엇이 오진이었고 무엇이 진짜였나"를 브리핑에 명시하는 게 재작업을 없앤다. ⚠ 서브에이전트(frontend)가 학습 프로토콜대로 pitfalls.md 를 **먼저 수정**해 orchestrator 큐레이션과 편집 충돌(파일 변경 감지)이 났다 — 지식파일을 만질 서브에이전트에겐 담당 항목을 지정하거나 "knowledge 는 orchestrator 전담"을 브리핑에 명시해 이중편집을 피할 것.

- [2026-07-22][process] **"UX 배치를 고민 중"인 설계-전 요청은 구현 금지 미션으로 명시**하고 코드구조(frontend-engineer)∥데이터가용성(ticker-data-curator)을 read-only 병렬로 먼저 돌린 뒤, 그 사실을 주입해 ui-ux-designer에 위임하니 설계가 데이터 현실 위에 앉았다(요청 5필드가 프리셋 73종에만 존재·검색탭 상장 1만2천종엔 수치 0 → 필터 모집단 제약이 설계 전제로 확정). 미션 도중 coordinator가 배치 방향(접이식 패널)을 확정해 왔는데, **확정을 designer 브리핑의 "이미 선택됨" 전제로 못박고 대안 비교는 짧게만** 요구하니 재설계 왕복이 없었다. ⚠ 파생필드 중복(expectedTotalReturn=yield+growth)은 데이터 조사에서 먼저 잡아 designer에 넘겨야 "3중 슬라이더 모순"을 설계가 선제 회피한다.

- [2026-07-22][process] **기능 제안 미션에서 오케스트레이터 브리핑의 "현재 코드 사실관계"조차 최신이 아닐 수 있다** — "진행률 게이지는 없음/약함"이라 브리핑했으나 pm-po·ui-ux-designer 둘 다 코드에서 이미 있는 StatTile 진행바(role=progressbar+그라데이션+"목표의 N%")를 찾아냈고, 나아가 target=0 미설정 시 "0원 목표 첫해 달성"으로 읽히는 무근거 표시 버그까지 잡았다. 교훈: **제안 미션에도 pm-po∥ui-ux-designer를 read-only로 병렬 위임하면 브리핑의 갭분석을 코드로 교차검증**해 "이미 있는 걸 새로 만들자"는 중복 제안과 숨은 버그를 동시에 걸러낸다. 두 에이전트에게 각각 "npm run search로 사실 교차확인"을 명시한 게 주효했다.

- [2026-07-22][process] **제안→승인→구현 전환 시 "확정 결정"을 이미 컨텍스트를 가진 designer에게 SendMessage로 재개시켜 구현스펙(hex·게이지옵션·카피 문자열)까지 뽑고, 그 스펙을 그대로 frontend 브리핑에 인라인**하니 designer→frontend 핸드오프 손실이 0이었다(retro의 "동일 도메인 재개" 패턴 재확인). 파이프라인 ui-ux(재개)→frontend→qa→reviewer 순차가 매끄럽게 완주. ⚠ **백그라운드 재개 에이전트의 완료 판정은 output 파일 크기·mtime 만으로는 오판한다** — 긴 thinking/tool 스텝 중 크기가 잠시 정체돼 "IDLE"로 오인했다(실제론 작업 중). 정답은 **파싱해서 마지막 assistant record의 `stop_reason==='end_turn'`을 확인**하는 것. output 파일이 JSONL(전체 transcript)이라 통째 Read는 256KB 초과로 실패 → python으로 text 블록만 추출해 파일로 저장 후 Read가 안전(콘솔 mojibake 회피). ⚠ **frontend가 스펙을 넘는 최소 이탈(스펙은 success/warning 2토큰, 실제 markPoint/게이지가 onBrand/progressTrack도 필요)**을 자율 판단해 기존 토큰만 readVar로 추가했고, reviewer가 이를 명시 검증항목으로 받아 "새 hex 0·5곳 정합·additive 무파손"으로 승인 — **브리핑에 "스펙 이탈 발생 시 reviewer가 판정"을 미리 심으면 이런 필연적 이탈이 무음으로 지나가지 않는다**. reviewer가 잡은 신규 함정(onBrand×success 대비 사각지대)은 pitfalls에 기록.

- [2026-07-23][process] **"구현 금지, 검토만" 미션은 pm-po + 전문가 3인(state/sim/ticker)을 read-only 병렬로 띄우니 오케스트레이터 브리핑의 전제 오류 3건을 코드·실측으로 교차반증**했다: ①"한 API로 KR+US 배당 못 준다"는 전제를 ticker-curator가 Yahoo `.KS` 실호출로 반증 ②"Supabase Edge Function/Cron 필요"를 pm-po가 "기존 GitHub Actions 월간 크론 + Yahoo 무키라 불필요"로 반박 ③"도달예상일은 findTargetYear로 이미 있음"을 sim-engineer가 "연 해상도라 월단위 예상일엔 부적합, runSimulation 본체는 정수주·실캘린더·월해상도 3가지로 재사용 불가, 헬퍼만 재사용"으로 정밀화. 교훈: **검토 미션에서도 브리핑의 "현재 사실"을 그대로 믿지 말고 각 전문가에게 "npm run search/실호출로 교차확인"을 명시**하면 서브에이전트가 오케스트레이터의 오진을 잡아준다. 상충이 아니라 서로를 정밀화하는 방향이라 종합이 수월했다. ⚠ 서브에이전트가 실측한 "코드만으론 모르는 발견"(Yahoo KR 커버)은 편집권 없어 핸드오프 텍스트로만 넘겼고, orchestrator가 architecture.md 토대B에 기록했다 — 검토 미션에서도 지식 반영은 orchestrator 전담 원칙 유지.

- [2026-07-24][process] **적응형 개발 프로세스 스킬 4종(ship·feature·new-ticker-page·refresh-data) + `verify` 도구를 구축**하며 얻은, 코드만 봐선 모를 것 3가지: ①dev CLI 3종(tracks·devstatus·predeploy)은 `chore/dev-cli-tools` 브랜치(worktree)에만 있고 **main엔 없다** — `tools/dev/verify.mjs`는 그 관례(predeploy의 `runNodeBin`·`findNodeModulesRoot`·정션 node_modules 탐색)를 **참고**하되 `trackConfig.mjs` import 없이 **standalone**으로 짜야 main에서 돈다. `ship` 스킬도 tracks/predeploy를 "있으면"으로 참조해 이 브랜치 분리를 정직하게 반영했다(없으면 `git status`+`verify` 폴백). ②verify의 단계 순서 **api:bundle→api:check는 의도적**이다 — `npm run build`는 api:check만 돌려 CI에서 stale이면 실패시키지만, verify는 **로컬 pre-ship** 도구라 소스 변경을 산출물에 **재생성 후 확인**한다(그래서 verify 후 `git status`로 바뀐 api/*.js 스테이징 안내가 필요). ③마스터 문서(dev-process SKILL.md)만으론 절차를 못 짠다 — 실제 도구 소스·#41~#43 커밋·pitfalls 실측(19종 가드 스킵·pathspec `--only` 커밋·og→marketData 전이의존)을 읽어야 스킬이 현실과 맞았다. 스킬을 "지어내지 말고 실측으로"가 이 미션의 핵심이었다. 근거 tools/dev/verify.mjs, .claude/skills/{ship,feature,new-ticker-page,refresh-data}/SKILL.md, CLAUDE.md "개발 프로세스(적응형)".

- [2026-07-25][process] **T3 캘린더 미션(v1+피벗 v2)**: 스펙이 충분히 확정된 T3는 pm-po 생략 가능 — designer(스펙)∥state(순수 로직, 오케스트레이터가 계약 이름·시그니처를 브리핑에 고정) 병렬 → frontend 접합 → qa∥reviewer 병렬이 잘 돌았다. qa(it.fails로 버그 고정)와 reviewer(같은 지점 minor 지적)가 **독립적으로 같은 경합 버그에 수렴하면 확신도가 높다**. 사용자 피벗(연간 보드→월간 달력)엔 **v1을 체크포인트 커밋으로 먼저 봉인하고 v2를 같은 브랜치 위에 쌓는 2커밋 전략**이 유효 — 피벗 전 상태로 되돌릴 수 있고 diff 리뷰가 깨끗하다. 수정 루프는 전부 SendMessage 재개(동일 도메인 재개 패턴 재확인, 왕복 5회).
- [2026-07-25][process] **qa가 버그를 `it.fails`로 고정한 채 수정 트랙이 병렬로 돌면, 수정이 랜딩되는 순간 it.fails가 "통과해서 실패"로 뒤집힌다** — 버그 고정 테스트의 승격(it.fails→it) 시점은 오케스트레이터가 수정 완료와 동기화해 조율하고, 승격 후 제목·주석의 "(미수정 버그)" 잔재 정리까지가 한 단위다.

- [2026-07-26][process] **"계속 개발" 미션은 pm-po∥designer read-only 교차검증 → 우선순위 확정 → 구현이 잘 돌았다**: 둘의 추천이 갈렸는데(pm-po=월 해상도 도달 시점, designer=가시성·온보딩 폐루프) designer의 **실측 갭**(목표 차트가 기본 화면에 미렌더 G1, 다크 대비 2.36:1 실패 G4)이 우선순위를 결정했다 — "새 가치 추가"보다 "이미 만든 것이 안 보임"이 먼저다. designer가 구현 스펙(카피 확정 문자열·토큰·포커스 처리)까지 내주면 frontend 브리핑에 그대로 인라인해 핸드오프 손실 0. 상충하지 않은 나머지(pm-po P1)는 다음 미션 후보로 정직하게 이월.
- [2026-07-26][process] **동기 orchestrator가 SendMessage 재개(백그라운드 강제)를 기다리는 실전 해법 = 관측 가능한 조건을 포그라운드 폴링** — foreground sleep은 차단되지만 `ping -n 4 127.0.0.1`(≈3초) 루프는 돈다. 대기 조건은 transcript의 end_turn 카운트 증가보다 **결과물 자체**(예: `git log -1`에 정정된 푸터 등장)가 레이스 없이 안전. 이로써 '트랙 완료 대기로 턴 종료 → 고아' 사고(아래 2026-07-25 항목)를 턴 안에서 회피했다.
- [2026-07-26][process] **git-manager가 지시한 Co-Authored-By 푸터를 다른 문구로 넣은 사례** — 커밋 직후 `git log -1 --format=%B` 원문 검증을 커밋 위임의 기본 체크리스트에 넣어라(로컬 미푸시면 amend 정정으로 충분).
- [2026-07-27][process] **T3 목표 페이지: 엔진→상태→프론트→qa→reviewer 동기 순차 + 미션 중 스펙 델타 2회(라우트 변경·로드맵 v2)를 SendMessage 재개로 흡수** — 컨텍스트를 가진 에이전트 재개가 델타 반영 비용을 크게 낮췄다(frontend 1회 재개로 델타 3건+QA 버그 2건+프리필 계약 일괄). **사용자 데이터 쓰기 경로가 걸린 전제조건(D2)은 "판정 임무"로 분리해 state-engineer에 위임**한 게 결정적: 3경로 위험을 코드 근거로 기각하고 안전한 대안 계약(프리필+커밋 지점 고정)을 설계해 와서, 이후 구현 브리핑에 조건 4개를 그대로 주입하니 reviewer가 조건 실재를 기계적으로 검증할 수 있었다. qa의 red 고정(일반 it) → frontend 재개 수정 → 그린 동기화도 매끄러웠다.
- [2026-07-27][process] **SendMessage 재개는 강제 백그라운드다** — 동기 orchestrator가 기다리는 실전 해법은 transcript(JSONL)를 파싱해 **마지막 assistant 레코드의 stop_reason=='end_turn'**을 폴링하는 것(python 파서 + ping 루프). ⚠ 꼬리 grep으로 end_turn을 찾으면 **이전 턴의 end_turn에 속는다**(실측 — DONE 오판 1회). 완료 알림이 메인 세션으로 가는 구조(2026-07-25 항목)도 재확인 — 코디네이터가 같은 결과를 중계해 와 이중 수신됐지만 무해했다. 신규 위임은 Agent(run_in_background:false)로 동기가 정답.
- [2026-07-27][process] **UI 배치 요청(동기화 표시 모바일 깨짐)은 designer가 320px 산술 근거+확정 카피+styled 단위 스펙까지 내주니 frontend가 이탈 0으로 구현** — 사용자 제안("설정 열기 밑에 조그맣게")을 1안으로 존중하되 대안 4개를 짧게 기각 비교한 스펙이 재작업을 없앴다. qa 뮤테이션이 3번째 미션 연속으로 실구멍을 발견(부분일치 단정이 카피 축약 회귀 무음 통과) — **카피를 바꾸는 변경엔 "풀 카피 정확일치" 계약 테스트를 기본으로**.
- [2026-07-27][process] **미션 도중 사용자 정정("스크롤 미작동은 티커 생성쪽")으로 버그 위치가 바뀌어도 1차 수정(설정 드로어 containment)은 되돌리지 않고 독립 실버그로 봉인(bb48ae9)한 뒤 2차(TickerModal, bec12b3)를 같은 브랜치에 쌓는 2커밋 전략이 유효** — 사용자 증상 서술로 위치를 단정하기 전에 후보 UI를 넓게 잡을 것(같은 "슬라이더" 표현이 드로어·모달을 다 가리켰다). 2차에서도 frontend→qa∥reviewer→소수정→대조군 테스트→git 순차가 재현됐고, qa 뮤테이션이 또 실효(생존 뮤턴트 2종=자기충족 테스트 발견, 게이트 미커버 발견→대조군 추가로 킬), reviewer가 qa 실측(순차 닫힘이 진짜 잠금 경로)과 교차해 주석 오서술까지 정정했다.
- [2026-07-27][process] **모바일 드로어 UX 미션(T2): frontend(동기) → qa ∥ reviewer 병렬 → 경합 수정(새 동기 frontend) → 훅만 좁힌 재리뷰 → git** 이 매끄럽게 완주. ①qa 브리핑에 **뮤테이션 확인을 명시**하니 기존 6케이스 중 3계약이 뮤턴트 생존(테스트 구멍)으로 드러나 19케이스로 보강 + 실사용 경합 결함 1건(닫기→빠른 재열기 시 첫 뒤로가기 먹통)까지 발굴 — "전부 통과"만 믿었으면 놓쳤다. ②수정 위임은 SendMessage 재개(백그라운드 폴링 비용) 대신 **qa의 재현·원인·제안을 인라인한 새 동기 frontend**로 했는데 컨텍스트 손실 없이 더 단순했다 — 수정 스펙이 이미 문서화돼 있으면 재개보다 신규 동기가 낫다. ③구현이 1차 리뷰 후 구조적으로 바뀌면(상태머신化) **바뀐 파일만 좁힌 재리뷰**가 싸고 충분했다. ④서브에이전트 2명이 독립적으로 pitfalls의 낡은 항목(container-type 무해 주장)의 정정 필요를 보고 — "knowledge는 orchestrator 전담" 규칙이 이중편집 없이 잘 작동. curator→qa→reviewer→(수정 curator)→git 전부 동기 순차로 매끄럽게 완주** — 병렬 이득이 없는 단일 파일군은 순차가 조율 비용이 0이다. reviewer가 major 1건(--only 미존재 티커→스냅샷 EMPTY 폴백 경로)을 잡아 커밋 전 수정했고, qa의 **뮤테이션 검증**(핵심 계약 2개를 일부러 깨서 테스트가 정확히 빨개지는지 확인 후 원복)이 "노이즈 커밋 방지·마커 생존" 같은 **부정 계약(안 일어나야 하는 일)** 테스트의 실효성을 증명했다 — 이런 계약엔 뮤테이션 확인을 qa 브리핑에 명시할 가치가 있다.

- [2026-07-28][process] **중단된 오케스트레이터를 이어받는 미션은 "검증 → 리뷰 → 정리 → 커밋" 순으로 짜고, 인수인계 브리핑의 상태 서술을 그대로 믿되 *결론*은 직접 실측하라** — 이전 세션이 모델 리밋으로 죽어 90파일이 미커밋으로 남은 상황에서 tsc·test가 곧바로 그린이었고 reviewer도 Blocking 0을 냈다(중단이 곧 미완성은 아니다). 값을 한 건 오히려 **브리핑에 없던 축**이었다: ①고아 참조 탐색을 `Explore`로 **병렬 분리**해 정적 SEO 자산·api 번들·배포 설정까지 훑게 한 것 ②`npm run api:check` 단독 실행으로 **verify가 자동 치유해 숨기던** 배포 블로커(og 번들 stale)를 찾은 것. 교훈: 인수 미션의 검증은 "테스트가 그린인가"에서 멈추지 말고 **"배포까지 가는가"**를 따로 물어라.
- [2026-07-28][process] **"정리(cleanup) 위임은 한 번에 안 끝난다 — 잔재 목록을 에이전트가 되돌려주면 그걸 다음 브리핑에 인라인해 동기로 재발주**하는 루프가 효율적이었다(3회 왕복으로 주석 잔재 14곳 소거). SendMessage 재개(강제 백그라운드) 대신 **새 동기 에이전트 + 앞선 에이전트가 만든 목록 인라인**이 컨텍스트 손실 없이 더 단순했다(2026-07-27 항목 재확인). 각 라운드마다 "이번엔 이 N곳만, 범위 넘지 마라"로 못박고 **범위 밖 발견은 보고만 하게** 하니 스코프 크립 없이 목록이 수렴했다.
- [2026-07-28][process] **사용자가 "미리 확장성 있게 짜라"는 품질 요구를 걸면, reviewer에 "기능이 2배 들어와도 버티는가"를 *구체적 확장 시나리오 4개*로 물어라** — 추상적 "구조 좋은가"보다 훨씬 실행 가능한 답이 나왔다(각 시나리오를 "버틴다/국소 수정/구조가 터진다" 3단 판정 + 수정 파일 수 추정). 그 결과 **"지금 고칠 것 4건(저위험)" vs "후속 후보(카드 추가 미션의 1단계로 묶기)"** 로 자연히 갈렸고, 조기 추상화를 명시적으로 금지한 덕에 reviewer가 "이건 지금 하면 조기 일반화"라고 스스로 되밀어냈다. 구조 개선은 **이번 미션이 만든 코드로 범위를 한정**해야 검증 그린을 안 깬다.

- [2026-07-28][process] **디자인 시스템 교체(아이덴티티 패스)는 "조사 2트랙 → 구현 3트랙 순차 → qa∥reviewer → 수정 → 커밋"이 잘 돌았다.** 값을 한 설계 판단 3가지: ①**조사를 구현에서 완전히 분리**했다 — 폰트 조달(general-purpose, 웹·npm 실측)과 색 스펙(ui-ux-designer, 8프리셋 대비 실측)을 read-only로 먼저 끝내 hex·패키지명·라이선스를 확정하니 구현자가 재설계 왕복 0으로 갔다. designer가 낸 674행 스펙(§9 구현 지시서 `path:line` 포함)을 구현 브리핑에서 **파일 경로로 가리키기만** 해도 됐다(인라인 불필요). ②**구현 3트랙을 병렬이 아니라 순차**로 돌렸다 — 파일 경계는 깨끗했지만 `npm install`이 node_modules를 갈아 상대 트랙의 테스트를 오염시킬 위험이 실재했고, 색→폰트→워드마크는 실제로 의존 사슬이었다(워드마크가 두 트랙 산출물을 다 쓴다). ③**브리핑마다 "안 해야 할 것"을 파일 목록으로** 못박아(다른 트랙 파일·10단계 소관·커밋 금지 파일) 스코프 크립 0. ⚠ **조사 에이전트가 브리핑의 전제를 반증한 건이 2건**(“Gmarket·LINE Seed는 OFL이 아닌 자체 약관” → 4종 전부 OFL 1.1 / “Pretendard와 동일한 92분할” → 조각 수만 같고 경계는 다름) — 조사 트랙에 "실측으로 교차확인"을 명시한 게 또 값을 했다.
- [2026-07-28][process] **구현자가 스펙의 사실오류를 스스로 찾아 신고하고, reviewer가 그걸 독립 검증해 "이탈은 승인하되 근거 서술은 정정"으로 갈라준 게 이 미션의 백미**였다(designer 스펙이 "pay 배지 ↔ ex 배지 초록 충돌"을 전제했는데 `pay`는 이미 null 반환이라 렌더되지 않았다 — 이탈의 **결론은 옳고 근거 하나가 무효**). 브리핑에 **"스펙 이탈 발생 시 reviewer가 판정한다"를 미리 심어두면** 필연적 이탈이 무음으로 지나가지 않는다(2026-07-22 항목 재확인). reviewer에게 이탈 3건을 **번호로 지목해 "각각 승인/조건부/반려"를 요구**한 형식이 답을 실행 가능하게 만들었다.
- [2026-07-28][process] **qa·reviewer를 같은 작업트리에서 병렬로 돌리면 서로의 중간 상태를 결함으로 오인한다** — reviewer가 본 `contrast.test.ts` 8건 실패는 qa의 뮤테이션이었고, 존재하지 않는 파일로 스위트가 깨진 것도 qa의 임시 파일이었다(둘 다 재실행으로 소멸). 이번엔 reviewer가 스스로 "재실행으로 먼저 가르라"고 판정해 무해했지만, **병렬 브리핑에 "상대가 뮤테이션 중이니 실패는 재실행으로 교차확인하라"를 명시**하는 편이 안전하다. 부수 소득: reviewer가 우연히 잡은 그 실패가 **새 대비 게이트가 실제로 이빨이 있다는 증거**가 됐다.

- [2026-07-28][process] **대규모 레이아웃 개편(시뮬레이터 드로어화 + 12열 그리드 + 아이덴티티 폴리시)은 "조사 2트랙 병렬 → 구현 4트랙(파일 경계로 분리) → qa∥reviewer → 수정 라운드 → 커밋"이 매끄러웠다.** 값을 한 판단 4가지: ①**설계와 영향범위를 따로 띄웠다** — `ui-ux-designer`(888행 구현 스펙: 확정 hex·확정 카피·props·`path:line`)와 `Explore`(A~H 8축 blast radius: 소비처 전수·테스트 36케이스 견적·자동저장 결함 위치)를 read-only 병렬로 돌리니 구현자가 재설계 왕복 0으로 갔다. 이후 모든 구현 브리핑은 **스펙 파일 경로 + 절 번호를 가리키기만** 하면 됐다(인라인 불필요). ②**브리핑마다 "네 소유 / 남의 소유" 파일 목록을 못박아** 4트랙이 같은 작업트리에서 충돌 0. ③**큰 트랙을 부품(3a)/조립(3b)으로 쪼개고 3a의 props 시그니처 원문을 3b 브리핑에 인라인**하니 핸드오프 손실 0. ④**"이번에 만들 공용 부품의 최소 집합"을 designer가 표로 판정하고 나머지를 '10단계'로 명시 기각**하게 한 게 조기 일반화를 원천 차단했다(12부품 중 4개만 신설).
- [2026-07-28][process] **조사 트랙에 "실측으로 교차확인"을 명시하니 이번에도 정본이 4곳 틀렸다** — 스펙이 "유지하라"고 한 목표 미설정 CTA는 **이미 철거돼 있었고 그 부재를 지키는 회귀 테스트까지 있었다**(되살렸으면 의도적으로 설계된 테스트를 깨는 것). 브레이크포인트도 브리핑/스펙 3곳이 서로 어긋나 있었다(≥981 vs ≥961 vs 821–980) → designer가 **딤 경계=`drawer`960 / 그리드 경계=`layout`980으로 기존 토큰 2개의 의미만 재배정**해 신규 브레이크포인트 0으로 해소. **오케스트레이터 브리핑의 "현재 사실"을 서브에이전트가 반증하는 것이 이 팀의 정상 작동**이다.
- [2026-07-28][process] **qa 브리핑에 뮤테이션을 명시한 것이 4번째 미션 연속으로 실구멍을 찾았다** — 17종을 심었더니 2종이 **원래 살아남아** 있었다: ①`data-tour` 앵커를 지워도 4,600건 전부 그린 ②카피 단정이 **같은 상수를 기대값으로 쓰는 동어반복**이라 축약 회귀가 무음 통과. 둘 다 "카드를 통째로 갈아끼우는 개편"에서 가장 잃기 쉬운 계약이라 리스크 적중률이 높았다. ⚠ **reviewer와 qa를 병렬로 돌릴 때 "상대가 뮤테이션 중이니 실패는 재실행으로 교차확인"을 양쪽에 명시**하는 것이 이제 표준(이번엔 사고 0).
- [2026-07-28][process] **서브에이전트가 모델 한도로 조기 종료되면 `model` 파라미터로 갈아 끼워 재발주하면 된다** — reviewer가 리밋으로 죽어 `model: "opus"`로 다시 띄웠고 손실은 그 트랙 1회분뿐이었다. 재발주 전에 **그 사이 orchestrator가 처리한 것(api 번들 재생성)을 브리핑에 반영**해 이미 해결된 블로커를 다시 파게 하지 마라.
- [2026-07-28][process] **리뷰 후 수정 라운드는 "고칠 것 N건"과 "절대 손대지 말 것"을 같은 무게로 나열**하니 스코프 크립 0으로 끝났다. 특히 **사용자 결정 대기 항목 2건(드로어 자동 닫힘·투명 스크림)을 금지 목록에 넣은 것**이 핵심 — reviewer가 둘 다 "바꾸는 게 옳다"는 강한 의견을 냈지만 그건 **제품 결정이라 사용자에게 올리는 것**이 맞다(retro 2026-07-21 "상충을 봉합하지 말고 드러내라"의 연장).

## 사고·마찰에서 배운 것
- [2026-07-25][process] 🔴 **작업트리의 예상 밖 변경을 "무단 작업"으로 단정하고 원복을 지시했다가 철회** — PrimaryNav의 캘린더 링크가 브리핑의 "금지(사용자 결정 전)"와 어긋나 보였지만, 실제로는 **그 사이 메인 세션이 사용자 직접 지시로 만든 변경**이었다(전제 "사용자 결정 전"이 해소된 것). 병렬 세션 환경에선 브리핑 시점의 확정 결정이 미션 도중 사용자 지시로 뒤집힐 수 있다 — **원복 같은 파괴 지시 전에 변경의 출처(메인 세션/다른 트랙/사용자 지시)를 코디네이터에 먼저 확인**하라. 서브에이전트(frontend)가 소유권 의심으로 원복을 보류한 판단이 옳았다 — "브리핑과 다르다"는 위반의 충분조건이 아니다.
- [2026-07-17][process] 에이전트 간 **계약(어트리뷰트명·저장 키·함수명)은 먼저 랜딩한 구현이 정본** — 스펙 문서가 다르게 쓰면 취합자가 즉시 조정 지시. (data-preset vs data-palette / buildSimSummary vs buildScenarioSimSummary 사례)
- [2026-07-17][process] 병렬 작업 중 관측된 테스트 실패는 상대 작업자의 중간 상태일 수 있다 — 보고는 하되 **자기 변경과의 인과를 교차 확인**(파일 스왑·재실행)한 뒤 판단. (Button.styled 일시 오류, TS2551 사례)
- [2026-07-17][process] 서브에이전트는 wmux 브라우저를 못 쓴다 — 시각 QA 항목은 "자동화 근거 병기 + 수동 확인 필요"로 분류해 사용자/메인 세션에 넘긴다.
- [2026-07-16][process] 사용자 요청의 진짜 의도를 확인하라 — "velog 참조"가 팔레트가 아니라 **글 카드 포맷**이었던 사례. 모호하면 이행 전에 1회 확인이 재작업보다 싸다.
- [2026-07-17][process] 세션 중단(API 529) 후 백그라운드 에이전트 생사는 **output 파일 mtime + 워킹트리 변경 흔적**으로 판별하고 SendMessage로 확인·재개한다("queued"=실행 중 / "no active task; resumed"=정지였음). 0바이트 output은 완료 여부 판별 근거가 못 된다(완료돼도 0바이트).
- [2026-07-17][process] 재개된 에이전트에 추가 지시를 보냈을 때 "완료" 보고만 오면 **산출물을 grep으로 실검증**하라 — 기존 핸드오프만 재발행하고 추가 작업을 안 한 사례(제안서 면책 § 누락) 있음.

## 사용자 확인이 필요한 결정 유형 (독단 금지)
- 기본값 변경(기본 테마, 라우트 등), 기능 제거, 스키마·저장 형식 변경, 타사 브랜드명 노출, 파괴적 플로우(탈퇴 등).

- [2026-07-25][process] **동기(run_in_background:false)로 실행한 orchestrator가 백그라운드로 띄운 트랙 에이전트의 완료 알림은 orchestrator가 아니라 메인 세션으로 온다** — orchestrator는 자기 턴 종료 후 재개되지 않으므로 '트랙 완료 대기'로 턴을 끝내면 통합·검증·커밋이 고아가 된다. 이 구조에선 메인 세션이 알림을 받아 직접 마무리하거나, orchestrator 안에서 트랙을 동기로 기다리게 설계하라. (구조 리팩터링 미션 2차에서 실측)

- [2026-07-29][process] **"팝업이 잘려 나온다" 같은 시각 신고는 CSS 를 뒤지기 전에 "그 팝업을 누가 그리는가"부터 확정하라.** 이번 건의 범인은 우리 스타일이 전혀 아니었고 `navigator.share` 가 여는 **OS 창**이었다(브라우저 밖이라 CSS 로 도달 불가). 팝오버 넘침·모달 max-height·containment 세 가설을 다 뒤졌어도 못 찾았을 문제다. 판별은 `Runtime.evaluate`로 `typeof navigator.share` 한 줄이면 끝난다 — **가설 목록을 소진하기 전에 실행 경로를 코드에서 따라가고, 그 분기 조건을 실브라우저에서 실측**하는 순서가 싸다.
- [2026-07-29][process] **화면 기능은 jsdom 계약 테스트로 "존재"만 잠그고, "동작"은 헤드리스 크롬으로 1회 실증하라.** 결과 이미지 캡처는 jsdom 에서 캔버스가 없어 5케이스(버튼 존재·호출·실패 알림·캡처 범위·제외 마커)만 잠글 수 있었는데, CDP 로 실제 클릭해 **948KB PNG 다운로드와 파일명까지** 확인하니 "테스트는 그린인데 실제로는 빈 이미지" 부류의 사고 가능성이 그 자리에서 사라졌다. 비용은 스크립트 하나(≈40줄).
- [2026-07-29][process] **리뷰가 "가장 작고 기존 규약과 일치한다"며 권장한 처방이 물리적으로 불가능할 수 있다 — 채택 전에 뮤턴트 1개로 검증하라.** 중첩 Escape 회귀에서 권장안은 "위층 3곳에서 `preventDefault()`"였는데, 실제로 TickerModal 에 그 한 줄을 넣고 돌려보니 **여전히 빨갰다**(document 버블이 window 버블보다 먼저 도는 DOM 전파 순서 때문). 근거 없이 권장안을 따랐다면 "고쳤다"는 커밋과 함께 회귀가 남았을 것이고, 근거 없이 대안(스택 레지스트리)으로 갔다면 스코프 확대라는 비판을 반박할 수 없었다. **비용은 편집 1줄 + 테스트 7초**이고, 그 결과가 설계 선택과 커밋 메시지의 근거를 동시에 만들어 줬다. 순서는 항상 ①실패하는 계약 테스트 먼저 ②권장 처방을 그 위에 얹어 확인 ③그래도 빨가면 대안으로.

## 하루치 전면 리워크(P1~P4)에서 배운 것 — 2026-07-31
- [2026-07-31][process] 🔴 **공통 브리핑을 스크래치패드 파일 1개로 뽑고 "먼저 통독하라"만 주입하니 12트랙을 같은 규율로 굴릴 수 있었다.** 금지사항·구조규칙·도구함정·테스트규율·**"이미 랜딩한 것"**을 한 파일에 모으고, 트랙별 브리핑에는 그 트랙 고유의 목표·파일 소유권·검증만 남겼다. 특히 "이미 랜딩한 것" 절이 중복 구현과 **삭제된 prop 사용**을 막았다(P1이 `Card.elevation`을 지웠는데 P2~P4가 그걸 몰랐다면 전부 깨졌다).
- [2026-07-31][process] 🔴 **병렬 트랙의 충돌은 "파일 소유권을 브리핑에 명시적으로 나열"하는 것으로 0이 됐다** — 각 트랙에 *너의 것* / *절대 만지지 마라*를 경로 단위로 주고, 공유 파일은 **`Write` 금지·`Edit`만·편집 직전 재Read**를 못 박았다. 5트랙이 동시에 `pages/Main/**`을 만졌는데 덮어쓰기 사고 0건.
- [2026-07-31][process] **브리핑에 적은 "진단 수치"가 낡을 수 있다 — 서브에이전트가 반증하면 그게 성과다.** "reduced-motion 9곳 얼어붙음"은 실제로 4곳이었고(5곳은 전날 이미 처리), "카드 반경 잔여 50곳"·"PageHero 3벌"도 재확인이 필요했다. **전제를 의심하라고 명시적으로 허락**하는 문장을 브리핑에 넣어라.
- [2026-07-31][process] 🔴 **자기 가드는 자기가 검증하면 안 된다 — 교차검증자를 따로 세워라.** 각 트랙이 뮤턴트 감도를 성실히 보고했는데도, `qa-tester`가 13종 뮤턴트로 다시 재니 **감도 0인 가드가 2건** 나왔다(투어 앵커는 "지우면 잡히나 더하면 안 잡힘", W3 연출은 오발과 정상이 같은 `true`로 겹침). 둘 다 "그 트랙이 만든 가드"였다.
- [2026-07-31][process] 🔴 **규칙을 지키려고 남을 깎아 놓고 자기가 그 규칙을 깨는 일이 실제로 일어난다.** 이번 브랜치가 `Banner info`와 `GoalCard` 성공 줄에서 틴트를 빼놓고, 다른 트랙이 `StatTile`에 새 틴트를 더해 한 화면 4개가 됐다. 원인은 **가드의 기본 라우트가 전부 "상태 없는 화면"**이라 목표 도달·경고 상태를 한 번도 안 쟀기 때문. **개수 상한 가드에는 "이 화면이 가질 수 있는 최대 상태"를 기준선으로 넣어라.**
- [2026-07-31][process] **리밋으로 죽은 에이전트가 남긴 것은 "깨진 상태"가 아닐 수 있다 — 먼저 재라.** V1·헤더후속 두 트랙이 보고 없이 죽었지만 `npm run test` 263파일 그린 + `headerprobe` 25/25였고, 실제로 기능이 **완성돼 있었다**(보고만 유실). 판정 순서는 ①`git status`를 **수정 시각순**으로 ②테스트·타입체크·실측 도구를 돌려 실제 상태를 확인 ③산출물의 존재를 grep으로 대조. 재작업 대신 **실측 수치만 다시 재면** 된다.
- [2026-07-31][process] **오래 도는 트랙을 기다릴 때 폴링은 `git status` 파일 수 + 도구 종료코드로 하라** — 커밋이 아니라 **파일 수정 시각**이 생사 판정의 근거다(이 레포의 백그라운드 에이전트는 조용히 죽는다). 폴링 중 `styled-comment-backticks` 종료코드를 같이 찍어 두면 남이 깨뜨린 백틱을 즉시 안다(실제로 1회 잡았다).
- [2026-07-31][process] **P0→P1→P2 의존 사슬은 "단일 지점"을 먼저 만들면 하위 단계를 안심하고 쌓을 수 있다.** 라우트 hue가 `usePageHue.utils.ts` 2줄, identity 값이 `sharedTokens.ts` 1값이라, 기본 프리셋의 색 충돌이 미결인 채로도 P2~P4를 진행할 수 있었다(뒤집는 비용이 2줄). **미결정 항목이 있으면 그것을 단일 지점으로 만들어 두고 진행하라** — 결정을 기다리며 멈추는 것보다 싸다.

## 야간 자율 세션 (2026-08-01) — 중단 인수 + 신규 2미션
- [2026-08-01][process] 🔴 **중단된 트랙을 인수할 때 "무엇이 남았나"는 파일 수정 시각으로, "무엇이 옳은가"는 실측으로 따로 판정하라.** 트랙 B(테마)는 mtime 상 가장 최근이었고 실제로 **거의 완성돼 있었다**(보고만 유실). 반면 트랙 A(반응형)는 산출물이 없어 미완으로 보였는데, 실제로는 **고칠 것이 애초에 없는 트랙**이었다 — 브리핑의 "3px 공통 폭 버그"가 터치 타깃 의사요소의 거짓 양성이었다. **"미완=할 일이 남았다"가 아니다.** 인수 미션의 첫 질문은 "얼마나 했나"가 아니라 **"이 전제가 참인가"** 여야 한다.
- [2026-08-01][process] 🔴 **브리핑에 "이건 거짓 양성일 수 있다"는 반례를 미리 심은 것이 이번 세션 최고의 투자였다.** 트랙 A 브리핑에 pitfalls 의 `Button::before` 8px 항목을 발췌해 "원인을 특정하기 전에 고치지 마라"를 못 박았더니, 에이전트가 **8px 뿐 아니라 3px·4px 까지 같은 가족임을 실측으로 밝히고 소스 수정 0줄로 끝냈다.** 그 문장이 없었다면 여러 컴포넌트에 근거 없는 폭 보정이 흩뿌려졌을 것이다 — **접근성 자산을 깎는 방향으로.**
- [2026-08-01][process] **스파이크를 구현 트랙과 완전히 분리하고 "결론을 빨리, 지어내지 말고"를 명시하니 설계가 사실 위에 앉았다.** `drive.file` 조사가 공식 메서드 레퍼런스로 "가능"을 확정하고 **CORS 를 실제 curl 로 실측**해 "프록시 불필요"를 확정한 덕에, 데이터 계층 구현자가 재조사 왕복 0으로 갔다. 동시에 **"실측 불가"(자격증명 0건)를 정직하게 보고**해 아침의 가장 중요한 정보가 됐다.
- [2026-08-01][process] 🔴 **T3 신규 기능에서 "데이터 계층 → 화면 스펙"을 병렬로, "화면 구현"을 그 뒤에 두는 3단 배치가 잘 맞았다.** state-engineer(어댑터+쓰기안전 순수함수+199테스트)와 ui-ux-designer(11상태 스펙)가 **서로를 몰라도 충돌하지 않는다**(파일 경계가 완전 분리). 그다음 frontend 가 둘을 받아 재설계 0으로 조립했다. ⚠ 대가: 스펙이 가정한 env 이름·`latestMonth` 가 데이터 계층과 어긋났다 — **브리핑에 "데이터 계층이 정본"을 명시**해 구현자가 그 자리에서 갈랐다.
- [2026-08-01][process] 🔴 **"쓰기 안전을 테스트로 못 박기 전에는 쓰기 코드를 붙이지 마라"가 구조를 바꿨다.** 그 제약이 있으니 구현자가 안전 규칙을 **순수 함수로 뽑았고**, 나아가 "여러 열을 걸치는 범위 생성기를 아예 만들지 않는" 설계로 갔다 — 규칙이 **관례가 아니라 구조**가 됐다. 뮤턴트 10종 중 1종(짧은 열 이어 채우기)이 살아남아 테스트 구멍을 드러낸 것도 성과다.
- [2026-08-01][process] **qa 를 구현자와 다른 에이전트로 세운 것이 또 값을 했다** — 뮤턴트 10종 전부 KILL 을 확인하는 과정에서 **실결함 2건**(`:has()` 접근성 붕괴, 재연결 클로저로 "두 번 눌러야 저장")을 찾았다. 둘 다 **구현자 자신은 "그린"이라 보고했던 코드**다. 수정은 red 테스트를 인라인한 **새 동기 frontend** 로 발주했고(재개보다 단순 — 2026-07-27·28 항목 재확인) 89/89 그린으로 닫혔다.
- [2026-08-01][process] 🔴 **큰 미션을 "가장 안전한 중간 상태"에서 끊는 판단이 야간 자율의 정답이었다.** 라우트 이전은 P0~P3 중 **P2 까지만** 했다 — `/simulator` 가 생기고 내부 링크가 전부 옮겨갔지만 **`/` 는 여전히 시뮬레이터**라 공유 링크(사용자 자산)가 한 건도 위험해지지 않는다. P3(랜딩)는 콘텐츠 설계가 필요한 별도 미션이고, 사용자 없이 밤에 만들 것이 아니었다. **"어디까지 하면 되돌릴 필요가 없나"를 먼저 정하고 거기서 멈춰라.**
- [2026-08-01][process] **공통 브리핑 파일 1개(스크래치패드) + 트랙별 고유 브리핑 패턴이 7트랙에서 재확인됐다**(2026-07-31 항목). 이번엔 "①금지 ②구조규칙 ③검증 ④테스트규율 ⑤jsdom 한계 ⑥현재 작업트리 상태 ⑦핸드오프 형식"으로 절을 고정했고, **"전제를 의심해도 된다"를 명시**한 것이 5개 트랙에서 브리핑 오류를 반증하게 했다(3px 원인·`?share=` 파라미터 3종·pitfalls 91 vs 89 케이스·`useMainComputed` 3지점 vs 4지점·ROADMAP-v2 의 트랙⑬ 부재).
