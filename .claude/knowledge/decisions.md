# 확정된 결정 (되돌리려면 사용자 승인 필요)

## 데이터·색 규율
- [2026-07-16][design] 상승=빨강/하락=파랑 (한국 증권 관례) — **전 테마 프리셋 공통 불변**. 데이터색은 크롬(버튼·에러)에 쓰지 않는다.
- [2026-07-16][design] 숫자(데이터)에 accent 색 금지 — accent는 배지·아이콘·장식 전용.
- [2026-07-16][design] 그라데이션 이원화: `gradient-cta`(버튼 채움 전용) vs `gradient-aurora`(리본·장식 전용) — 교차 사용 금지.
- [2026-07-17][design] CTA/브랜드 채움 위 라벨은 **반드시 `color.onBrand`** — 흰색 하드코딩 금지. velog·sunset·ink 다크는 라벨이 어두운색으로 반전된다 (README §1 교정 완료).
- [2026-07-17][design] Toggle 썸은 정적 `#ffffff` — onBrand를 비브랜드 표면에 쓰면 velog 다크에서 소실(1.07:1 사고 이력).

## 테마 프리셋 시스템
- [2026-07-17][design] 프리셋 8종, 기본 = `velog`(표시 라벨 "미니멀 그린" — 타사명 노출 회피, 사용자 고지됨). 순서: velog/forest/aurora/vivid/navy-gold/grape/sunset/ink.
- [2026-07-17][design] velog 프리셋 라이트 bg는 **의도적 무틴트**(#f8f9fa, open-color 충실성이 정체성). 나머지 프리셋은 색상군 틴트 bg.
- [2026-07-17][arch] 팔레트 계약: `html[data-palette]` 어트리뷰트 + localStorage `snowball:palette` + `palettePresetAtom`(쓰기 시 DOM **동기** 반영 — 차트 stale 방지). 스펙 문서와 어긋나면 **먼저 랜딩한 구현이 정본**.
- [2026-07-17][design] knife-edge 대비 하한 (더 조정 금지, presets.ts 주석 참조): navy-gold 라이트 글로우 4.52 / grape 다크 brand 3.19 / sunset 시리즈0 3.19 / ink 라이트 data-positive 4.50.

## 커뮤니티
- [2026-07-17][product] 댓글은 1단계 평면 스레드 — 대댓글에도 답글 버튼이 있지만 **답글의 답글도 루트 스레드에 달린다**(velog·유튜브식). 컴포저는 스레드 맨 아래.
- [2026-07-17][product] 댓글 스팸 레이트리밋: 1분 10개 (DB 트리거, 20260714000000_community.sql). 의도된 동작 — UI는 에러를 반드시 표시해야 한다.
- [2026-07-17][design] 갤러리 카드/행 푸터는 **닉네임만**(아바타 썸네일·"by" 접두어 제거 — 사용자 verbatim "아바타 썸네일+'by' 제거 → 닉네임만"). 카드 포맷 = velog 글 카드(제목→요약→서브정보→구분선→닉네임|♥), Row 메타도 "by" 제거. ⚠ 이 항목이 아래 profile-v2의 "갤러리 카드도 이니셜 아바타로 통일"을 **덮어쓴다**(카드/행은 아바타 자체를 렌더 안 함) — 다른 아바타 소비처(댓글·상세·AuthControl)는 이니셜 아바타 유지. 근거 components/community/ScenarioCard/ScenarioCard.tsx:59, ScenarioRow/ScenarioRow.tsx:36.
- [2026-07-17][design] 갤러리 카드 정보 = velog 글 카드 포맷. **카드 정보 = 3층 위계**(구 "hero1+보조2+배지1 초과 금지"를 사용자 요청으로 완화): ① hero 1(월 배당 세후, 강조) ② 보조 2(최종자산·투입대비) ③ 배지(목표 달성) ④ **조건 컨텍스트 1줄**(초기·월적립·기간·티커/목표 — 전부 textMuted·무강조·최대 2줄 clamp). 원칙은 유지: **정보량은 "개수"가 아니라 "위계"로 규율** — 추가 정보는 가장 약한 컨텍스트 줄에만, 제목·hero를 죽이지 않는다. card variant만 확장(attach/inline 절제 유지). 분기: 달성=티커 표시/목표는 배지, 미달성=목표 월배당 표시/티커 생략, targetMonthlyDividend=0이면 목표 생략.
- [2026-07-17][arch] `sim_summary`는 **게시 시점 고정**(재계산 금지) — 카드·상세·OG·첨부 미리보기 표기 일치가 목적. 프리뷰 렌더 조건은 sim_summary 존재(has_payload 아님). 요약 숫자 표기는 `formatSummaryKRW`(shared/utils, "187만원"/"9.2억") **단일 포맷터** — `formatApproxKRW`("약 187만")를 커뮤니티 표면에 직접 쓰면 화면 간 표기가 어긋난다. 글쓰기 첨부 미리보기만 예외적으로 클라이언트에서 `buildScenarioSimSummary`를 실행(표시 전용, 저장 아님 — useScenarioCandidates의 candidate.summary).
- [2026-07-17][design] 콘텐츠 카드 radius.xs vs 도구 카드 radius.lg — 의도적 구분.
- [2026-07-17][product] 글쓰기 라우트는 `/community/write` (구 /new에서 변경, 미배포라 리다이렉트 없음).
- [2026-07-18][product] 글쓰기 시뮬 첨부 = **"첨부" 토글 + 1단계 피커**(구 2단계 커밋 폐기 — 사용자 버그: 라디오만 고르고 버튼을 안 눌러 payload=null 저장되던 함정). 섹션 제목 "시뮬레이션"(구 "시뮬레이션 첨부") **우측에 common `ToggleField`("첨부")** — FormSection은 title 우측 슬롯 미지원이라 이 섹션만 헤더를 로컬 조립(AttachSection/AttachSectionHeader/AttachSectionTitle, 타이포는 SectionTitle과 동일). **토글 OFF(기본)=미첨부·피커 숨김**, **ON=피커 노출**, 시나리오 카드를 **고르는 즉시 composer.attachScenario 커밋**(별도 버튼 없음), **토글 OFF=composer.detachScenario**. ⚠ 앞서 랜딩한 "첨부 안 함 라디오 맨 앞"은 **이 토글이 대체**(라디오 제거, `attachNoneLabel`/`attachNoneHint` 카피 삭제) — 되살리지 말 것. optional 유지: 토글 OFF거나 카드 미선택이면 본문만으로 게시 가능(canSubmit=title&&(body||attachedPayload) 불변). 무효 payload는 여전히 비활성 카드+사유, 선택가능 0개면 빈 상태("시뮬레이터로 가기"). **수정 모드 외부 첨부**(서버 payload가 후보와 매칭 안 됨)는 `useEffect`로 **토글 자동 ON** + 요약 카드만 노출(해제=헤더 토글, 카드 내 버튼 없음). 삭제 카피: `attachButton`/`attachOptionActiveTag`/`attachRepick`/`attachAttachedStatus`/`attachNoneLabel`/`attachNoneHint`, styled `OptionActiveTag`/`PickerActions`. 추가 카피 `attachToggleLabel='첨부'`. sim_summary 계산·저장·publish·검증·이탈방지 **불변**(첨부 UX만). 토글 상태(attachEnabled)는 **뷰 로컬**(composer/스키마 무변경). 근거 pages/Community/CommunityWritePage/CommunityWritePage.view.tsx(handleToggleAttach·attachEnabled effect·ScenarioPicker), useScenarioCandidates.ts(isActive/defaultSelectedId는 훅에 남지만 뷰 미사용).
- [2026-07-18][design] 글쓰기 페이지 = **surface 면색 패널로 card화**(사용자 피드백 "배경색과 너무 똑같아 밋밋"·"절제된 단색=허접"). WriteForm에 surface bg + `border` + `shadow.e1` + `radius.lg`(도구 카드 계열) + clamp 패딩 — bg 위에서 뜨게(토큰만, 전 8프리셋·라이트/다크 bg≠surface 유지). 옵션 카드 배경 surface→**surfaceSunken**(패널 위에서 자체 카드로 뜨게, velog PreviewBlock 관례). "지금 열려 있음" 태그 제거(사용자 요청). PickerGrid는 **scrollbar-gutter/overflow/max-height 제거**해 폼 폭에 꽉 맞춤(구 `scrollbar-gutter:stable`이 우측에 스크롤바 폭을 상시 예약해 다른 필드와 우측 정렬이 어긋나던 사용자 피드백 해소). 게시 설정의 **공개/비공개 안내 문구는 토글 아래가 아니라 옆**(VisibilityRow=flex row, ToggleField "비공개" + VisibilityText 나란히, 상태 따라 문구 전환). 근거 pages/Community/CommunityWritePage/CommunityWritePage.styled.ts.
- [2026-07-17][product] 프로필 설정 `/community/profile` **v2 — 프로필 사진 기능 폐기**(사용자 verbatim "프로필 사진은 사용하지 않는게 좋을거 같아 썸네일 UI는 다 삭제해줘"). 구조 = **닉네임 카드 + 그 아래 회원 탈퇴 아코디언**(기본 접힘, danger 톤, 헤더 button+aria-expanded/aria-controls, 클릭 시 탈퇴 버튼 노출→DeleteAccountDialog 무변경). 아바타 썸네일·업로드·변경/제거 UI·관련 카피 전부 삭제. **작성자 아바타 표시는 전 소비처(댓글·갤러리 카드·상세·AuthControl) 이니셜 아바타로 통일** — 권고안=`Avatar` 컴포넌트가 avatar_url 무시하고 항상 이니셜(단일 지점, 소비처 무변경). OAuth **이메일은 여전히 미노출**(훅이 email 미반환). 진입점=AuthControl 드롭다운. avatar_url 컬럼·Storage `avatars` 버킷·카카오 프로필 이미지 scope 실삭제는 후속(표시만 끄면 앱은 안 죽음). ⚠ **구 결정(좌 아바타+닉네임 통합/우 탈퇴 2단, layout 980 스택)은 이 항목으로 폐기** — 아바타 복원 금지. 근거 pages/Community/CommunityProfilePage, components/community/Avatar.
- [2026-07-17][product] 회원 탈퇴는 **200 확정 시에만** 로그아웃·이동(성공 위장 금지, 스코핑 F3). 실패 시 다이얼로그 유지+에러. 순수 `runAccountDelete`(pages/Community/CommunityProfilePage/hooks/accountDeletion.ts)에 계약을 못 박아 UI가 우회 못 하게 함. 로컬 dev는 /api 부재라 항상 `deleteFailed`가 정상 경험.
- [2026-07-17][design] 갤러리 "정밀 검색"(월배당·목표·기간·티커 필터)은 검색바 우측 아이콘(lucide `SlidersHorizontal`) 트리거+활성필터 카운트 배지로 노출. 상호작용 모델 = **텍스트 검색은 즉시(디바운스) 유지, 정밀필터는 명시적 "적용" 2단계 커밋**(min/max 상호의존 검증 + 매 키입력 리페치 방지). "초기화"는 필터 파라미터만 지우고 정렬·텍스트검색 보존. 반응형은 @media 대신 **variant prop으로 구조 분기**: 데스크톱=앵커 팝오버(비모달, ThemePresetSwitcher와 동일 — Tab-out/Esc/바깥클릭 닫기, 포커스 트랩 없음), 태블릿/모바일(drawer↓)=in-flow 인라인 패널(오버레이·포털·트랩 없음 — MySavePanel 포털 함정 회피). URL 파라미터는 canonical **원 단위**(mdmin/mdmax/tgtmin/durmin/durmax/ticker), UI는 만원/년 표기. **숫자 facet은 sim_summary jsonb의 텍스트 필터로 못 건다**(PostgREST `->>`는 사전식 비교) → generated column+인덱스 필요(state-engineer/마이그레이션 의존, UI는 언블록). 티커 필터는 컬럼 준비 전까지 플래그로 섹션 자체를 숨김.
- [2026-07-18][design] 정밀 검색 **구현 랜딩**(위 결정의 코드화): 컴포넌트 폴더명 = **PrecisionSearch**(components/community/PrecisionSearch) — 스펙 초안(precision-search-spec.md)의 `CommunityFilterMenu`가 아니라 브리핑/오케스트레이터 명명 채택(리네임 금지). **자기완결형**: `useSearchParams`로 URL을 직접 read/write(CommunitySearchBar 패턴), useGallery에 필터 콜백을 프롭 드릴하지 않는다(useGallery는 facets 조회+filteredEmpty status+clearFilters[CTA용]만). **필터 타입 2개는 의도**: UI/URL 모델 `GalleryFilters`(mdMin/mdMax/tgtMin/durMin/durMax, 원·년 — shared/constants/community/galleryFilters.ts)와 데이터레이어 `GalleryFacetFilters`(monthlyMin/…)는 **별도**이고 `toFacetFilters`가 매핑(키만 다름, 값·단위 동일) — 하나로 합치지 말 것. 티커 필터는 `TICKER_FILTER_ENABLED=false`(config.ts)로 섹션 자체 미렌더(G2). 검증은 zod 대신 순수 min>max 비교(과도한 추상화 회피, 스펙 §11 zod 대비 기능동일).
- [2026-07-18][design] 갤러리 리스트 **행 = B안**(구분선 피드 유지, 카드화 기각): hover 시 좌측 `accentBorder` 레일(평상시 transparent로 자리확보→점프 방지) + `surfaceHover` 배경 동시 변화, 우측 `surfaceSunken` 숫자 칩(카드 PreviewBlock 축약). 숫자 칩 = **row 숫자 클러스터**(hero 월배당 xl + 목표 배지 + 최종자산·투입대비 체인) — 카드와 **동일 카피**("월 배당(세후)"/"최종 자산"/"n년차 목표 달성"). `SimSummaryStats`는 **'inline' variant 제거, 'row' 신설**. reduced-motion: 레일/배경 전이는 duration만(transition-delay 금지). 근거 components/community/ScenarioRow, SimSummaryStats.
- [2026-07-18][product] 글쓰기 **수동 "요약(description)" 입력칸 폐지 → 카드 미리보기는 항상 본문에서 자동 발췌**(사용자 verbatim "요약 선택은 필요 없어, 내용을 미리보기로"). composer.submit의 `finalDescription = safeBody ? deriveExcerpt(safeBody, COMMUNITY_DESCRIPTION_EXCERPT_LENGTH) : null`(수동 description 분기 삭제). description/setDescription state·타입·반환 전부 제거, 게시설정 FormSection엔 공개범위만 남음. 본문 없이 첨부만 있는 글은 description=null → 카드가 텍스트 발췌 대신 sim_summary를 노출(정상, 위 sim_summary 결정과 정합). **DB `description` 컬럼·write/update 저장 경로·카드/행 렌더는 불변**(값 출처만 본문 발췌로 단일화). 삭제 카피: `write.fieldDescription`/`descriptionPlaceholder`, 스타일 `DescriptionTextarea`. 근거 pages/Community/CommunityWritePage/hooks/useScenarioComposer.ts, CommunityWritePage.view.tsx.
- [2026-07-18][design] 커뮤니티 헤더 로고 = 시뮬레이터 HeaderLogoImage·파비콘·PWA와 통일한 원형 `/app_icon.png` `<img>`(장식 `alt=""` — 워드마크 BrandWordmark가 접근명, MobileMenuDrawer 선례). 구 `BrandMark` SVG 마크는 헤더에서 제거하되 **컴포넌트 파일은 삭제 금지**(api/og.tsx가 계속 소비). 근거 components/community/CommunityHeader.tsx.
- [2026-07-18][design] 상세(읽기) 페이지 **surface화**(사용자 피드백 "배경이랑 색이 똑같아 밋밋"): 본문(제목·메타·본문·첨부 CTA·좋아요)을 `PostCard`, 댓글을 `CommentsCard` 두 개의 surface 패널로 배경(bg) 위에 띄운다 — 갤러리 velog 카드와 같은 언어(`color.surface`+1px `color.border`+`shadow.e1`, 콘텐츠 카드 `radius.xs`, 하드코딩 색 0). 구조·카피 불변, 대비·계층(헤더 하단 hairline)·여백만 정돈. `Article`을 grid+gap으로 바꾸며 DetailHeader `margin-bottom`/AttachCta `margin`/LikeRow `margin-top`을 제거(gap이 대신). CommentsCard는 `& > section`(자기 요소 선택자, 컴포넌트 셀렉터 아님)으로 CommentSection 루트의 자체 top divider(margin/padding/border-top)를 상쇄 — 카드 경계가 그 분리를 대신한다. 근거 pages/Community/CommunityDetailPage/CommunityDetailPage.styled.ts(PostCard/CommentsCard), .view.tsx.
- [2026-07-18][frontend] 상세 페이지 **첨부 시나리오 미리보기(숫자+파이) 추가** — CTA와 **한 카드로 병합**: `AttachUnit`(단일 surfaceSunken+brandBorder+radius.lg+overflow) 안에 위=CTA 배너(brandSubtle, 자체 테두리·radius 제거)·아래=미리보기 아코디언(자체 테두리·radius·bg 제거, 헤더 `border-top` 한 줄로 seam) 순으로 담아 이중 테두리·간격 없이 한 덩어리로 읽히게(사용자 요청). 같은 조건(`scenario.payload && openInSimulatorHref`)에서 렌더. **아코디언(기본 접힘)**: 헤더 button(aria-expanded/aria-controls) + ChevronDownIcon(장식) → 펼침 region(role=region, aria-labelledby). **파이(ECharts)는 펼칠 때만 마운트**(pieOption useMemo `open` 가드 + 조건 렌더 — 첫 페인트 부담↓, lazy 청크와 정합). `hidden={!open}` 패널은 styled에 `display`를 두지 말 것(안 그러면 author display가 UA `[hidden]{display:none}`을 이겨 접혀도 보인다). **숫자**=`buildScenarioSimSummary`(순수, 표시전용)→`SimSummaryStats variant='card'`. **파이**=시뮬레이터 `buildAllocationPieOption`(pages/Main/utils) **그대로 재사용**(동일 비주얼: 외곽라벨·슬라이스경계·중앙 월배당) + 정규화도 `getIncludedProfiles`+`buildNormalizedAllocation` 재사용 — **레전드·슬라이더·티커태그 없음(파이 하나만)**. 캔버스 색은 `getChartTheme().series`(var() 불가), 팔레트 전환 리빌드=`usePalettePresetAtomValue` useMemo 의존성. 파이는 `role=img`+정규화 비중 aria-label. 배치=**페이지 로컬** `pages/Community/CommunityDetailPage/components/ScenarioPreview`(ResponsiveEChart는 `@/pages/Main/components/ResponsiveEChart`에서 재사용=내부 lazy echarts, 테스트는 test/community/ScenarioPreview.test.tsx에서 ResponsiveEChart mock). copy `detail.previewTitle/previewChartLabel` 추가. 근거 pages/Community/CommunityDetailPage/components/ScenarioPreview/, CommunityDetailPage.view.tsx.
- [2026-07-18][design] `SimSummaryStats` **목표달성 배지를 금액 밑으로** 이동(사용자 요청): card·row 헤드행(구 `CardHeadRow`/`RowClusterHead` 삭제)엔 hero 라벨만 남고, 순서=**라벨 → 금액(HeroValue/RowHero) → 배지**. 배지는 `BadgeRow`(flex)로 감싸 세로 스택 stretch로 pill이 폭 전체로 늘어나는 걸 막는다(왼쪽 정렬). attach variant는 배지 없어 무관. **갤러리 카드/행에도 동일 반영=의도**(일관성). 배지 카피/스타일 불변(위치만). 근거 components/community/SimSummaryStats/SimSummaryStats.tsx·.styled.ts(BadgeRow).
- [2026-07-18][design] 테마 스위처를 **로그인 시 AuthControl 프로필 드롭다운 안으로** 이동(사용자 요청 "테마 아이콘을 프로필 메뉴 밑으로"). 팝오버 중첩을 피하려 드롭다운에 "테마" 디스클로저 MenuItem(aria-haspopup/aria-expanded/aria-controls) → 클릭 시 radiogroup을 **인라인**으로 편다(선택해도 드롭다운 유지=비교/전환 편의). 이를 위해 `ThemePresetSwitcher`에 `variant='menu'` 신설(inline과 달리 드로어 미디어 숨김·보더 박스 없이 radiogroup만; 여백·스크롤은 AuthControl `ThemePanel`이 소유). copy `nav.theme='테마'` 추가. ⚠ **테마 접근성은 어떤 상태에서도 사라지면 안 됨** — 분기: 비로그인(드롭다운 없음)=헤더에 그대로, 커뮤니티 비활성(AuthControl 없음)=헤더에 그대로, **로그인+커뮤니티 활성일 때만** 헤더에서 숨기고 드롭다운으로. CommunityHeader는 `!isLoggedIn`일 때만 ThemeSlot 렌더, Main.view는 `themeInProfileDropdown=isCommunityEnabled&&isLoggedIn`일 때만 헤더 `<ThemePresetSwitcher/>` 숨김(CloudSyncIndicator/headerStatus·드로어 inline 변형은 불변). Main.view의 `useIsLoggedInAtomValue` 구독은 로그인/로그아웃에만 바뀌어 타건 memo를 안 깬다. 근거 components/community/AuthControl/AuthControl.tsx, components/ThemePresetSwitcher/ThemePresetSwitcher.tsx(menu variant), components/community/CommunityHeader/CommunityHeader.tsx, pages/Main/Main.view.tsx.
- [2026-07-18][community] **시드(합성) 계정 추적 = `profiles.is_seed boolean not null default false`**(서버만 설정, 화면 표시 안 함, 나중 gradual de-seed). 콜드스타트 시딩 페르소나 20명을 식별·집계제외·순차삭제하기 위한 **내부 플래그**. **`is_seed`는 service_role만 쓴다** — 방어는 RLS가 아니라 **컬럼 GRANT**(profiles UPDATE는 `display_name`/`avatar_url`만 grant, 20260714 community.sql:714 — 새 컬럼은 자동 미포함이라 authenticated는 permission denied). `handle_new_user`는 is_seed를 안 건드려 신규 가입자는 default false. de-seed는 **삭제순서 likes/comments → scenarios → 계정** + "오래된 N개" 파라미터화(migration `deseed_oldest_seed_scenarios(p_limit)`, service_role 전용, 실사용자 댓글 달린 시드글은 보호=고아 방지). 계정 자체 삭제는 auth.users admin API(api/account-delete.ts 선례) 서버 몫. 근거 supabase/migrations/20260721000000_seed_tracking.sql, docs/community-seeding/README.md §5.7.
- [2026-07-17][design] 소셜 로그인 버튼(구글/카카오/네이버) 색·로고는 **브랜드 규정색 하드코딩 — 테마 토큰 예외**다. 토큰화(`--sb-*`)하면 프리셋 따라 색이 바뀌어 규정 위반 + 카카오 OAuth 앱 심사 반려 사유. 구글=흰바탕+#747775 테두리+4색 G, 카카오=#FEE500+검정 85%, 네이버=#03C75A+흰 N. Toggle 썸(#ffffff)과 같은 "토큰화가 항상 개선이 아니다" 케이스 — 되돌리지 말 것. 근거 components/community/SocialLoginButton/SocialLoginButton.styled.ts.

## 클라우드 저장
- [2026-07-18][product] **"데이터 저장" 패널(MySavePanel) 완전 제거 — 자동저장(latest-wins 클라우드 동기화)이 대체**(사용자 확정). `components/MySavePanel/` 폴더·툴바 "데이터 저장" 버튼(+CloudSyncIndicator 배지)·`onOpenMySave` 배선 삭제. 툴바 퀵액션은 이제 **[공유] 하나뿐**(Coffee는 display:none 유지) → `TickerQuickActionRow` 그리드 2열→**단일 열 전폭**. 연쇄 dead code 제거: usePortfolioPersistence의 이름슬롯/JSON 함수(list/load/delete/download/JSON import) + `pages/Main/hooks/persistence/savedName.ts`(파일 통삭제, 배럴에서 제거) + `appStateStorage.ts`의 이름슬롯 IO 4종(list/read/write/delete-ByName). **보존**: buildPayload·applyPersistedPayload·readLocalAutosaveForSync·createShareLink·retryCloudSave(Track B 렌더게이트·공유·동기화가 계속 씀). indexedDB.deleteDatabase·통삭제는 안 함(코드 경로만 제거). 되돌리려면 사용자 승인.
- [2026-07-18][design] **동기화 상태 표시는 헤더로 이전**(패널 제거로 CloudSyncIndicator가 노출될 곳이 사라진 것 보강 — 무음 실패 금지). `CloudSyncIndicator`에 `header` variant 신설: idle(비로그인/첫 렌더)은 **렌더 안 함**, 저장중/저장됨/오프라인은 아이콘만(라벨은 sr-only+title), **실패에서만 "저장 실패" 라벨+"다시 시도" 버튼을 편다**(로그인 사용자에게 저장 실패가 반드시 보이게). Main.view 헤더 액션 영역에 `isCommunityEnabled`일 때만 배치. 상태는 컴포넌트가 `cloudSyncStateAtom`을 직접 구독(컨테이너 불필요). 재시도 배선은 **훅 hoist 금지 규칙 준수**: retryCloudSave는 MainLeftPanel이 계속 소유하고 `onRegisterRetryCloudSave(fn|null)`로 함수 참조만 Main.view의 ref에 등록(setState 아닌 대입 → 헤더 크롬 memo 유지), 헤더는 안정적 래퍼로 그 ref를 호출. 근거 components/CloudSyncIndicator/CloudSyncIndicator.tsx, pages/Main/Main.view.tsx, pages/Main/components/MainLeftPanel/MainLeftPanel.tsx.
- [2026-07-17][product] **명시적 저장 없는 순수 자동저장 + 조용한 로드로 전환**(사용자 확정, 구 "충돌 프롬프트" 폐기). 세션 시작 시 클라우드 autosave를 pull해 현재와 다르면 **조용히 적용**(last-write-wins 수용 — `CloudConflictPrompt` 폴더·`CLOUD_CONFLICT_RESOLVED` 이벤트 삭제됨). IndexedDB→클라우드 마이그레이션은 **자동·안전**(`useCloudWorkspaceSync`에 배선): 클라우드 비었으면 로컬 autosave push→**read-back 확인→확인된 autosave 슬롯만** 정리(이름 슬롯·비로그인 데이터 불변), 클라우드에 있으면 정본 채택+로컬 보존(push 안 했으니 정리 대상 없음). 실패(push·read-back·로컬읽기·pull)는 전부 로컬 보존+플래그 미설정(다음 로그인 재시도, 무음 실패 금지). 근거 jotai/snowball/cloud/cloudWorkspaceSyncEngine.ts(순수 엔진).
- [2026-07-17][product] 클라우드 저장은 **의미있는 액션만** 유발(사용자 확정): 티커 추가/삭제·비중·초기금·월적립·기간·세율·재투자·시나리오 탭 추가/삭제/이름·목표. 배제=탭 전환·뷰 토글·티커 선택·테마. 디바운스 4000ms 유지. no-op 게이트가 본질(API 절감).
- [2026-07-17][product] **명시적 체크포인트 저장 폐기 완료 — 클라우드 = 단일 워크스페이스 자동 동기화**(MySavePanel 정리 트랙). `useCloudSync.saveCheckpoint`·`userAppStates.saveCloudCheckpoint`·MySavePanel 체크포인트 폼/수동 마이그레이션 배너 전부 삭제. 마이그레이션은 세션 시작 시 자동(useCloudWorkspaceSync). 클라우드 슬롯 목록/열기/삭제(list/fetch/delete)는 **MySavePanel 삭제로 제거됨**(2026-07-18, 사용자 승인 — 노출 UI가 사라져 프로덕션 0-caller가 됐다). `useCloudSavedStates`는 세션 동기화용 `pullAutosave`/`pushAutosave`만 남고, `userAppStates.ts`는 `fetchCloudAutosave`/`pushCloudAutosave`만 남는다(atom `cloudSavedStatesAtom`·`CloudSavedStateSummary`도 제거). buildPayload는 MySavePanel prop에서 제거(체크포인트가 유일 소비처였음). 근거 shared/lib/supabase/userAppStates.ts, jotai/snowball/cloud/useCloudSync.ts.
- [2026-07-18][arch] **세션시작 로컬 autosave read는 하이드레이션 ↔ 세션시작 sync가 1회 공유**(M1 유실 gap 보강). 예전엔 usePortfolioPersistence hydrate와 useCloudWorkspaceSync가 IndexedDB를 각각 독립 read → 두 read 불일치 시(하이드레이션 성공+sync read 실패, cross-tab blocked 등) 엔진이 더 오래된 cloud를 apply → 잠기지 않은 app autosave가 로컬 최신본을 덮는 유실 경로가 있었다. 해법=`createSessionLocalAutosaveCache`(jotai/snowball/cloud/sessionLocalAutosave.ts): 하이드레이션이 1회 read·캐시→`readLocalAutosaveForSync`로 노출→sync가 재사용(L2 이중 read도 자연 해소). 불변식="로컬을 못 읽으면 로컬을 덮어쓰지 않는다"가 app autosave 경로까지 성립. **되돌리려면(sync 독립 read 복원) 사용자 승인 필요** — 유실 경로 재개방. 근거 test/persistence/sessionLocalAutosave.test.ts.
- [2026-07-18][arch] **공유가 이중 포맷**(트랙 E, 사용자 확정): 신규 `?s=<key>`(DB 스냅샷 key) + 구 `?share=`(lz-string) **병존**. 포맷 감지=**파라미터 이름**(둘 다 있으면 `?s=` 우선). 생성은 DB 우선(`createSharedSnapshot`→`?s=`), **폴백**: `getSupabaseClient()` null(미설정/테스트/미배포) 또는 INSERT throw면 구 lz-string `?share=`로 폴백해 **공유는 항상 성립**(무음 실패 금지, 반환 `{ok,url,copied}` 불변). 복원 `?s=`는 **비동기**(fetch, cancelled 가드) + 실패=`OPERATION_ERROR`+쿼리정리. `?share=` 디코드 경로는 **절대 불변**(구 링크 영구 유효). 서버=`shared_snapshots`(RLS 정책 0개 + SECURITY DEFINER RPC `create_shared_snapshot`/`get_shared_snapshot`만, anon 실행 GRANT=구 익명공유 동등 UX, payload jsonb≤64KB, key=서버생성 base64url~22자). **payload 계약 = `{ v:1, scenario: PersistedScenarioState }`** — OG(api/og)가 이 payload로 카드를 그린다(형태 변경 시 OG 동반 수정). 읽기 scenario는 신뢰불가 → `normalizeSharedSnapshotScenario`(normalizePersistedAppState)로 정규화. 근거 supabase/migrations/20260720000000_shared_snapshots.sql, shared/lib/supabase/sharedSnapshots.ts, pages/Main/hooks/persistence/usePortfolioPersistence.ts(createShareLink·restore effect)·shareUrl.ts(S_QUERY_PARAM).
- [2026-07-17][product] **PNG 캡처 기능 완전 폐기**(구 Stage 2 "Capture 강등" → 전면 삭제, 사용자 확정). `components/TickerCreation/capture/*`+`capturePage.ts` 삭제, 툴바 퀵액션 = **[내 저장][Share]**(Coffee 숨김 유지), html2canvas 의존성 제거(유일 소비처가 capture/tiling.ts였음). 재도입 금지 — 되살리려면 사용자 승인. 관련 계측(`CTA_CLICK quick_action_capture`·`OPERATION_ERROR capture_page`)도 소멸(analytics.ts OPERATION_ERROR 상수 자체는 저장/불러오기용으로 존치).

## 도메인 (배당 시뮬레이터)
- [2026-07-15][engine] 배당은 가격에 앵커: `priceGrowth = dividendGrowth`, `totalReturn = dy + dg`(파생값). 티커 3분류: 가격·배당률·주기=관측 사실(크론 갱신) / expectedTotalReturn=사람의 가정(자동화 금지) / dividendGrowth=파생(etr−dy, 과거 CAGR로 덮어쓰기 금지).
- [2026-07-18][data] **티커 자동 갱신 기본 프로바이더 = Yahoo Finance**(무료·무키, `scripts/tickerRefresh/provider/yahooProvider.ts`), FMP는 `--provider=fmp` 옵션으로만 유지(유료 키 보유자용). 근거는 pitfalls.md "FMP 무료 티어 불가" 항목. 워크플로(`.github/workflows/refresh-tickers.yml`)도 기본 Yahoo로 전환돼 `FMP_API_KEY` 시크릿 없이 주간 크론이 돈다.
- [2026-07-18][data] ~~**티커 갱신 = 월~금 요일 분할(5버킷) + 주간 누적 PR**~~ **(대체됨 → 아래 월간 자동머지)**. `partitionTickers`(정렬 후 `index%5`, 순수 함수)와 CLI `--bucket=N`은 수동용으로 **남겨둠**(테스트도 유지). `--only`가 `--bucket`을 완전히 대체하는 원칙도 유지.
- [2026-07-18][data] **티커 갱신 = 월 1회 전체 갱신 + 자동 머지**(사용자 요청으로 위 요일분할을 대체). 매월 1일 크론(`0 21 1 * *` = 2일 06:00 KST)이 유니버스 전체를 한 번에 갱신하되, `--delay=2000`(요청 2s 간격, ~68종 ≈ 2–3분)으로 러너 IP 버스트를 막는다 — 레이트리밋 방어를 **요일분할 대신 요청 딜레이**로 달성. 게이트(`tsc -b tsconfig.build.json` + `test:ci`) 통과 후에만 `chore/refresh-ticker-data` 브랜치에 커밋→PR 생성→**자동 머지(`gh pr merge --merge --delete-branch`) = Vercel 배포**. 스케줄 실행은 항상 자동머지, 수동 dispatch는 `auto_merge=false`로 PR만 열고 검토 대기 가능. 브랜치는 매월 fresh(푸시 전 remote 브랜치 삭제 → `--force` 불필요). **⚠ 이 자동머지는 [[ask-before-deploy]] '배포는 승인 후' 원칙의 명시적 예외 — 사용자가 티커 데이터에 한해 자동 배포를 승인한 것**. 근거 `.github/workflows/refresh-tickers.yml`.
- [2026-07-16][product] 아이콘은 lucide-react, strokeWidth 1.8 통일. 클리셰 글리프(🚀 로켓, 안락의자 등) 금지 — "올드함"의 원인은 라이브러리가 아니라 글리프 선택.
