# 확정된 결정 (되돌리려면 사용자 승인 필요)

## 데이터·색 규율
- [2026-07-16][design] 상승=빨강/하락=파랑 (한국 증권 관례) — **전 테마 프리셋 공통 불변**. 데이터색은 크롬(버튼·에러)에 쓰지 않는다.
- [2026-07-16][design] 숫자(데이터)에 accent 색 금지 — accent는 배지·아이콘·장식 전용.
- [2026-07-24][design] **환율 위젯(ExchangeRateWidget)은 pm-po AC11의 "값에 중립 토큰만"을 사용자 피드백("너무 밋밋")으로 완화** — 위젯 **크롬(아이콘 배지·타이틀·테두리)** 엔 accent 서브틀 색 허용(`accentSubtle`/`accentText`/`accentBorder`, Chip accent 변형과 동일 토큰·contrast.test 검증됨 line 85). ⚠ **단 환율 값 숫자엔 손익색(빨강=손실/파랑=이익)·accent 여전히 금지 + 등락 화살표·증감색 금지** — 환율은 P&L 이 아니다. 위 "숫자에 accent 금지"의 연장선(값은 `color.text` 중립만). 근거 components/ExchangeRateWidget/ExchangeRateWidget.styled.ts.
- [2026-07-16][design] 그라데이션 이원화: `gradient-cta`(버튼 채움 전용) vs `gradient-aurora`(리본·장식 전용) — 교차 사용 금지.
- [2026-07-17][design] CTA/브랜드 채움 위 라벨은 **반드시 `color.onBrand`** — 흰색 하드코딩 금지. velog·sunset·ink 다크는 라벨이 어두운색으로 반전된다 (README §1 교정 완료).
- [2026-07-17][design] Toggle 썸은 정적 `#ffffff` — onBrand를 비브랜드 표면에 쓰면 velog 다크에서 소실(1.07:1 사고 이력).

## 목표 달성 시각화 (feat/target-dividend-viz-v2)
- [2026-07-26][design] **"월 평균 배당" 차트는 목표 설정 시(target>0) 분할 토글과 무관하게 기본 노출**(`showSplitGraphs || hasTarget`) — 목표선·도달 마커가 숨은 토글 뒤에 갇히던 G1 해소. 자산 가치·누적 배당은 분할 전용 유지, `showSplitGraphsAtom` 기본값 불변. 배치는 현행 자리(B안) — **A안(SimulationResult 바로 아래 이동)은 사용자 확인 대기**, 독단 이동 금지. 근거 pages/Main/components/MainRightPanel/MainRightPanel.tsx.
- [2026-07-26][design] **목표 미설정 CTA 확정**: 서사 "목표 월배당을 정하면 도달 시점과 진행률을 함께 보여줘요."(구 "왼쪽 투자 설정…" 위치 안내는 ≤960px에서 거짓이라 삭제) + Chip accentAlt 3개(월 100/300/500만원 — GA value_bucket 경계 정렬) + "직접 입력"(ghost, ≤960px 드로어 오픈+목표 입력 rAF 포커스). 칩 클릭 → setField 경유(신규 atom 없음), 포커스는 rAF 뒤 서사로(aria-live 의도적 미사용). 콜백 미배선 시 액션 행 미렌더(격리 렌더 안전). 근거 components/SimulationResult/SimulationResult.tsx, test/snowball/simulationResultTargetCta.test.tsx.
- [2026-07-26][design] **도달 markPoint 라벨 = 핀 밖 success×successSurface 상태 칩, 전 뷰포트 상시 표시**(onBrand 라벨·모바일 숨김 분기 폐기 — 다크 2.36:1 실측 실패였음). 목표선 라벨·서사 금액은 표시 통화 인식(formatChartCompact/formatResultAmount — 원화 고정 formatApproxKRW는 이 표면에서 제거). 근거 pages/Main/utils/charts.ts, shared/styles/chartTheme.ts successSurface.

## 아이덴티티 (2026-07-28 아이덴티티 패스 — 브랜치 feat/identity-pass, 커밋 811f596·e478557·5015439)
- [2026-07-28][design] **서체 4역할 확정**(사용자 승인, 재설계 금지): `font.sans`=**Wanted Sans**(npm `wanted-sans`, 92분할 동적 서브셋) / `font.display`=**Gmarket Sans**(자체 91분할 서브셋, `h1~h6` 전역 1지점) / `font.heroNumeric`=**LINE Seed Sans KR**(숫자·단위 서브셋, 화면당 1곳) / `font.dataNumeric`=**Inter**(자체 서브셋)+`tabular-nums`. **4종 전부 SIL OFL 1.1** — 배포처 원문 + 폰트 `name` ID13 이중 확인, 고지 파일 `public/fonts/OFL-*.txt` 4개 동봉이 라이선스 준수 조건이다. **셀프호스팅만·CDN 금지**. Pretendard는 전면 제거(소스 잔재 0). ⚠ `dataNumeric` 스택 순서(1순위=우리 서브셋, 2순위=맨몸 `Inter`=OS 설치본 폴백, 그다음 `'Wanted Sans …'`)는 불변 — Inter엔 한글 글리프가 없어 표 안 "3종"이 폴백돼야 한다.
- [2026-07-28][design] 🔄 **CSS family 명은 앱 고유명으로 개명**(2026-07-28 시뮬레이터 대개편 미션, 사용자 지시). `Gmarket Sans` → **`Snowball Display`** / `Inter Variable` → **`Snowball Numeric`**. **파일명·폴더명·`OFL-*.txt`·저작권 고지는 원본 그대로**(폰트 woff2 바이너리·콘텐츠 해시 무변경 — `git status --porcelain public/fonts` 가 비는 것이 정상). 근거 = ①OFL §3 Reserved Font Name: 서브셋+포맷 변환으로 Modified Version이 되었으므로 원본 RFN을 CSS family로 선언하지 않는다 ②`'Inter Variable'`은 npm `@fontsource-variable/inter`와 **이름이 같아** 누가 설치하면 우리 서브셋이 조용히 무효화된다. `'LINE Seed Sans KR'`·`'Wanted Sans Variable'`은 **바꾸지 않는다**(전자는 RFN 선언 없음, 후자는 npm 패키지가 소유한 이름이라 바꾸면 동적 서브셋 CSS와 어긋난다). 동시 수정 지점 = `tools/fonts/build.mjs` → `node tools/fonts/build.mjs`로 `selfHostedFonts.css` **재생성**(손편집 금지) → `tokens.ts` → `typography.test.ts` → **`api/og.js`·`api/share-html.js` 재번들 동반 스테이징**.
- [2026-07-28][design] **워드마크 = 한글 "스노우볼 인컴", 텍스트 단독(아이콘 제거), C5 쿨 그라디언트 2색**: 라이트 스노우볼 `#3ba5d3→#79c5e6`·인컴 `#0d9488→#22a06b` / 다크 스노우볼 `#79c5e6→#aadcf2`·인컴 `#2dd4bf→#6ee7a0`. 단색 폴백 정본 `#3ba5d3`/`#0d9488`. **토큰 4개는 전 프리셋 공통**(`COMMON_LIGHT`/`COMMON_DARK`) — 로고는 스킨이 아니고, OG·파비콘엔 프리셋 맥락이 없어 프리셋별이면 "무엇을 구울지" 결정 불가다. `background-clip: text` + **폴백 3종 필수**(`@supports not`·`forced-colors: active`·`@media print` — 셋 다 없으면 글자가 통째로 사라진다). 렌더 지점은 `components/PrimaryNav` **단 하나**(세 헤더가 전부 이를 소비 — 복제 금지). 헤딩 레벨 `h1` 유지(span 강등은 10단계 R6).
- [2026-07-28][design] **세컨더리 액센트 = 틸·그린 축**. 골드·바이올렛 폐기, `accentAlt*`를 **16테마 × 4토큰 = 64 hex**로 그린 재정의(프리셋별로 "그린 계열이되 그 프리셋 색상군에 맞춘 값"). 앵커 `#22a06b`(라이트)/`#6ee7a0`(다크). ⚠ **`ink` 프리셋만 예외 — 2026-07-28 무채로 되돌렸다**(사용자 지시): ink의 정체성이 무채이고 "크롬 전 영역 무채(차트만 유채)"가 원래 서술이었다. 현재 정확한 서술 = **14테마 그린 + ink 라이트/다크 무채**(`#6b6b6b`/`#4f4f4f`/`#f0f0f0`/`#d6d6d6` · `#a3a3a3`/`#bfbfbf`/`#232323`/`#454545`). ink에서 `Chip variant='accentAlt'` 등 소비처가 회색으로 보이는 건 의도다. **🚫 노랑·골드는 제품 아이덴티티 축에서 전면 금지**(사용자 2회 지시) — 단 `navy-gold` **프리셋**의 골드 accent는 사용자가 고른 스킨이라 유지. 브랜드 블루 램프(`#0a6da3` 계열)는 인터랙션·활성 축으로 **유지**. `auroraViolet` 램프는 **삭제 금지**(aurora 리본/CTA stop-3가 계속 소비).
- [2026-07-28][design] **파스텔 히어로 그라디언트 신설** — `gradient-hero`/`gradient-hero-soft`, 16테마 × 2 = 32값. **웜톤 금지**(블루/틸/그린 파스텔), 각 프리셋의 자기 surface/bg에 쿨 캐스트를 섞어 파생. **장식 표면 전용**(PageHero 배경·EmptyState·프로모 카드) — 콘텐츠 카드 배경 금지. `gradient-cta`(버튼 채움) ↔ `gradient-aurora`(리본·장식)와 **교차 사용 금지**인 세 번째 계열이다. ⚠ 소비처는 아직 0(10단계에서 붙는다). ink는 hero↔bg ΔE 2.8이라 **fill 단독으론 밴드가 안 보인다 → 1px border 필수**. velog 다크 `text-muted` 위 **4.58:1은 knife-edge**(여유 1.8%) — 그 위에 더 옅은 것을 얹지 마라.
- [2026-07-31][design] **identity 4토큰 신설 — 아이덴티티는 스킨이 아니다**(`identity`/`identity-subtle`/`identity-border`/`identity-text`, `COMMON_LIGHT`/`COMMON_DARK` 이므로 **ink 포함 전 8프리셋 동일값**). 워드마크와 같은 논리이고 실제로 ink 에서도 워드마크는 이미 유채다 — accent 축의 무채 예외는 여기 적용되지 않는다. 값은 `palette.brand` 램프에서만 파생(새 hex 0): 라이트 600/50/200/700 · 다크 400/900/700/300. 다크 `identity-subtle`은 **`brand[900]`로 확정**(950 신설 불필요) — 그 위 `identity-text` 5.97:1 이고 다크 surface 와 ΔE 15.6(최저 aurora/dark)로 면이 읽힌다. 🚫 **identity 채움 위에 텍스트 금지**(다크 흰 라벨 2.79:1, on-brand 는 프리셋마다 명암이 갈려 대체 불가) — 아이콘·리본 등 비텍스트 전용, 면 위 라벨은 `identity-subtle`+`identity-text`. 소비처는 아직 0(S1 `PageHero`가 붙인다). 근거 shared/styles/presets/sharedTokens.ts:69,84.
- [2026-07-31][design] **페이지 정체성 hue `--sb-page-hue` 신설 + 히어로 3벌 → 1벌 수렴.** 배정(확정): `/`=identity(쿨 블루, 워드마크 첫 단어와 같은 색) · `/dividend/portfolio`=accentAlt · `/dividend/calendar`=accent · `/community/*`=brand · `/ticker/*`=**배정 안 함**(티커별 액센트 `--tk-*` 가 이미 그 화면의 색 체계다 — 겹치면 한 화면이 두 색으로 말한다). 소비 4곳 = 히어로 **상단 4px 리본 · 아이콘 배지 · 메타 줄 밑줄 · 테두리**(배경 `gradientHero` 는 전 페이지 공통 유지 = "같은 앱"). 🔴 **발행처는 `shared/hooks/usePageHue` 단 하나**(`document.documentElement`) — `PageHero` 에 색 prop 을 **의도적으로 두지 않았다**: ①커뮤니티엔 히어로가 없어 히어로가 발행하면 배정을 못 받고 ②라우트 전환마다 히어로가 lazy 언마운트돼 상단 내비 알약이 폴백색으로 깜빡인다. 소비처는 `var(--sb-page-hue, var(--sb-brand))` 로 읽는다(폴백=brand, 미배정 라우트는 현행 유지). 🔴 **hue 채움은 솔리드 금지 · 파생 면 위 텍스트 금지** — hue 4종은 라이트/다크에서 명암이 반대로 가고(라이트 accent-alt `#26a14f` ↔ 다크 `#6ee7a0`) `color-mix` 결과는 대비 테스트가 못 본다. 처방 = "옅은 틴트 면 + hue 글리프"(티커 상세 `--tk-soft`/`--tk-text` 와 같은 비율). 구 로컬 히어로 2벌(Portfolio·Calendar)은 삭제됐고 캘린더 `HeroDisclaimer` 는 `notice` 슬롯(`role="note"`)이 받는다. 근거 shared/styles/pageHue.ts, shared/hooks/usePageHue/usePageHue.utils.ts, components/common/PageHero/PageHero.styled.ts.

## 카드 위계 3단 (2026-07-31 — 리워크 S2/P1)
- [2026-07-31][design] 🔴 **한 카드는 테두리와 그림자 중 하나만 갖는다.** 3단 = **주역 `raised`(그림자 e2, 테두리 없음, 면 `surfaceRaised`) · 본문 `default`(테두리 1px, 그림자 없음) · 부속 `sunken`(면색만)**. 정본은 `shared/styles/surfaces.ts`의 **`cardElevation(tier)`** 한 함수이고, 카드 표면은 배경·테두리·그림자를 스스로 적지 않는다. 배정 = 시뮬레이터 주역은 `ResultSummaryCard`(`tone="raised"`) 하나, `/dividend/portfolio` 주역은 **"지금 받는 배당" 요약 카드**(hero 타일을 가진 그 카드 — 목표 달성·보유 종목은 본문). **화면당 주역 1개**. 구 `Card` 의 `elevation`(1|2|3) prop 은 **삭제**했다(호출부 0, 모든 카드가 테두리+그림자를 함께 갖는 한 화면에서 아무것도 바꾸지 못했다). 가드 `test/shared/cardElevationHierarchy.test.ts` + `components/common/Card/Card.test.ts`.
- [2026-07-31][design] **비중 슬라이더의 채워진 트랙 색 = 도넛 조각 색**(`var(--sb-chart-series-N)`, 인덱스 `% 8` — 캔버스 파이와 같은 규칙). 종전엔 전 행 트랙이 같은 브랜드색이라 "이 슬라이더가 어느 조각인가"가 연결되지 않았다. 🔴 **이건 카테고리 색이지 손익색이 아니다** — "숫자·표에 손익색 금지"의 예외가 아니라 **적용 대상 자체가 다르다**. 색이 붙는 곳은 **트랙(면)과 손잡이뿐**이고 비중 % 숫자는 계속 `color.text` 중립이다. 색이 유일한 단서도 아니다(같은 행에 티커명이 상시). 트랙 위에 텍스트를 얹지 마라 — 시리즈 색은 8프리셋 × 라이트/다크에서 명암이 갈린다. 슬라이더는 **자기 색표를 갖지 않는다**(호출부가 `CHART_SERIES_VARS` 로 넘긴다 = DOM 쪽 단일 지점, 캔버스는 `getChartTheme().series`). 실측 근거: 16조합(8프리셋 × 라이트/다크)에서 트랙 색이 도넛 캔버스 픽셀에 **64/64 존재**, 브랜드색이던 시절엔 9/16 조합에서 **0/4**였다. 근거 components/common/PortfolioAllocation/PortfolioAllocation.styled.ts, components/PortfolioComposition/PortfolioComposition.tsx.
- [2026-07-31][design] **주역 카드만 카드 제목을 생략한다** — 결과 요약 카드는 hero 숫자가 제목 역할을 한다(기존 결정 유지). 3단 도입 전에는 "제목 있는 카드와 없는 카드가 섞인" 우연이었지만, 이제 **뜬 면 = 제목 없는 카드**로 규칙이 된다. 본문 카드에는 제목이 반드시 있다.

## 앱 헤더 · 화면 순서 (2026-07-29 — 브랜치 feat/simulator-overhaul)
- [2026-07-29][arch] 🔴 **앱의 헤더는 `components/AppHeader` 한 벌뿐이다**(사용자 3회 반복 지시: "페이지가 달라도 같은 컴포넌트로 Header를 렌더링해야 한다"). 구 3벌(`components/SimulatorHeader` 삭제 · `CommunityHeader` · `TickerPageShell`)은 전부 이것을 소비한다. **AppHeader가 워드마크·라우트 메뉴·로그인(AuthControl)·더보기(⋯)를 직접 그린다** — 페이지가 "빼는" 옵션은 일부러 없고, 차이는 슬롯 prop(`status`/`center`/`actions`/`overflowMenu`/`below`/`contentGutter`)으로만 들어온다. 페이지별 실차이는 단 셋: ①시뮬레이터만 `brandAs='h1'` ②시뮬레이터만 PDF·튜토리얼이 붙은 자체 더보기(나머지는 `showTutorial={false}`) ③커뮤니티만 가운데 검색·모바일 검색 바·자기 본문에 맞춘 `contentGutter`. **복제 재발은 `test/shared/appHeaderSingleSource.test.ts`가 소스 수준으로 막는다**(렌더 테스트로는 못 잡는다 — 복제본은 각자 잘 동작한다). 헤더 높이는 하드코딩하지 않는다: AppHeader가 실측해 `--sb-app-header-h`로 발행하고 소비처는 `appHeaderHeight`를 쓴다(구 `--tk-header-h` 삭제).
- [2026-07-29][design] **시뮬레이터 헤더의 "설정 열기" 버튼 삭제**(사용자 결정) — 히어로 제목 우측 "투자 설정"과 역할이 중복됐다. 되살리지 마라. 대신 **히어로 버튼이 스크롤하면 헤더 아래에 고정**된다(`useStickyHeroAction` — 같은 버튼 하나를 `position: fixed`로 승격, 복제본 없음, 레이아웃 시프트 0). `SettingsEntryButton`의 `header` 변형과 카피 `SIMULATOR_COPY.settingsOpen`("설정 열기")도 함께 삭제 — 설정을 여는 라벨은 이제 `settingsTitle`("투자 설정") 하나다(테스트 헬퍼 `openSettingsDrawer`도 이 이름을 쓴다). 투어 앵커 `TOUR_TARGET.openSettings`는 히어로 버튼으로 이사했다.
- [2026-07-29][design] **클라우드 동기화 배지는 워드마크 오른쪽 같은 줄**(사용자 결정). 좁은 폭에서 아랫줄로 강등하던 `$ownLine` 규칙은 삭제 — 상태에 따라 헤더 높이가 달라지면 그 높이를 CSS 변수로 받아 쓰는 sticky 요소들이 상태마다 함께 튄다. ⚠ **지속 상태(저장 실패·동기화 보류 = 라벨+액션) ↔ 전이 상태(저장 중 = 아이콘만)의 차이는 유지**(4초 디바운스마다 도는 '저장 중'이 폭을 차지하면 헤더가 주기적으로 출렁인다).
- [2026-07-29][design] **카드·섹션 제목도 좁은 폭에서 줄어든다** — `shared/styles`의 `sectionTitleFontSize`(clamp 16~18px)가 **전 페이지 공통 규칙**이다(카드마다 다른 축소 곡선 금지). 하한을 본문(14px)보다 2단계 위에서 멈추는 이유: 헤딩 서체에 중간 굵기가 없어 위계가 **크기로만** 만들어진다. clamp 유동 항에는 `rem`을 섞는다(`vw` 단독이면 브라우저 기본 글자 크기 확대가 무시된다 — WCAG 1.4.4).
- [2026-07-29][design] **`/dividend/portfolio` 카드 순서 = 보유 종목 → 목표 달성 → 지금 받는 배당**(사용자 지시). DOM 순서를 `test/portfolio/portfolioCardOrder.test.tsx`가 잠근다. hero 타일(`emphasis="hero"`)은 여전히 요약 카드 하나만 갖는다(화면당 1개).
- [2026-07-29][design] **데스크톱 공유 = 앱이 그린 공유 창**(사용자 신고 "desktop 공유 팝업이 잘려 나온다"의 해결). `navigator.share`는 **터치가 주 입력인 기기에서만** 쓴다 — 데스크톱에도 이 API가 있어 호출하면 OS 창이 뜨는데 앱이 손댈 수 없다. 창의 1급 동작은 **링크 복사**, 그 아래 채널(X·페이스북·네이버, SDK 없이 https 인텐트만). 카카오톡은 SDK·앱키가 필요해 **일부러 뺐다**(링크 복사가 그 자리를 대신한다). 앵커 팝오버는 이 앱의 컨테인먼트 지형에서 다시 잘리므로 **중앙 창으로 확정** — 되돌리지 마라. 시뮬레이터 "공유"는 한 번 클릭 = 복사 + 토스트를 유지하고 **클립보드가 막혔을 때만** 이 창을 연다(잘 되는 흐름에 단계를 더하지 않는다). 근거 components/common/ShareDialog, components/community/hooks/usePostShare.ts.
- [2026-07-29][design] **결과 이미지 저장 버튼은 "간략히" 토글 왼쪽, 같은 줄**(사용자 지시). 캡처 대상은 활성 탭의 결과 카드 전부이고 **시나리오 탭 바는 제외**한다 — 구현상 캡처 루트(`ResultGrid`)의 형제라 자연히 빠지므로 둘의 부모/자식 관계를 바꾸지 마라. 결과 카드 안의 "조건 수정" 버튼은 `data-capture-exclude`로 그림에서 뺀다(누를 수 없는 버튼은 미끼다). 파일명 `스노우볼결과_{시나리오명}_{YYYYMMDD}.png`. ⚠ 2026-07-18에 폐기했던 구 Capture(`capture/*`·`capturePage.ts`)의 부활이 아니다 — 그 코드는 그대로 죽었고 새 구현은 결과 그리드 한 곳만 찍는다.
- [2026-07-31][design] 🔴 **앱 헤더는 ≥1024 에서 한 줄(실측 65px), ≤1023 에서 두 줄(105~111px)이다.** 경계 토큰 = `BREAKPOINT.headerStack`(1023, `media.up('headerStack')` = 1024↑). 마크업은 한 벌이고 배치만 `headerControlsGrid` 의 grid-area 두 벌(`brand nav actions` ↔ `brand actions` / `nav nav`)이 바꾼다 — 모드별 컴포넌트/복제 금지. 넘치는 메뉴는 항상 `NavScroller` 가 받는다(문서는 절대 넓어지지 않는다). 높이의 **단일 조절점은 `HeaderInner` 블록 패딩**이고 상한 80px·목표 64~72px 는 `tools/dev/headerprobe.mjs` 가 5라우트×5폭으로 잡는다. ⚠ 한 줄에서는 DOM 순서 = 화면 순서지만 두 줄에서는 탭이 아랫줄 메뉴를 먼저 지난다(두 모드를 동시에 만족하는 DOM 순서는 없다 — 데스크톱을 맞춘 결정).
- [2026-07-31][design] **활성 내비 알약은 페이지 hue 를 '링'으로만 읽는다** — 채움은 계속 `color.brand`+`onBrand` 라벨이고 `box-shadow: 0 0 0 2px pageHueMix(55)` 만 라우트 색을 따라간다. hue 로 **채우면 안 되는** 이유는 `/` 의 identity 채움 위 흰 라벨이 다크에서 2.79:1 이라 semantic.ts 가 명시 금지하기 때문이다(라우트마다 검증된 on-색이 다를 수 없다). 텍스트가 없는 링이라 `color-mix` 파생도 안전하다.
- [2026-07-31][design] 🔴 **헤더에는 페이지 위젯을 얹지 않는다 — 갤러리 검색·글쓰기는 본문 첫 줄로 내려갔고, `center`/`below` 슬롯은 삭제됐다**(사용자 지시: "포트폴리오 갤러리의 검색 필터를 헤더 말고 아래로 내려줘. 같은 라인에 있으니까 이상해"). 구 절충안("메뉴가 스크롤 뒤로 밀리는 건 의도"·가운데 슬롯 상한 360px)은 **폐기**다. 실측 before→after @갤러리: 메뉴 6개 중 가려진 항목 1280 **2개→0개** · 1024 **3개→0개**, 검색 입력 1280 183→983px · 1024 **72→792px**, 헤더 높이 71→65px(다른 라우트와 동일). 자리 규칙 = 검색 줄(독립) → 정렬 탭 ↔ [뷰 토글][글쓰기]. **한 줄에 검색+정렬+뷰토글 3덩어리를 넣지 마라** — 헤더에서 진 싸움을 본문에서 반복하는 것이다. "← 목록"이 2026-07-28 에 같은 이유로 내려간 것과 같은 처방.
- [2026-07-31][design] **갤러리 검색 바는 ≤1023 에서 `sticky`(`top: var(--sb-app-header-h)`)다 — `fixed` 가 아니다.** 사용자는 "fixed"라고 했지만 fixed 는 흐름에서 빠져 본문을 덮고 그만큼 상단 패딩을 손으로 벌어야 하며, 그 값이 헤더 실측 높이와 어긋나는 순간 겹치거나 빈 띠가 생긴다. sticky 는 자리를 지켜 레이아웃 시프트 0이고 시각 결과가 같다(실측 @390 스크롤 후 바 top=111 = 헤더 bottom, 겹침 0, 문서 가로 오버플로 0). ⚠ 컨테이닝 블록은 갤러리 `<section>` — 부모를 짧은 줄이나 grid item 으로 바꾸면 조용히 무력해진다.
- [2026-07-31][design] **테마 프리셋 스위처는 헤더 오른쪽 상시 노출(아이콘+현재 색 점 트리거 → 팝오버)이고 `⋯` 서랍에서는 제거됐다**(F4). 진입점 둘 금지. **"색 동그라미 8개 한 줄"은 실측으로 기각** — 1024 에서 내비 스크롤러의 여유는 트리거를 넣고도 **44px**뿐이라(내용 667px / 트랙 711px) 8칸 인라인(≈170px)은 메뉴를 다시 스크롤 뒤로 민다. 접근성: 각 옵션은 프리셋 이름 접근명 + `aria-checked` + ✓(색 외 단서), 트리거 접근명은 `테마 프리셋 선택 (현재: X)`. 계측 `theme_preset_changed`·팔레트 계약(`html[data-palette]`·localStorage·`palettePresetAtom`) 무변경 — 구현은 기존 `ThemePresetSwitcher` 의 잠자던 `popover` 변형 재사용(복제 0).

## 시뮬레이터 대개편 (2026-07-28 — 브랜치 feat/simulator-overhaul, 실행순서 3단계)
- [2026-07-28][design] **설정 패널 = 전 해상도 오버레이 드로어**(사용자 확정, 재설계 금지). 데스크톱 좌측 고정 컬럼 폐지, 2단 분기 소멸, 결과 영역은 항상 전 폭. 밀어내기(push) 사이드시트·핀 고정은 **기각**됐다. 첫 방문 기본값 = **닫힘**(`isConfigDrawerOpenAtom` false, 비영속).
- [2026-07-28][design] 🔴 **경계 2개를 서로 다른 토큰에 배정했다 — 하나로 합치지 마라.** ①**딤 + body 스크롤락 = `drawer`(960)**: `≤960` 딤(`overlay`+blur 2px)+락 ON / `≥961` **투명 스크림**(시각 딤 없음)+락 OFF ②**ResultGrid 12열→1열 = `layout`(980)**. 근거: `drawer`는 이미 드로어 의미론의 단일 경계로 CSS·JS 10곳이 쓰고, `layout`은 2단 그리드 소멸로 의미를 잃어 재배정했다. **961–980은 "결과 1열 + 딤 없는 드로어"** 밴드 — 의도된 조합이다. 데스크톱에서 딤·락을 끄는 목적은 **"설정을 바꾸며 결과가 변하는 걸 동시에 보는 조정↔확인 루프"** 보존이다.
- [2026-07-28][design] **`SimulationResult` 한 덩어리를 4개로 분해**: `components/ResultSummaryCard`(hero+타일+조건 스트립) / `components/ConditionStrip`(조건 요약 — 순수 함수 `buildConditionStripItems`가 표시 규칙 소유) / `pages/Main/components/MainRightPanel/components/FinancialIncomeNotice`(종합과세 배너 — 결론 카드 **밖** 전폭) / `components/SaleTaxCard`("전량 매도한다면" — `Card tone='sunken'`, 다른 가정의 세계라 맨 끝). "결과 간략히" 토글은 요약 카드 헤더 → **`ScenarioTabsRow` 우측**으로 이동(계측 `field_name:'isResultCompact'` 불변). **hero 값 1개 = 최종 자산 가치**는 유지.
- [2026-07-28][design] **조건 스트립의 표시 규칙**: 값이 0/미설정이어도 **"—" 단독 금지**. `taxRate`는 **미입력(undefined)일 때만** 사유 문구(`세율 미입력 (0% 적용)`)를 쓰고 **사용자가 명시적으로 0을 넣으면 `세율 0%`** 로 쓴다 — 엔진 결과는 같지만 "앱이 대신 골랐다"와 "내가 비과세를 가정했다"는 다르게 읽혀야 한다.
- [2026-07-28][arch] 🔴 **드로어 패널은 항상 마운트**(닫힘 = `visibility:hidden`). 조건부 마운트(`{isOpen && …}`)로 바꾸지 마라 — ①`usePortfolioPersistence`의 언마운트 flush가 열닫기마다 IndexedDB write를 유발하고 ②`onHydratedChange`/`onRegisterRetryCloudSave`/`onRegisterResumeConflict` ref 등록 3종이 무음 no-op이 되어 **우패널이 영구 로더로 굳는다**. `test/main/settingsDrawerAlwaysMounted.test.tsx`가 이 계약을 잠근다.
- [2026-07-28][arch] **공용 부품은 4개만 만들었다**(`components/common/{SideDrawer,ResultGrid,PageHero}` + `Card tone='sunken'`). `AppShell`·`CardGrid`·`EmptyState`·`Skeleton`·`LiveRegion`·`FootNotes`·`PageFooter`·`FeedToolbar`는 **10단계**로 명시 기각 — 소비처가 없거나 전 페이지 동시 변경이 필요하다. `PageHero`엔 `stat` 슬롯을 **일부러 안 만들었다**(시뮬레이터는 히어로 숫자를 요약 카드가 갖는다). ⚠ ~~아직 `PageHero`는 **3벌째**다~~ → **2026-07-31 에 공용 1벌로 수렴 완료**(아래 "페이지 정체성 hue" 항목).
- [2026-07-31][design] 🔴 **드로어 상단 결과 스트립 신설 — "딤을 껐다"가 "가리지 않는다"를 뜻하지 않았다.** 실측: 드로어 폭이 400px 고정인데 본문이 중앙 정렬이라 결과 hero 숫자(`₩246,021,879`)의 **잉크가 1280·1024·961 전 폭에서 100% 드로어 뒤**였다(도넛도 59/79/85% 가림). 즉 데스크톱 딤을 끈 목적(조정↔확인 동시 관찰)과 정반대였다. 처방은 **(b)안 = 최종 자산·월배당·목표 도달 3값을 드로어 최상단에 얇게 복제**. **(a)안(콘텐츠 우측 밀기)은 기각** — 레이아웃 시프트·스크롤 사고가 따라오고 961–1280 대역엔 밀 공간이 없다. 규율: 스트립은 **카드가 아니라 면**(`surfaceSunken`, 주역 카드는 화면당 1개) · **숫자에 색 금지** · 값은 결과 카드와 **같은 빌더(`buildSimulation`)·같은 포맷터(`createResultAmountFormatter`+`isResultCompact`)** 에서 나온다(가드가 두 문자열의 동일성을 단정한다). **폭 분기를 두지 않는다** — ≤960 은 딤으로 결과가 100% 가려져 필요가 더 크고, 분기를 두면 `SideDrawer` 가 소유한 `drawer` 경계가 두 벌이 된다. ⚠ sticky 는 불가(`SideDrawerBody` 가 grid → grid item sticky 는 자기 행에 갇힌다). 근거 pages/Main/components/MainLeftPanel/components/DrawerResultStrip, test/main/settingsDrawerSectionOrder.test.tsx.
- [2026-07-31][design] **드로어 내부 순서 = 사용 빈도 순: ①종목 ②투자 조건 ③계산 방식 ④도구.** 종전은 정확히 반대였다(공유 전폭 고스트 → 티커 생성 전폭 그라디언트 CTA → 칩 → 환율 → 토글 4개 → 그제서야 월적립·기간·세율). 시각적으로 가장 강한 두 요소가 가장 드물게 쓰는 동작이었다. 조치: 공유+환율을 마지막 "도구" 섹션(`MainLeftPanel/components/SettingsToolsSection`)으로 이동하고 공유 버튼은 **전폭을 버렸다**(전폭 고스트는 빈 입력칸으로 오독됐다) · `TickerCreateButton` 은 `gradient-cta` 채움 → 담백한 외곽선으로 강등하고 칩 **아래**로 내렸다 · `InvestmentSettings` 카드 안도 입력 그리드 → 구분선 → 토글 순으로 뒤집었다. 부수 정리: 쓰이지 않게 된 `TickerQuickAction*` styled 3종 삭제. 투어 단계 순서(`ticker-create → investment-settings → quick-actions`)가 원래 이 순서였다 — DOM 이 투어를 따라온 셈. 가드 `test/main/settingsDrawerSectionOrder.test.tsx`.
- [2026-07-31][design] 🔄 **티커 저장·삭제가 설정 드로어를 닫던 동작을 제거**(`useTickerActions.ts` 의 `setIsConfigDrawerOpen(false)` 2곳). "드로어 = 화면을 덮는 모달"이던 시절의 잔재라 전 해상도 드로어에서는 "종목 3개 추가 = 드로어 3번 열기"였다. 닫아서 얻으려던 것(만든 결과를 바로 보여주기)은 위 결과 스트립이 대신 받는다. 구 기록 테스트(`tickerSaveClosesDrawer`)는 **단정을 뒤집어** `test/main/tickerSaveKeepsDrawerOpen.test.tsx` 로 개명했다.
- [2026-07-31][design] **`배당 없음` 주기는 계속 자유롭게 고를 수 있다 — 모순은 막지 않고 알린다.** 배당률>0 + 주기 `none` 조합에 `FieldHint` 한 줄("배당률이 입력돼 있지만 지급 주기가 '배당 없음'이라 배당이 계산되지 않습니다.")을 붙였고 `aria-describedby` 로 셀렉트가 함께 읽는다. 막지 않는 이유: 무배당 종목을 정직하게 담는 것이 그 옵션의 존재 이유이고, 배당률 칸에 옛 값이 남아 있는 것도 정상적인 편집 중간 상태다. 고지는 **편집 가능한 `TickerDraftForm` 에만** 붙인다(`PresetTickerPreview` 는 읽기 전용·큐레이션 데이터). 근거 pages/Main/components/TickerModal/TickerModal.utils.ts `buildFrequencyMismatchHint`, test/main/frequencyMismatchHint.test.tsx.
- [2026-07-28][state] **자동저장 120ms 유실 수정** — `useEffect` cleanup은 deps 변경과 언마운트를 구분할 수 없다. 정답은 **"대기 중인 저장을 ref에 담고, 마운트 1회 effect의 cleanup에서 flush"**(`usePortfolioPersistence.ts`). 클라우드도 같은 본문으로 처리하되 **반드시 `flushCloudSave()`와 짝으로** 부른다 — `useCloudSync`의 `dispose()`가 훅 호출 순서상 **먼저** 돌아, 예약만 하면 취소 불가 고아 타이머가 된다.

## 테마 프리셋 시스템
- [2026-07-17][design] 프리셋 8종, 기본 = `velog`(표시 라벨 "미니멀 그린" — 타사명 노출 회피, 사용자 고지됨). 순서: velog/forest/aurora/vivid/navy-gold/grape/sunset/ink.
- [2026-07-17][design] velog 프리셋 라이트 bg는 **의도적 무틴트**(#f8f9fa, open-color 충실성이 정체성). 나머지 프리셋은 색상군 틴트 bg.
- [2026-07-17][arch] 팔레트 계약: `html[data-palette]` 어트리뷰트 + localStorage `snowball:palette` + `palettePresetAtom`(쓰기 시 DOM **동기** 반영 — 차트 stale 방지). 스펙 문서와 어긋나면 **먼저 랜딩한 구현이 정본**.
- [2026-07-17][design] knife-edge 대비 하한 (더 조정 금지, presets.ts 주석 참조): navy-gold 라이트 글로우 4.52 / grape 다크 brand 3.19 / sunset 시리즈0 3.19 / ink 라이트 data-positive 4.50 / **ink 라이트 `accent`↔`accentAlt` ΔE 16.37**(하한 15, 여유 1.37 — 2026-07-28 무채 복원. 둘 중 어느 쪽이든 미세조정하면 즉시 `contrast.test.ts`가 빨개진다).

## 커뮤니티
- [2026-07-25][product] **"내가 쓴 글"은 프로필 설정 안의 섹션이 아니라 독립 라우트 `/community/my-posts`**(사용자 요청 verbatim "내 글을 프로필 설정에 넣지 말고, 프로필 설정 밑에 '내가 쓴 글' 메뉴를 넣어줘"). 진입점 = AuthControl 드롭다운의 **"프로필 설정" 바로 다음** MenuItem(ListIcon). 페이지는 프로필과 **같은 딥링크 게이트**(authReady 로딩 → 비로그인 SocialLoginButton 구글·네이버·카카오)를 자체 보유하고, 목록 데이터는 MyPostsSection이 스스로 쥔다(페이지 viewModel = 게이트 3필드뿐). 카피는 `COMMUNITY_COPY.profile.myPosts` → **최상위 `COMMUNITY_COPY.myPosts`** 승격(페이지 인증 로딩 `loading`과 충돌해 목록 로딩만 `listLoading`으로 개명). 공개 전환 버튼은 여전히 없음(상세/수정의 공개 토글로 안내) — 프로필 설정에 되돌려 붙이지 말 것. 근거 pages/Community/CommunityMyPostsPage, router/routes.tsx:127, components/community/AuthControl/AuthControl.tsx:81.
- [2026-07-17][product] 댓글은 1단계 평면 스레드 — 대댓글에도 답글 버튼이 있지만 **답글의 답글도 루트 스레드에 달린다**(velog·유튜브식). 컴포저는 스레드 맨 아래.
- [2026-07-17][product] 댓글 스팸 레이트리밋: 1분 10개 (DB 트리거, 20260714000000_community.sql). 의도된 동작 — UI는 에러를 반드시 표시해야 한다.
- [2026-07-17][design] 갤러리 카드/행 푸터는 **닉네임만**(아바타 썸네일·"by" 접두어 제거 — 사용자 verbatim "아바타 썸네일+'by' 제거 → 닉네임만"). 카드 포맷 = velog 글 카드(제목→요약→서브정보→구분선→닉네임|♥), Row 메타도 "by" 제거. ⚠ 이 항목이 아래 profile-v2의 "갤러리 카드도 이니셜 아바타로 통일"을 **덮어쓴다**(카드/행은 아바타 자체를 렌더 안 함) — 다른 아바타 소비처(댓글·상세·AuthControl)는 이니셜 아바타 유지. 근거 components/community/ScenarioCard/ScenarioCard.tsx:59, ScenarioRow/ScenarioRow.tsx:36.
- [2026-07-17][design] 갤러리 카드 정보 = velog 글 카드 포맷. **카드 정보 = 3층 위계**(구 "hero1+보조2+배지1 초과 금지"를 사용자 요청으로 완화): ① hero 1(월 배당 세후, 강조) ② 보조 2(최종자산·투입대비) ③ 배지(목표 달성) ④ **조건 컨텍스트 1줄**(초기·월적립·기간·티커/목표 — 전부 textMuted·무강조·최대 2줄 clamp). 원칙은 유지: **정보량은 "개수"가 아니라 "위계"로 규율** — 추가 정보는 가장 약한 컨텍스트 줄에만, 제목·hero를 죽이지 않는다. card variant만 확장(attach/inline 절제 유지). 분기: 달성=티커 표시/목표는 배지, 미달성=목표 월배당 표시/티커 생략, targetMonthlyDividend=0이면 목표 생략.
- [2026-07-21][frontend] 포폴 글 OG 카드(`/api/og?post=<id>`)는 sim_summary만 읽어 그리므로 헤드라인이 **"N개 종목"**(티커명·비중 없음)이고 목표 문구도 **"N년차 달성"**(달력연도 아님)이다 — `?s=`/`?share=` 카드(티커 비중 줄 + "YYYY년 도달")와 **의도적으로 다르다**. 원인=sim_summary가 holdings를 저장 안 하고 tickerCount만, 투자 시작일도 없어 달력연도 복원 불가(재계산 금지 결정). 리뷰/디자인에서 이 차이를 "티커 줄 깨짐/회귀"로 오인하지 말 것. 매핑 단일 지점 = `buildOgCardModelFromSimSummary`(pages/Main/utils/ogCard.ts), n년차 분기는 `OgCardModel.targetReachedInYears`(post 전용 옵셔널, `?s=`는 undefined라 출력 불변). 게이트: og는 `kind=eq.portfolio`+`is_public`, post-html은 presence(비-null object)만 보고 og:image 전환(파싱은 og가 — post-html 번들 엔진 미유입). 캐시는 immutable 금지(수정 5분 반영, CACHE_POST_SCENARIO). 근거 server/handlers/Og/Og.tsx resolveCardModel, PostHtml.ts applyPostMeta.
- [2026-07-17][arch] `sim_summary`는 **게시 시점 고정**(재계산 금지) — 카드·상세·OG·첨부 미리보기 표기 일치가 목적. 프리뷰 렌더 조건은 sim_summary 존재(has_payload 아님). 요약 숫자 표기는 `formatSummaryKRW`(shared/utils, "187만원"/"9.2억") **단일 포맷터** — `formatApproxKRW`("약 187만")를 커뮤니티 표면에 직접 쓰면 화면 간 표기가 어긋난다. 글쓰기 첨부 미리보기만 예외적으로 클라이언트에서 `buildScenarioSimSummary`를 실행(표시 전용, 저장 아님 — useScenarioCandidates의 candidate.summary).
- [2026-07-17][design] 콘텐츠 카드 radius.xs vs 도구 카드 radius.lg — 의도적 구분.
- [2026-07-17][product] 글쓰기 라우트는 `/community/write` (구 /new에서 변경, 미배포라 리다이렉트 없음).
- [2026-07-18][product] 글쓰기 시뮬 첨부 = **"첨부" 토글 + 1단계 피커**(구 2단계 커밋 폐기 — 사용자 버그: 라디오만 고르고 버튼을 안 눌러 payload=null 저장되던 함정). 섹션 제목 "시뮬레이션"(구 "시뮬레이션 첨부") **우측에 common `ToggleField`("첨부")** — FormSection은 title 우측 슬롯 미지원이라 이 섹션만 헤더를 로컬 조립(AttachSection/AttachSectionHeader/AttachSectionTitle, 타이포는 SectionTitle과 동일). **토글 OFF(기본)=미첨부·피커 숨김**, **ON=피커 노출**, 시나리오 카드를 **고르는 즉시 composer.attachScenario 커밋**(별도 버튼 없음), **토글 OFF=composer.detachScenario**. ⚠ 앞서 랜딩한 "첨부 안 함 라디오 맨 앞"은 **이 토글이 대체**(라디오 제거, `attachNoneLabel`/`attachNoneHint` 카피 삭제) — 되살리지 말 것. optional 유지: 토글 OFF거나 카드 미선택이면 본문만으로 게시 가능(canSubmit=title&&(body||attachedPayload) 불변). 무효 payload는 여전히 비활성 카드+사유, 선택가능 0개면 빈 상태("시뮬레이터로 가기"). **수정 모드 외부 첨부**(서버 payload가 후보와 매칭 안 됨)는 `useEffect`로 **토글 자동 ON** + 요약 카드만 노출(해제=헤더 토글, 카드 내 버튼 없음). 삭제 카피: `attachButton`/`attachOptionActiveTag`/`attachRepick`/`attachAttachedStatus`/`attachNoneLabel`/`attachNoneHint`, styled `OptionActiveTag`/`PickerActions`. 추가 카피 `attachToggleLabel='첨부'`. sim_summary 계산·저장·publish·검증·이탈방지 **불변**(첨부 UX만). 토글 상태(attachEnabled)는 **뷰 로컬**(composer/스키마 무변경). 근거 pages/Community/CommunityWritePage/CommunityWritePage.view.tsx(handleToggleAttach·attachEnabled effect·ScenarioPicker), useScenarioCandidates.ts(isActive/defaultSelectedId는 훅에 남지만 뷰 미사용).
- [2026-07-18][design] 글쓰기 페이지 = **surface 면색 패널로 card화**(사용자 피드백 "배경색과 너무 똑같아 밋밋"·"절제된 단색=허접"). WriteForm에 surface bg + `border` + `shadow.e1` + `radius.lg`(도구 카드 계열) + clamp 패딩 — bg 위에서 뜨게(토큰만, 전 8프리셋·라이트/다크 bg≠surface 유지). 옵션 카드 배경 surface→**surfaceSunken**(패널 위에서 자체 카드로 뜨게, velog PreviewBlock 관례). "지금 열려 있음" 태그 제거(사용자 요청). PickerGrid는 **scrollbar-gutter/overflow/max-height 제거**해 폼 폭에 꽉 맞춤(구 `scrollbar-gutter:stable`이 우측에 스크롤바 폭을 상시 예약해 다른 필드와 우측 정렬이 어긋나던 사용자 피드백 해소). 게시 설정의 **공개/비공개 안내 문구는 토글 아래가 아니라 옆**(VisibilityRow=flex row, ToggleField "비공개" + VisibilityText 나란히, 상태 따라 문구 전환). 근거 pages/Community/CommunityWritePage/CommunityWritePage.styled.ts.
- [2026-07-17][product] 프로필 설정 `/community/profile` **v2 — 프로필 사진 기능 폐기**(사용자 verbatim "프로필 사진은 사용하지 않는게 좋을거 같아 썸네일 UI는 다 삭제해줘"). 구조 = **닉네임 카드 + 그 아래 회원 탈퇴 아코디언**(기본 접힘, danger 톤, 헤더 button+aria-expanded/aria-controls, 클릭 시 탈퇴 버튼 노출→DeleteAccountDialog 무변경). 아바타 썸네일·업로드·변경/제거 UI·관련 카피 전부 삭제. **작성자 아바타 표시는 전 소비처(댓글·갤러리 카드·상세·AuthControl) 이니셜 아바타로 통일** — 권고안=`Avatar` 컴포넌트가 avatar_url 무시하고 항상 이니셜(단일 지점, 소비처 무변경). OAuth **이메일은 여전히 미노출**(훅이 email 미반환). 진입점=AuthControl 드롭다운. avatar_url 컬럼·Storage `avatars` 버킷·카카오 프로필 이미지 scope 실삭제는 후속(표시만 끄면 앱은 안 죽음). ⚠ **구 결정(좌 아바타+닉네임 통합/우 탈퇴 2단, layout 980 스택)은 이 항목으로 폐기** — 아바타 복원 금지. 근거 pages/Community/CommunityProfilePage, components/community/Avatar.
- [2026-07-17][product] 회원 탈퇴는 **200 확정 시에만** 로그아웃·이동(성공 위장 금지, 스코핑 F3). 실패 시 다이얼로그 유지+에러. 순수 `runAccountDelete`(pages/Community/CommunityProfilePage/hooks/accountDeletion.ts)에 계약을 못 박아 UI가 우회 못 하게 함. 로컬 dev는 /api 부재라 항상 `deleteFailed`가 정상 경험.
- [2026-07-17][design] 갤러리 "정밀 검색"(월배당·목표·기간·티커 필터)은 검색바 우측 아이콘(lucide `SlidersHorizontal`) 트리거+활성필터 카운트 배지로 노출. 상호작용 모델 = **텍스트 검색은 즉시(디바운스) 유지, 정밀필터는 명시적 "적용" 2단계 커밋**(min/max 상호의존 검증 + 매 키입력 리페치 방지). "초기화"는 필터 파라미터만 지우고 정렬·텍스트검색 보존. 반응형은 @media 대신 **variant prop으로 구조 분기**: 데스크톱=앵커 팝오버(비모달, ThemePresetSwitcher와 동일 — Tab-out/Esc/바깥클릭 닫기, 포커스 트랩 없음), 태블릿/모바일(drawer↓)=in-flow 인라인 패널(오버레이·포털·트랩 없음 — MySavePanel 포털 함정 회피). URL 파라미터는 canonical **원 단위**(mdmin/mdmax/tgtmin/durmin/durmax/ticker), UI는 만원/년 표기. **숫자 facet은 sim_summary jsonb의 텍스트 필터로 못 건다**(PostgREST `->>`는 사전식 비교) → generated column+인덱스 필요(state-engineer/마이그레이션 의존, UI는 언블록). 티커 필터는 컬럼 준비 전까지 플래그로 섹션 자체를 숨김.
- [2026-07-18][design] 정밀 검색 **구현 랜딩**(위 결정의 코드화): 컴포넌트 폴더명 = **PrecisionSearch**(components/community/PrecisionSearch) — 스펙 초안(precision-search-spec.md)의 `CommunityFilterMenu`가 아니라 브리핑/오케스트레이터 명명 채택(리네임 금지). **자기완결형**: `useSearchParams`로 URL을 직접 read/write(CommunitySearchBar 패턴), useGallery에 필터 콜백을 프롭 드릴하지 않는다(useGallery는 facets 조회+filteredEmpty status+clearFilters[CTA용]만). **필터 타입 2개는 의도**: UI/URL 모델 `GalleryFilters`(mdMin/mdMax/tgtMin/durMin/durMax, 원·년 — shared/constants/community/galleryFilters.ts)와 데이터레이어 `GalleryFacetFilters`(monthlyMin/…)는 **별도**이고 `toFacetFilters`가 매핑(키만 다름, 값·단위 동일) — 하나로 합치지 말 것. 티커 필터는 `TICKER_FILTER_ENABLED=false`(config.ts)로 섹션 자체 미렌더(G2). 검증은 zod 대신 순수 min>max 비교(과도한 추상화 회피, 스펙 §11 zod 대비 기능동일).
- [2026-07-18][design] 갤러리 리스트 **행 = B안**(구분선 피드 유지, 카드화 기각): hover 시 좌측 `accentBorder` 레일(평상시 transparent로 자리확보→점프 방지) + `surfaceHover` 배경 동시 변화, 우측 `surfaceSunken` 숫자 칩(카드 PreviewBlock 축약). 숫자 칩 = **row 숫자 클러스터**(hero 월배당 xl + 목표 배지 + 최종자산·투입대비 체인) — 카드와 **동일 카피**("월 배당(세후)"/"최종 자산"/"n년차 목표 달성"). `SimSummaryStats`는 **'inline' variant 제거, 'row' 신설**. reduced-motion: 레일/배경 전이는 duration만(transition-delay 금지). 근거 components/community/ScenarioRow, SimSummaryStats.
- [2026-07-18][product] 글쓰기 **수동 "요약(description)" 입력칸 폐지 → 카드 미리보기는 항상 본문에서 자동 발췌**(사용자 verbatim "요약 선택은 필요 없어, 내용을 미리보기로"). composer.submit의 `finalDescription = safeBody ? deriveExcerpt(safeBody, COMMUNITY_DESCRIPTION_EXCERPT_LENGTH) : null`(수동 description 분기 삭제). description/setDescription state·타입·반환 전부 제거, 게시설정 FormSection엔 공개범위만 남음. 본문 없이 첨부만 있는 글은 description=null → 카드가 텍스트 발췌 대신 sim_summary를 노출(정상, 위 sim_summary 결정과 정합). **DB `description` 컬럼·write/update 저장 경로·카드/행 렌더는 불변**(값 출처만 본문 발췌로 단일화). 삭제 카피: `write.fieldDescription`/`descriptionPlaceholder`, 스타일 `DescriptionTextarea`. 근거 pages/Community/CommunityWritePage/hooks/useScenarioComposer.ts, CommunityWritePage.view.tsx.
- [2026-07-18][design] ~~커뮤니티 헤더 로고 = 원형 `/app_icon.png` `<img alt="">`~~ → **2026-07-28 아이덴티티 패스로 폐기**: 헤더 브랜드 블록에서 **아이콘이 통째로 제거**됐다(워드마크 텍스트 단독). 살아남은 부분은 "**워드마크가 유일한 브랜드 접근명**"이라는 계약뿐 — 이후에도 `aria-hidden`·이미지 치환을 넣지 마라. `/app_icon.png`는 이제 파비콘·PWA·`PdfCoverPage.tsx:32`만 소비한다(고아 자산 아님). ⚠ **같은 항목의 "`BrandMark` 컴포넌트 파일 삭제 금지 — api/og.tsx가 계속 소비"는 사실이 아니었다**(reviewer 실측 2026-07-28): `server/handlers/Og/Og.tsx:131`은 **동명의 로컬 컴포넌트를 따로 정의**하고 `components/BrandMark/`를 import하는 곳은 자기 테스트뿐이다. 삭제해도 무방하지만 이번 미션에선 스코프 밖이라 손대지 않았다.
- [2026-07-18][design] 상세(읽기) 페이지 **surface화**(사용자 피드백 "배경이랑 색이 똑같아 밋밋"): 본문(제목·메타·본문·첨부 CTA·좋아요)을 `PostCard`, 댓글을 `CommentsCard` 두 개의 surface 패널로 배경(bg) 위에 띄운다 — 갤러리 velog 카드와 같은 언어(`color.surface`+1px `color.border`+`shadow.e1`, 콘텐츠 카드 `radius.xs`, 하드코딩 색 0). 구조·카피 불변, 대비·계층(헤더 하단 hairline)·여백만 정돈. `Article`을 grid+gap으로 바꾸며 DetailHeader `margin-bottom`/AttachCta `margin`/LikeRow `margin-top`을 제거(gap이 대신). CommentsCard는 `& > section`(자기 요소 선택자, 컴포넌트 셀렉터 아님)으로 CommentSection 루트의 자체 top divider(margin/padding/border-top)를 상쇄 — 카드 경계가 그 분리를 대신한다. 근거 pages/Community/CommunityDetailPage/CommunityDetailPage.styled.ts(PostCard/CommentsCard), .view.tsx.
- [2026-07-18][frontend] 상세 페이지 **첨부 시나리오 미리보기(숫자+파이) 추가** — CTA와 **한 카드로 병합**: `AttachUnit`(단일 surfaceSunken+brandBorder+radius.lg+overflow) 안에 위=CTA 배너(brandSubtle, 자체 테두리·radius 제거)·아래=미리보기 아코디언(자체 테두리·radius·bg 제거, 헤더 `border-top` 한 줄로 seam) 순으로 담아 이중 테두리·간격 없이 한 덩어리로 읽히게(사용자 요청). 같은 조건(`scenario.payload && openInSimulatorHref`)에서 렌더. **아코디언(기본 접힘)**: 헤더 button(aria-expanded/aria-controls) + ChevronDownIcon(장식) → 펼침 region(role=region, aria-labelledby). **파이(ECharts)는 펼칠 때만 마운트**(pieOption useMemo `open` 가드 + 조건 렌더 — 첫 페인트 부담↓, lazy 청크와 정합). `hidden={!open}` 패널은 styled에 `display`를 두지 말 것(안 그러면 author display가 UA `[hidden]{display:none}`을 이겨 접혀도 보인다). **숫자**=`buildScenarioSimSummary`(순수, 표시전용)→`SimSummaryStats variant='card'`. **파이**=시뮬레이터 `buildAllocationPieOption`(pages/Main/utils) **그대로 재사용**(동일 비주얼: 외곽라벨·슬라이스경계·중앙 월배당) + 정규화도 `getIncludedProfiles`+`buildNormalizedAllocation` 재사용 — **레전드·슬라이더·티커태그 없음(파이 하나만)**. 캔버스 색은 `getChartTheme().series`(var() 불가), 팔레트 전환 리빌드=`usePalettePresetAtomValue` useMemo 의존성. 파이는 `role=img`+정규화 비중 aria-label. 배치=**페이지 로컬** `pages/Community/CommunityDetailPage/components/ScenarioPreview`(ResponsiveEChart는 `@/pages/Main/components/ResponsiveEChart`에서 재사용=내부 lazy echarts, 테스트는 test/community/ScenarioPreview.test.tsx에서 ResponsiveEChart mock). copy `detail.previewTitle/previewChartLabel` 추가. 근거 pages/Community/CommunityDetailPage/components/ScenarioPreview/, CommunityDetailPage.view.tsx.
- [2026-07-18][design] `SimSummaryStats` **목표달성 배지를 금액 밑으로** 이동(사용자 요청): card·row 헤드행(구 `CardHeadRow`/`RowClusterHead` 삭제)엔 hero 라벨만 남고, 순서=**라벨 → 금액(HeroValue/RowHero) → 배지**. 배지는 `BadgeRow`(flex)로 감싸 세로 스택 stretch로 pill이 폭 전체로 늘어나는 걸 막는다(왼쪽 정렬). attach variant는 배지 없어 무관. **갤러리 카드/행에도 동일 반영=의도**(일관성). 배지 카피/스타일 불변(위치만). 근거 components/community/SimSummaryStats/SimSummaryStats.tsx·.styled.ts(BadgeRow).
- [2026-07-18][design] 테마 스위처를 **로그인 시 AuthControl 프로필 드롭다운 안으로** 이동(사용자 요청 "테마 아이콘을 프로필 메뉴 밑으로"). 팝오버 중첩을 피하려 드롭다운에 "테마" 디스클로저 MenuItem(aria-haspopup/aria-expanded/aria-controls) → 클릭 시 radiogroup을 **인라인**으로 편다(선택해도 드롭다운 유지=비교/전환 편의). 이를 위해 `ThemePresetSwitcher`에 `variant='menu'` 신설(inline과 달리 드로어 미디어 숨김·보더 박스 없이 radiogroup만; 여백·스크롤은 AuthControl `ThemePanel`이 소유). copy `nav.theme='테마'` 추가. ⚠ **테마 접근성은 어떤 상태에서도 사라지면 안 됨** — 분기: 비로그인(드롭다운 없음)=헤더에 그대로, 커뮤니티 비활성(AuthControl 없음)=헤더에 그대로, **로그인+커뮤니티 활성일 때만** 헤더에서 숨기고 드롭다운으로. CommunityHeader는 `!isLoggedIn`일 때만 ThemeSlot 렌더, Main.view는 `themeInProfileDropdown=isCommunityEnabled&&isLoggedIn`일 때만 헤더 `<ThemePresetSwitcher/>` 숨김(CloudSyncIndicator/headerStatus·드로어 inline 변형은 불변). Main.view의 `useIsLoggedInAtomValue` 구독은 로그인/로그아웃에만 바뀌어 타건 memo를 안 깬다. 근거 components/community/AuthControl/AuthControl.tsx, components/ThemePresetSwitcher/ThemePresetSwitcher.tsx(menu variant), components/community/CommunityHeader/CommunityHeader.tsx, pages/Main/Main.view.tsx.
- [2026-07-19][design] 시뮬레이터 헤더의 **튜토리얼 보기 + 앱 설치(PWA) + 테마를 아이콘 전용 "더보기(⋯)" 메뉴**(`components/HeaderOverflowMenu`)로 통합(사용자 결정, 헤더 공간 절약). 로그인/커뮤니티 여부와 무관하게 **항상 노출** — AuthControl 프로필 드롭다운은 커뮤니티와 공유돼 부적합. 트리거=common `Button iconOnly variant=secondary size=sm` + lucide `MoreHorizontal` + aria-label "더보기". 드롭다운 메커니즘은 AuthControl 미러(바깥 pointerdown+Esc, role=menu/menuitem, aria-haspopup/expanded/controls)지만 **AuthControl.styled는 커뮤니티 전용이라 재사용 금지 → 자체 styled**. 항목: ① 튜토리얼 보기 ② 앱 설치 — **3분기**: `beforeinstallprompt` 잡히면 네이티브 `prompt()`+`CTA_CLICK{cta_name:'install_app',outcome}` 계측 / 없으면(iOS Safari·FF·미지원 데스크톱) **플랫폼별 수동 가이드 모달**(userAgent+maxTouchPoints로 iOS/Android/데스크톱 판정, 공용 Modal 프리미티브 재사용) / standalone이면 "설치됨" disabled ③ **테마 디스클로저** — `ThemePresetSwitcher variant="menu"`(radiogroup만; 팝오버/드로어 래퍼·미디어숨김 없음, 컴포넌트 로직 재사용)를 메뉴 안에서 인라인 펼침(선택해도 메뉴 유지=비교/전환). **시뮬레이터의 테마 접근점을 이 메뉴로 단일화** → Main.view headerAction의 standalone `<ThemePresetSwitcher/>` **제거**(항상 노출되는 ⋯ 메뉴가 "테마는 어떤 상태에서도 사라지면 안 됨" 제약을 대신 충족). 구 standalone `components/InstallAppButton`도 삭제(유일 소비처=Main.view). ⚠ 이 통합으로 구 [2026-07-18] "테마를 AuthControl 프로필 드롭다운으로" 결정은 **실제 코드에 미배선(고아)**임이 확인됨 — AuthControl.tsx는 테마 디스클로저를 렌더 안 하고 `variant="menu"`의 첫 실소비처가 이 HeaderOverflowMenu다(드로어 인라인 스위처·커뮤니티 헤더 테마는 불변). 근거 pages/Main/Main.view.tsx headerAction, components/HeaderOverflowMenu/HeaderOverflowMenu.tsx.
- [2026-07-20][product] **`profiles.is_admin`(boolean, NOT NULL DEFAULT false)은 "표시 힌트"이지 권한이 아니다** — 사용자 결정으로 이번 관리자 차단은 **UI 수준만**이고 **RLS 정책은 추가·수정하지 않았다**. update GRANT도 안 줘서 사용자가 스스로를 승격할 수 없고, 관리자 지정은 SQL 에디터 수동(`update public.profiles set is_admin=true where id=...`)이다. ⚠ 이 불리언으로 **서버가 막아야 할 동작을 게이팅하지 말 것**(클라이언트가 값을 위조해도 DB가 허용하는 범위는 그대로다) — 실권한이 필요해지면 정책/함수를 별도 마이그레이션으로 설계한다. 소비 API는 Provider가 아니라 **jotai 파생 atom**(`isCommunityAdminAtom` / `useIsCommunityAdmin()`, `@/jotai/community`)이라 Provider 없이 격리 렌더돼도 throw하지 않는다. 근거 supabase/migrations/20260725000000_add_profile_is_admin.sql.
- [2026-07-20][product] **자유게시판(kind='board')은 시뮬 첨부 없음 + 공개/비공개 선택은 게시판에서만 운영자 전용**(갤러리=예외, 모두 노출 — 사용자 verbatim "portfolio/write 는 비공개 노출을 해줘도 돼 이건 예외야"). 게이트 판정은 **컨테이너가 `kind !== 'board' || isAdmin`으로 접어 `canChooseVisibility` 하나로** 내려주고 뷰는 그 불리언만 본다(뷰가 kind와 isAdmin을 각자 해석하지 않는다 — viewModel에 `isAdmin`은 더 이상 없다). ①board는 첨부 섹션 전체를 미렌더하고, 게이트의 **단일 출처는 `usePostComposer.attachAllowed`(kind!=='board')** — 뷰가 이 값을 읽고, 훅도 `attachScenario`를 no-op으로 막고 `attachedPayload`를 항상 null로 노출한다(뷰가 실수해도 안 샘). ②board 제출은 **`payload` 키 자체를 전송 안 함**(스프레드 `attachFields`) — 신규는 서버 default NULL, **수정은 updatePost가 payload/sim_summary 키를 생략해 기존 첨부가 보존**된다(구 board 글의 첨부를 조용히 지우지 않는 쪽을 선택: 상세 페이지가 payload로 미리보기를 계속 렌더하므로 삭제는 사용자 데이터 파괴다). `queries.ts`는 **무변경**(publishPost의 `payload ?? null` 계약 유지 → 기존 테스트 회귀 0). ③공개범위는 `canChooseVisibility`가 true일 때만 **FormSection 통째로** 렌더(공개범위가 유일 필드라 빈 껍데기 방지). 숨겨진 경우(=게시판 비운영자) 신규=`isPublic` 기본 true 그대로, **수정=서버 `detail.is_public`가 그대로 유지**(setIsPublic을 부를 UI가 없을 뿐 값을 강제 true로 덮지 않는다 — 기존 비공개 글이 공개로 뒤집히면 안 됨). 갤러리(portfolio)의 저장 경로는 완전 무변경. ⚠ **첨부를 "없는 것"으로 취급하는 값과 "본문 대체로 인정하는" 값은 별개여야 한다**(`effectiveAttachedPayload` vs `bodyAlternativePayload`) — 과거에 만들어진 `board + body=null + payload≠null` 글이 전자만 보면 "내용 없음"으로 판정돼 **제목 오타조차 못 고치게 잠긴다**(첨부가 화면에 안 보여 원인 불명). 수정 모드에서만 서버 첨부를 본문 대체로 인정하되 저장 인자에는 여전히 안 싣는다(신규 board는 본문 필수 유지). 근거 pages/Community/CommunityWritePage/hooks/usePostComposer.ts, CommunityWritePage.view.tsx, test/community/boardWriteRestrictions.test.tsx.
- [2026-07-20][design] 글쓰기 **제목 입력(TitleInput)은 전역 포커스 링을 끄는 유일한 승인 예외**다(사용자 verbatim "제목이 active 되면 네모 border가 나오는데 border는 없애줘"). `shared/styles/README.md:239`의 "컴포넌트에서 `outline: none` 하지 마라"와 정면으로 충돌하므로 **되돌리기 전에 이 항목을 볼 것**. 전역 링은 `input:focus-visible`(globalStyles.ts:147-150, outline 2px + box-shadow 3px)이라 테두리 없는 전폭 언더라인 입력에서 사각형으로 그려진다. 대체 신호는 **언더라인 강조 + 면색 변화 2중**: `inset 0 -2px` box-shadow(border-width를 1→2px로 키우면 56px 입력이 1px 점프하므로 inset shadow) + `color.brand`, 배경 `color.surfaceSunken`. 색 단독이 아니라 두께+면색이라 저대비 프리셋·색각 이상에서도 남는다. invalid일 땐 강조색이 `color.danger`를 유지(brand가 에러 표시를 덮지 않게). 선택자는 `&:focus, &:focus-visible` 한 블록 — 특이도 (0,2,0) > 전역 (0,1,1). 근거 pages/Community/CommunityWritePage/CommunityWritePage.styled.ts TitleInput.
- [2026-07-25][product] **"내 글"은 프로필 페이지(`/community/profile`)의 섹션**이다(별도 라우트 아님) — 비공개 글을 볼 유일한 경로. 갤러리/게시판 목록은 partial index를 타려고 `is_public=true`를 **명시**하므로(queries.ts:171) RLS가 본인 비공개를 허용해도 목록엔 안 뜬다. 조회는 필터 없는 `fetchMyPosts`(queries.ts:217, 그전까지 0-caller였다). ⚠ **목록에서 공개로 전환하는 버튼은 의도적으로 두지 않는다**(되돌리기 어려운 동작 — 상세/수정 화면의 공개 토글로 보내고, 그 안내는 섹션 Hint가 상시 노출). 페이지네이션도 없다(내 글은 한 화면 규모 — 커지면 useBoard의 keyset을 그대로 얹는다). 항목은 PostCard/PostRow를 **재사용하지 않는다**: 그 둘은 자기 닉네임 반복 + `PostShareButton`(비공개 글은 남이 못 여는 URL을 공유하게 된다)을 달고 있어, 배지/공유 두 prop을 새로 뚫으면 피드 컴포넌트가 다른 표면을 겸하게 된다. 근거 pages/Community/CommunityProfilePage/components/MyPostsSection/.
- [2026-07-17][design] 소셜 로그인 버튼(구글/카카오/네이버) 색·로고는 **브랜드 규정색 하드코딩 — 테마 토큰 예외**다. 토큰화(`--sb-*`)하면 프리셋 따라 색이 바뀌어 규정 위반 + 카카오 OAuth 앱 심사 반려 사유. 구글=흰바탕+#747775 테두리+4색 G, 카카오=#FEE500+검정 85%, 네이버=#03C75A+흰 N. Toggle 썸(#ffffff)과 같은 "토큰화가 항상 개선이 아니다" 케이스 — 되돌리지 말 것. 근거 components/community/SocialLoginButton/SocialLoginButton.styled.ts.
- [2026-07-21][auth] **Supabase 인증 흐름은 `flowType: 'implicit'` 을 명시 고정한다**(client.ts). @supabase/supabase-js 브라우저 클라이언트의 **현 기본값이 이미 implicit** 이므로 이건 동작 변경이 아니라 **핀 고정**(향후 SDK 기본이 pkce 로 바뀌어도 흐름이 조용히 갈리지 않게). 근거: PKCE 는 로그인 시작 시 `code_verifier` 를 그 브라우징 컨텍스트 localStorage 에 저장하고 콜백에서 꺼내야 하는데, 카카오톡 등 **인앱 브라우저(WKWebView)** 는 Safari/Chrome 과 저장소가 분리돼 verifier 가 없어 교환이 실패한다(iOS 카카오 로그인 장애의 배경). implicit 은 토큰을 URL 해시로 직접 받아 verifier 의존이 없다. ⚠ **[[avoid-hash-routing]] "해시 라우팅 자제" 결정의 명시적 예외** — implicit 은 `#access_token=` 을 쓰지만 이는 OAuth 표준이고 라우팅용이 아니며 콜백 직후 즉시 제거한다(supabase `hash=''` + `finalizeOAuthCallback` 이 잔여까지 정리). ⚠ **한계(정직히)**: implicit 이어도 세션은 인앱 브라우저 저장소에 생겨 사용자의 Safari 엔 안 생길 수 있다 — flowType 으로 못 고치는 근본 한계라 LoginModal 이 **실패 후** 실패 배너로 "다른 브라우저로 열기"를 안내한다(선제 배너는 2026-07-21 철회 — 위 [community] 항목). 되돌려 pkce 로 가려면 사용자 승인 필요(인앱 로그인 회귀). 근거 shared/lib/supabase/client.ts flowType 주석.

- [2026-07-21][auth] 🔄 **카카오 로그인 실패의 진짜 원인은 DB CHECK 제약 위반이었다 — 가입 트리거가 avatar_url 을 항상 null 로 저장하도록 변경**(프로덕션 적용·검증 완료). 카카오가 주는 프로필 이미지 URL 이 `http://k.kakaocdn.net/...`(비-https)라 `profiles.avatar_url ~ '^https://'` CHECK 를 위반 → 가입 트리거 INSERT 실패 → 콜백 500 → 세션 미생성(구글은 https 라 통과 → "카카오만 안 됨"). 마이그레이션 `supabase/migrations/20260728000000_fix_kakao_profile_avatar.sql`이 `handle_new_user()`를 create-or-replace 해 avatar_url 을 null 로 넣는다 — 프로필 이미지는 v2 에서 폐기돼 화면에서 안 쓰이므로(이니셜 아바타 통일, 위 profile-v2 결정) null 이 정답이고, CHECK 제약은 방어선으로 유지, 기존 profiles 행은 불변(멱등). 트러블슈팅 문서 = docs/supabase/README.md §3-2-1.
- [2026-07-21][community] ~~카카오톡 인앱 로그인 선제 배너 + "외부 브라우저로 열기" 버튼~~ **철회**(2026-07-21): 인앱 저장소 분리를 카카오 실패의 원인으로 오진해 쌓았던 **선제 배너 전체**(모달 열림 즉시 노출)와 `buildKakaoOpenExternalUrl`·링크 복사·외부 열기 버튼을 제거했다 — 진짜 원인은 위 DB 500 이었고, DB 수정 후엔 되는 로그인을 "막힌다"고 겁주는 오안내였다. **유지**: 실제 실패 후에만 뜨는 반응형 실패 배너(`FailureNotice role="alert"`, `LOGIN_FAILURE_COPY.{title,generic,inAppBrowser}`) + `selectOAuthFailureGuidance` — 인앱 저장소 분리는 실재하는 현상이라 "실패 후" 안내는 남긴다. `detectInAppBrowser`도 유지(finalizeOAuthCallback 이 실패 기록에 UA 를 실음). 근거 components/community/LoginModal/LoginModal.tsx.

## 클라우드 저장
- [2026-07-18][product] **"데이터 저장" 패널(MySavePanel) 완전 제거 — 자동저장(latest-wins 클라우드 동기화)이 대체**(사용자 확정). `components/MySavePanel/` 폴더·툴바 "데이터 저장" 버튼(+CloudSyncIndicator 배지)·`onOpenMySave` 배선 삭제. 툴바 퀵액션은 이제 **[공유] 하나뿐**(Coffee는 display:none 유지) → `TickerQuickActionRow` 그리드 2열→**단일 열 전폭**. 연쇄 dead code 제거: usePortfolioPersistence의 이름슬롯/JSON 함수(list/load/delete/download/JSON import) + `pages/Main/hooks/persistence/savedName.ts`(파일 통삭제, 배럴에서 제거) + `appStateStorage.ts`의 이름슬롯 IO 4종(list/read/write/delete-ByName). **보존**: buildPayload·applyPersistedPayload·readLocalAutosaveForSync·createShareLink·retryCloudSave(Track B 렌더게이트·공유·동기화가 계속 씀). indexedDB.deleteDatabase·통삭제는 안 함(코드 경로만 제거). 되돌리려면 사용자 승인.
- [2026-07-18][design] **동기화 상태 표시는 헤더로 이전**(패널 제거로 CloudSyncIndicator가 노출될 곳이 사라진 것 보강 — 무음 실패 금지). `CloudSyncIndicator`에 `header` variant 신설: idle(비로그인/첫 렌더)은 **렌더 안 함**, 저장중/저장됨/오프라인은 아이콘만(라벨은 sr-only+title), **실패에서만 "저장 실패" 라벨+"다시 시도" 버튼을 편다**(로그인 사용자에게 저장 실패가 반드시 보이게). Main.view 헤더 액션 영역에 `isCommunityEnabled`일 때만 배치. 상태는 컴포넌트가 `cloudSyncStateAtom`을 직접 구독(컨테이너 불필요). 재시도 배선은 **훅 hoist 금지 규칙 준수**: retryCloudSave는 MainLeftPanel이 계속 소유하고 `onRegisterRetryCloudSave(fn|null)`로 함수 참조만 Main.view의 ref에 등록(setState 아닌 대입 → 헤더 크롬 memo 유지), 헤더는 안정적 래퍼로 그 ref를 호출. 근거 components/CloudSyncIndicator/CloudSyncIndicator.tsx, pages/Main/Main.view.tsx, pages/Main/components/MainLeftPanel/MainLeftPanel.tsx.
- [2026-07-17][product] **명시적 저장 없는 순수 자동저장 + 조용한 로드로 전환**(사용자 확정, 구 "충돌 프롬프트" 폐기). 세션 시작 시 클라우드 autosave를 pull해 현재와 다르면 **조용히 적용**(last-write-wins 수용 — `CloudConflictPrompt` 폴더·`CLOUD_CONFLICT_RESOLVED` 이벤트 삭제됨). IndexedDB→클라우드 마이그레이션은 **자동·안전**(`useCloudWorkspaceSync`에 배선): 클라우드 비었으면 로컬 autosave push→**read-back 확인→확인된 autosave 슬롯만** 정리(이름 슬롯·비로그인 데이터 불변), 클라우드에 있으면 정본 채택+로컬 보존(push 안 했으니 정리 대상 없음). 실패(push·read-back·로컬읽기·pull)는 전부 로컬 보존+플래그 미설정(다음 로그인 재시도, 무음 실패 금지). 근거 jotai/snowball/cloud/cloudWorkspaceSyncEngine.ts(순수 엔진).
- [2026-07-17][product] 클라우드 저장은 **의미있는 액션만** 유발(사용자 확정): 티커 추가/삭제·비중·초기금·월적립·기간·세율·재투자·시나리오 탭 추가/삭제/이름·목표. 배제=탭 전환·뷰 토글·티커 선택·테마. 디바운스 4000ms 유지. no-op 게이트가 본질(API 절감).
- [2026-07-17][product] **명시적 체크포인트 저장 폐기 완료 — 클라우드 = 단일 워크스페이스 자동 동기화**(MySavePanel 정리 트랙). `useCloudSync.saveCheckpoint`·`userAppStates.saveCloudCheckpoint`·MySavePanel 체크포인트 폼/수동 마이그레이션 배너 전부 삭제. 마이그레이션은 세션 시작 시 자동(useCloudWorkspaceSync). 클라우드 슬롯 목록/열기/삭제(list/fetch/delete)는 **MySavePanel 삭제로 제거됨**(2026-07-18, 사용자 승인 — 노출 UI가 사라져 프로덕션 0-caller가 됐다). `useCloudSavedStates`는 세션 동기화용 `pullAutosave`/`pushAutosave`만 남고, `userAppStates.ts`는 `fetchCloudAutosave`/`pushCloudAutosave`만 남는다(atom `cloudSavedStatesAtom`·`CloudSavedStateSummary`도 제거). buildPayload는 MySavePanel prop에서 제거(체크포인트가 유일 소비처였음). 근거 shared/lib/supabase/userAppStates.ts, jotai/snowball/cloud/useCloudSync.ts.
- [2026-07-18][arch] **세션시작 로컬 autosave read는 하이드레이션 ↔ 세션시작 sync가 1회 공유**(M1 유실 gap 보강). 예전엔 usePortfolioPersistence hydrate와 useCloudWorkspaceSync가 IndexedDB를 각각 독립 read → 두 read 불일치 시(하이드레이션 성공+sync read 실패, cross-tab blocked 등) 엔진이 더 오래된 cloud를 apply → 잠기지 않은 app autosave가 로컬 최신본을 덮는 유실 경로가 있었다. 해법=`createSessionLocalAutosaveCache`(jotai/snowball/cloud/sessionLocalAutosave.ts): 하이드레이션이 1회 read·캐시→`readLocalAutosaveForSync`로 노출→sync가 재사용(L2 이중 read도 자연 해소). 불변식="로컬을 못 읽으면 로컬을 덮어쓰지 않는다"가 app autosave 경로까지 성립. **되돌리려면(sync 독립 read 복원) 사용자 승인 필요** — 유실 경로 재개방. 근거 test/persistence/sessionLocalAutosave.test.ts.
- [2026-07-18][arch] **공유가 이중 포맷**(트랙 E, 사용자 확정): 신규 `?s=<key>`(DB 스냅샷 key) + 구 `?share=`(lz-string) **병존**. 포맷 감지=**파라미터 이름**(둘 다 있으면 `?s=` 우선). 생성은 DB 우선(`createSharedSnapshot`→`?s=`), **폴백**: `getSupabaseClient()` null(미설정/테스트/미배포) 또는 INSERT throw면 구 lz-string `?share=`로 폴백해 **공유는 항상 성립**(무음 실패 금지, 반환 `{ok,url,copied}` 불변). 복원 `?s=`는 **비동기**(fetch, cancelled 가드) + 실패=`OPERATION_ERROR`+쿼리정리. `?share=` 디코드 경로는 **절대 불변**(구 링크 영구 유효). 서버=`shared_snapshots`(RLS 정책 0개 + SECURITY DEFINER RPC `create_shared_snapshot`/`get_shared_snapshot`만, anon 실행 GRANT=구 익명공유 동등 UX, payload jsonb≤64KB, key=서버생성 base64url~22자). **payload 계약 = `{ v:1, scenario: PersistedScenarioState }`** — OG(api/og)가 이 payload로 카드를 그린다(형태 변경 시 OG 동반 수정). 읽기 scenario는 신뢰불가 → `normalizeSharedSnapshotScenario`(normalizePersistedAppState)로 정규화. 근거 supabase/migrations/20260720000000_shared_snapshots.sql, shared/lib/supabase/sharedSnapshots.ts, pages/Main/hooks/persistence/usePortfolioPersistence.ts(createShareLink·restore effect)·shareUrl.ts(S_QUERY_PARAM).
- [2026-07-31][state] 🔴 **공유 링크 복원 실패는 "조용히 빈 화면"이 아니라 배너 한 줄로 말한다**(무음 실패 금지의 공유 판). 파싱 경계는 `decodeSharedScenarioResult` **한 곳**이고 절대 던지지 않는다 — 화면 컴포넌트에 try/catch 를 흩뿌리지 마라. 상태는 `shareLinkFailureAtom`(비영속 UI atom, 값 2개: `invalid`=링크를 못 읽음 · `unavailable`=`?s=` 스냅샷 부재/조회 실패)이고 소비처는 `MainRightPanel` 의 `ShareLinkFailureNotice` **하나**. 화면은 둘로 충분하고 **계측은 더 세분한다**(`operation_error` + `apply_share_link` + reason `decode_failed`(기존 라벨 보존)/`schema_mismatch`/`db_snapshot_missing`/`db_fetch_failed`) — 새 이벤트를 만들지 않았다. 값이 빈 `?share=` 는 **공유 시도가 아니므로 안내도 없다**(평범한 방문자에게 없던 사고를 알리지 않는다). 공유 URL·영속 스키마 **무변경**(잘못된 입력의 처리이지 형식 변경이 아니다). 근거 pages/Main/hooks/persistence/shareLink.ts, test/main/shareLinkFailureNotice.test.tsx.
- [2026-07-19][state] 세션시작 동기화의 **"양쪽 내용 있고 의미있게 다름"은 무음 last-write-wins → 충돌 화해로 교체**(사용자 승인된 반전 — 구 CloudConflictPrompt 개선 재도입). 순수 엔진(cloudWorkspaceSyncEngine)은 그 케이스에 **conflict 이벤트만 방출**(어느 쪽도 apply/mirror/push 안 함). 3-way 화해는 순수함수(cloudWorkspaceReconcile.ts): **디바이스=로컬을 클라우드에 push만**(앱·로컬 이미 정본) / **클라우드=apply+로컬미러**(push 없음) / **블렌드=합집합 후 앱·로컬·클라우드 양쪽 반영**. 경계훅 useCloudWorkspaceSync가 `cloudConflictAtom`(모달 데이터)+`cloudSyncStateAtom.status='conflict'`(push 정지 신호)로 배선하고 `{conflict,summary,resolveWithDevice/Cloud/Blend,deferConflict}` API를 반환. **무음 유지 케이스=한쪽만/동일/로컬읽기실패/pull실패**(빈 클라우드슬롯+로컬내용은 여전히 잔여 latest-wins — 결정 §1). 블렌드 병합은 **이름 포함 비교**(같은 id라도 이름만 달라도 발산본 "(클라우드)" 접미로 둘 다 보존=비파괴, 1라운드 수렴). **저장/공유/DB 스키마 무변경**(런타임 'conflict' 상태·API만 추가). 이연=디바이스 유지+세션 내 push 정지+충돌 데이터 유지(헤더 재개봉용). 모달/헤더 UI는 frontend 후속. 근거 jotai/snowball/cloud/cloudWorkspaceReconcile.ts·cloudConflictState.ts, useCloudWorkspaceSync.ts.
- [2026-07-23][state] **세션시작 리컨사일에 merge-base 3-way FF(정책 A) 도입**(사용자 승인, 위 2026-07-19 충돌화해의 상위 판정으로 추가). 무음 last-write-wins도, 무조건 모달도 아닌 **git 스타일 3-way**: per-user 마지막 동기화 해시(base=`serializeMeaningfulPayload`, `jotai/snowball/cloud/cloudSyncBase.ts`, **localStorage** `snowball:cloud-sync-base:<userId>`)로 판정 — **한쪽만 base에서 변함=조용히 FF**(cloud apply 또는 local push, 모달 없음·헤더는 기존 saved/synced 전이만 재사용), **양쪽 다 변함=진짜 동시편집 → 리컨사일 모달 세션당 1회**(이 기기/클라우드/둘 다-합치기 선택, 해결 시 base 갱신→재발 없음), **base 없음(신규기기·사파리프라이빗·localStorage불가)=현행 1.4/1.45/1.5 휴리스틱 폴백**(하위호환). base 확립 지점 3곳: ①세션시작 엔진의 모든 수렴(noop/applied-cloud/pushed-local)에서 `writeBase(합의해시)`, ②`resolveWith*`(device/cloud/blend) 성공 시(defer는 안 함), ③디바운스 autosave 'saved' 시 **push된 payload 해시**(현재값 X — 스케줄러 `onSaved(pushedPayload)`가 run() 캡처값 전달, 레이스 가드). **하위호환 불변**: 영속 payload/`user_app_states`/공유 스냅샷/공유 URL(`?share=`·`?s=`) **전부 무변경**, base는 그 밖의 client 부기값이라 **마이그레이션 불요**(기존 사용자 base=없음→폴백→자동확립), **로컬 삭제 없음**(2026-07-19 결정 존중). "sync후 IndexedDB 삭제→클라우드 단일소스" 아이디어는 오프라인편집·로그아웃 유실이라 기각. **M1 보강(2026-07-23, 배포 전 리뷰)**: base는 per-user지만 로컬 autosave는 전역이라 계정전환(A→B→A) 시 3-way가 B로 A의 클라우드를 무음 FF-push하는 손실이 있었다 → 전역 로컬의 **직전 소유 사용자**(`snowball:cloud-local-owner`, 세션시작 claim, 로그아웃에 안 지움)를 기록해 owner≠현재면 무음 FF 금지·conflict(보호 모달)로 위임한다(엔진 1.25 가드 `isForeignLocalWorkspace`). 콘텐츠 휴리스틱은 고정 DEFAULT_SCENARIO_TAB_ID+인기 티커 겹침 때문에 이 앱에선 불충분해 identity로 잡음(pitfalls 참조). 근거 cloudWorkspaceSyncEngine.ts(1.3 base 블록·1.25 M1 가드·주입), useCloudSync.ts, useCloudWorkspaceSync.ts, cloudSyncBase.ts.
- [2026-07-23][state] ~~**무모달 자동병합(A+)**~~ **철회 → Policy A 확정**(사용자 최종 결정). A+는 진짜 동시편집도 `mergeWorkspaces`로 즉시 자동 해소하고 비침습 notice(`cloudReconcileNoticeAtom` + 헤더 "여러 기기 변경 정리됨")+in-memory 되돌리기 스냅샷으로 처리하는 안이었는데, 사용자가 **"진짜 충돌일 때만 1회 뜨는 모달"이 낫다**고 최종 확정해 **되돌렸다**. 지금 코드 = 위 [정책 A] 그대로: 양쪽 다 변함=리컨사일 모달 1회(이 기기/클라우드/둘 다). ⚠ **재도입 금지(사용자 승인 필요)**: `autoReconcile`(즉시 자동병합)·`cloudReconcileNotice.ts`·`useCloudReconcileNoticeValue`·`HeaderReconcileButton`·analytics `auto_blend/undo/use_cloud/keep`·모달의 "리뷰(keep/undo/cloud)" 변형은 전부 제거됐다. **blend 로직 자체(`mergeWorkspaces`/`resolveWithBlend`/`previewBlend`)는 유지** — 모달의 "둘 다 합치기(추천)" 선택지가 그대로 쓴다. **merge-base(핑퐁 해소)는 A+와 무관하게 유지**(위 항목). 모달 카피는 "덜 위압적"으로 정돈(제목 "다른 기기에서도 편집했어요", 파괴적 red hint→담담한 "…탭은 반영하지 않아요"). 근거 useCloudWorkspaceSync.ts(conflict→cloudConflictAtom+resolveWith*), components/CloudReconcileModal(device/cloud/blend/defer), MainLeftPanel(auto-open 모달), test/main/cloudConflictLoop·cloudReconcileModal·cloudSyncIndicator.
- [2026-07-17][product] **PNG 캡처 기능 완전 폐기**(구 Stage 2 "Capture 강등" → 전면 삭제, 사용자 확정). `components/TickerCreation/capture/*`+`capturePage.ts` 삭제, 툴바 퀵액션 = **[내 저장][Share]**(Coffee 숨김 유지), html2canvas 의존성 제거(유일 소비처가 capture/tiling.ts였음). ⚠ **이 "재도입 금지"는 2026-07-20 PDF 리포트 트랙에서 사용자 승인으로 해제됐다**(아래 §PDF 리포트) — 단 화면 PNG 캡처 기능(툴바 퀵액션)은 여전히 폐기 상태이고, html2canvas는 PDF 리포트 파이프라인 안에서만 쓴다. 관련 계측(`CTA_CLICK quick_action_capture`·`OPERATION_ERROR capture_page`)도 소멸(analytics.ts OPERATION_ERROR 상수 자체는 저장/불러오기용으로 존치).

## PDF 리포트
- [2026-07-20][product] 더보기(⋯) 메뉴의 **"PDF 리포트 저장" = jspdf + html2canvas 다운로드**(사용자 verbatim "리포트 형식의 pdf 파일을 다운로드"). `window.print()` 인쇄 다이얼로그는 **기각**(다운로드가 아니다). 두 의존성은 `await import()` 전용이라 초기 번들 격리 — 실측: 엔트리 청크에 `jsPDF|html2canvas` 문자열 0회, 별도 청크 `pdfReportPipeline`(26KB)·`html2canvas.esm`(202KB)·`jspdf.es.min`(388KB)로 분리. **이것이 구 [2026-07-17] "PNG 캡처 폐기·html2canvas 재도입 금지"의 사용자 승인된 예외**다(화면 PNG 캡처 기능 자체는 계속 폐기). ⚠ 승인 경위 정정: 재설치 시점에는 승인이 **없었고**(에이전트가 "대시보드를 그대로 캡쳐" 요청을 승인으로 임의 해석), 사후에 사용자가 유지로 확정했다. 금지 결정을 뒤집을 때는 **먼저 물어라** — 요청 문구의 해석은 승인이 아니다. 현 상태의 정확한 규칙 = **"html2canvas는 동적 import로 격리된 PDF 리포트 파이프라인 안에서만 허용, 그 밖의 재도입은 여전히 사용자 승인 필요"**.
- [2026-07-20][design] 리포트는 **현재 프리셋의 `light` 토큰 고정**(다크 사용자도 라이트 출력). 구현은 `getPrintChartTheme(presetId)`/`getPrintThemeTokens(presetId)`(shared/styles/chartTheme.ts, 순수·DOM 무접촉) + **각 페이지 div의 인라인 `style`에 `--sb-*` 맵 주입**. `document.documentElement.dataset.theme='light'` 금지(화면 번쩍임 + 차트 useMemo 미재빌드).
- [2026-07-20][design] 리포트 차트는 **대시보드와 같은 옵션 빌더**(`buildAllocationPieOption`/`buildYearlyResultBarOption`/`buildRecentCashflowBarOption`)를 재사용하되, 세 빌더에 **optional `theme` 파라미터**를 새로 열었다(미지정=`getChartTheme()` 화면 테마 — 기존 호출부 무변경). 옵션을 새로 만들지 말 것: 비주얼 동일성이 "대시보드를 그대로 캡쳐" 요구의 본질이다. 렌더는 오프스크린 임시 인스턴스 + `getDataURL({pixelRatio:2, backgroundColor})` → `<img>`(고정 크기라 모바일 뷰포트에서도 같은 품질).
- [2026-07-20][frontend] `HeaderOverflowMenu`는 커뮤니티와 공유하므로 **시뮬레이터 데이터에 결합시키지 않는다** — `showPdfReport`+`pdfReport`(onDownload/isGenerating/**failure**/blockedReason) prop만 받고, 상태는 페이지 로컬 컨테이너 `pages/Main/components/MainOverflowMenu`가 소유한다. `onDownload`는 **`Promise<boolean>`**(성공=true → 메뉴 닫고 트리거 포커스 복귀)로 정의했다 — void면 메뉴가 성공/실패를 구분할 수 없다. `failure`는 **불리언이 아니라 `{message, canRetry}`**: 메뉴는 사유 어휘를 모르고 완성된 문구와 재시도 가능 여부만 받는다(공유 컴포넌트가 시뮬레이터 실패 분류에 결합되지 않게).
- [2026-07-20][product] PDF 실패는 **두 갈래로 갈라 안내**한다(리뷰 Major): `report-unavailable`(payload로 계산 불가) = "이 시나리오로는 리포트를 만들 수 없어요 · 포트폴리오와 투자 조건을 확인해 주세요" + **재시도 버튼 없음**(몇 번을 눌러도 같은 결과라 "잠시 후 다시 시도"는 틀린 안내다) / `render-failed`·`unknown` = "잠시 후 다시 시도해 주세요" + 재시도. 사유 어휘는 **의존성 0짜리 `pages/Main/hooks/interaction/pdfReportError.ts`** 가 소유한다 — `PdfReportError`가 파이프라인 모듈에 살면 UI의 `instanceof` 한 줄이 jspdf·html2canvas를 초기 번들로 끌어온다. `OPERATION_ERROR`에도 `reason`을 실어 두 실패의 발생 비율을 나눠 본다.

## 티커 SEO 콘텐츠 (`shared/constants/tickers/`)
- [2026-07-22][seo] **엔진 6필드(가격·배당률·성장률·기대수익·주기·이름)는 `TickerContent`에 절대 복제하지 않는다** — 콘텐츠 문단은 `{{dividendYield}}` 같은 토큰만 담고, `resolveTickerEngineFacts(ticker)`가 `DIVIDEND_UNIVERSE`(프리셋+시장데이터 오버레이)에서 조인한 값을 `renderTickerContentTemplate`가 렌더 시점에 치환한다. 이유: 티커 자동 갱신 크론(Yahoo Finance)이 배당률·주가를 주기적으로 덮어쓰는데, 숫자를 문자열에 직접 박으면 프리셋이 갱신된 뒤에도 콘텐츠가 옛 숫자를 보여줘 결과적으로 "숫자 날조"와 같아진다. 반대로 운용보수·상장연도·추종지수처럼 자동 갱신 파이프라인이 안 건드리는 정적 사실은 토큰 없이 리터럴로 적어도 된다(과설계 회피 — 전부 토큰화하지 않는다). 근거 shared/constants/tickers/resolveTickerEngineFacts.ts, renderTickerContentTemplate.ts.
- [2026-07-22][seo] `shared/constants/tickers/`는 **`shared/constants/index.ts`(최상위 배럴)에 의도적으로 미연결**한다 — community 폴더와 같은 격리 관례(architecture.md "Isolation"). 티커가 늘어날수록 한국어 서사 텍스트 총량이 커지는데 최상위 배럴은 앱 전역 import라 엔트리 번들에 실린다. 소비는 반드시 `@/shared/constants/tickers` 폴더 경로로, 페이지는 React.lazy 라우트 청크 안에서만 import할 것.
- [2026-07-23][data] **10종 확장 세션에서 콘텐츠 작성 에이전트가 플래그한 7개 시점민감 항목을 ticker-data-curator가 전부 발행사 공식 소스로 스팟체크 완료**: VYM 보유종목수(605종, 뱅가드 공식 팩트시트로 확정 — 기존 "608~618 편차" 서술 대체)+섹터 순서 오류 정정(산업재 14.4%>헬스케어 12.4%인데 반대로 적혀 있었음), SCHY 보유종목수(133종, 2개 소스로 재확인 — 변경 없음), HDV 상위10 비중(약 51%, 재확인 — 변경 없음), SPYD 재편월(S&P DJI 공식 방법론 문서로 1월·7월 확정), JEPI 보유종목수·섹터(JP모건 공식 팩트시트로 129종+섹터 4종 신규 확보 — "액티브라 비움"이 처음엔 맞았지만 발행사가 팩트시트로 공개하고 있어 채울 수 있었다), O 배당 CAGR(연 4.1%, 리얼티인컴 공식 투자자 페이지)·AFFO 배당성향(2026 Q1 실적발표 71.7%), DGRW 섹터 비중(재검증 결과 소스 간 22.6~33.4%로 변동성이 커 **비움 유지가 맞다는 결론**). 이 과정에서 JEPQ 보유종목수(111→110)·O의 연속 지급/인상 횟수(670→673회/114→115회)도 같은 원천에서 더 정확한 최신값을 발견해 함께 갱신했다. **"의도적으로 비웠다"는 최초 판단이 확정적 선언이 아니라 "그 조사 시점에 확인 못 함"이라는 뜻임을 보여주는 선례** — 다음 리프레시에서 새 소스가 발견되면 다시 채울 수 있다. 근거 shared/constants/tickers/{vym,schy,hdv,spyd,jepi,jepq,o}.ts.
- [2026-07-23][seo] **`TickerReferenceFacts`에 없는 필드(예: FFO/AFFO 배당성향처럼 분기마다 바뀌는 개별종목 전용 지표)를 확인했다면, 타입을 확장하지 않고 본문 프로즈 + `asOfNote`에 산문으로 녹인다** — O의 AFFO 배당성향(2026 Q1 71.7%)이 이 사례. 구조화된 필드로 만들면 ①분기마다 갱신 의무가 생기고 ②ETF 대다수엔 해당 없는 개별종목 전용 개념이라 타입 전체가 무거워진다(과설계 회피 원칙, decisions.md 위 항목과 동일 취지). 정말 여러 개별종목에 반복적으로 필요해지는 시점에만 필드 추가를 재고할 것.
- [2026-07-22][seo] `TICKER_CONTENT_REGISTRY`(`registry.ts`)는 `PresetTickerKey`(계산 유니버스 전체)의 **부분집합**만 담는다 — 콘텐츠 엔트리가 없는 티커는 이 맵에 없고, `/ticker/:name`은 `findTickerContentBySlug` 결과가 `undefined`면 404로 처리해야 한다(엔진 계산은 되지만 SEO 콘텐츠는 없는 티커가 대다수라는 전제). 티커 하나 추가 = 데이터 파일 하나(`schd.ts` 패턴) + registry.ts 한 줄.
- [2026-07-22][seo] SCHD 콘텐츠의 `reference.topHoldings`는 **의도적으로 비웠다** — 리밸런싱마다 바뀌는 가장 변동성 큰 필드라 조사 시점에 신뢰할 단일 현재값을 못 얻으면 비우고, 채우려면 ticker-data-curator 검증을 거치게 했다(숫자·목록 날조 금지 원칙의 연장). `reference.consecutiveGrowthYearsApprox`(약 14년)·`historicalDividendCagrPercent`(약 10%대)·`topSectors` 순서는 2026-07 조사 시점 근사치이며 `reference.asOfNote` 필드가 모든 엔트리에 필수로 그 기준시점·한계를 고지하게 타입을 설계했다(옵셔널이 아님).
- [2026-07-23][seo] **10종 일괄 확장 완료** — 배당성장(VIG·DGRO·DGRW·SCHY), 고배당(HDV·VYM·SPYD), 커버드콜(JEPI·JEPQ), 리츠/월배당(O). 등록은 브리핑대로 "데이터 파일 1개 + registry.ts 한 줄 + index.ts 배럴 한 줄"로 끝났다(페이지·사이트맵·JSON-LD·목차는 무수정, 레지스트리 파생 그대로 동작 확인). 개별 종목(O)은 ETF 6섹션 템플릿의 "운용보수"·"구성 기준" 섹션을 **개념이 안 맞아 재해석**했다 — id는 유지하되(`expense-ratio`→FFO·트리플넷 비용구조, `selection-criteria`→보유 부동산 분산) navLabel/heading/본문만 바꾸는 패턴으로 템플릿을 깨지 않고 확장했다. 커버드콜(JEPI·JEPQ)은 브리핑 지시대로 "배당성장" 섹션을 "분배 전략" 프레이밍으로 바꿔 **엔진의 `dividendGrowth: 0`(JEPI)·`0.8`(JEPQ) 가정 자체를 콘텐츠의 핵심 메시지**로 삼았다(왜 0인지를 정직하게 설명 — 옵션 프리미엄 비중이 큰 분배금은 방향성 있게 늘어난다고 가정하기 어렵다는 서사와 엔진 값이 일치). `TickerReferenceFacts`의 `trackedIndex`(액티브 운용·개별종목엔 없음)·`expenseRatioPercent`(개별종목엔 없음)·`holdingsCountApprox`(개별종목엔 없음)는 해당 안 되면 **비우고 JSON-LD도 자동으로 그 PropertyValue를 생략**한다(buildFinancialProductSchema의 conditional spread가 이미 그렇게 설계돼 있어 추가 분기 불필요 — 확인만 하면 됨). 근거 shared/constants/tickers/{vig,dgro,dgrw,schy,hdv,vym,spyd,jepi,jepq,o}.ts.
- [2026-07-23][seo] **스케일 데이터 포인트**: `api/ticker-html.js` 번들 크기가 1종(SCHD, 2026-07-22)일 때 약 196KB → 11종(2026-07-23)일 때 **415,994 bytes**로 성장했다 — 티커당 순증 약 22KB(한국어 서사 6섹션+FAQ 5~8개 기준). 서버리스 함수 크기 한도(수십 MB)엔 전혀 문제없지만, architecture.md가 예고한 "수백 종 스케일에서 재점검"이 실제로 온다면 대략 이 비율(22KB/종)로 외삽해 판단할 것 — 예 100종이면 약 2.2MB, 1000종이면 약 22MB로 그때는 재점검 대상이 맞다. `npm run api:bundle` 재생성 시 `og.js`/`share-html.js` 등 무관 번들은 **바이트 단위로 동일**했다(git status에 `M` 없음) — "og/share-html이 stale로 뜰 수 있다"는 우려는 이번엔 현실화되지 않았다(재확인 습관은 유지할 것, 매번 보장되는 동작이 아닐 수 있다).
- [2026-07-22][seo] **티커별 액센트 테마 = `TickerContent.accent?`(optional, 장식 전용)**. 상세 페이지(`pages/Ticker/TickerDetailPage`)가 `AccentScope` 루트에 원시값(`--tk-from/to/text-light/text-dark`)을 인라인 주입하고, 파생 변수(`--tk-gradient/text/soft/border/active-bg/solid`)로 히어로 그라데이션·스탯 하이라이트·목차 활성·CTA에만 쓴다. **원칙: 앱 팔레트 토큰(`--sb-*`)은 절대 덮어쓰지 않는다** — 액센트는 별도 `--tk-*` 네임스페이스라 `html[data-palette]` 테마 시스템과 충돌하지 않고, 미지정 티커는 `AccentScope` 기본값(=브랜드 토큰)으로 폴백한다(전 티커가 색을 가질 필요 없음). 라이트/다크 대비는 `textLight`/`textDark` 2개로 명시하고 soft/border는 `color-mix(--tk-text, surface)`로 파생해 테마 전환을 리렌더 없이 따라간다(`@media prefers-color-scheme` + `data-theme` 오버라이드 둘 다 스코프 안에서 처리). ⚠ 서버(`api/ticker-html`)는 accent를 안 읽지만 `shared/constants/tickers` 수정이므로 `npm run api:bundle` 재생성 필수(`api:check` OK 확인). 근거 shared/constants/tickers/TickerContent.types.ts(TickerAccentTheme), pages/Ticker/TickerDetailPage/TickerDetailPage.styled.ts(AccentScope).
- [2026-07-22][seo] **티커 SEO 페이지는 자체 헤더를 만들지 않고 전역 `components/PrimaryNav`를 재사용**한다(`TickerPageShell`이 SimulatorHeader와 같은 전폭 sticky 글래스 바로 감쌈) — 로고·워드마크가 시뮬레이터/커뮤니티와 픽셀 단위로 일치. 진입 메뉴는 PrimaryNav NavItems에 `/ticker/all` 링크(BookOpen, copy `COMMUNITY_COPY.nav.tickers='종목 소개'`)를 추가해 **두 헤더에 자동 노출**(커뮤니티 활성 여부와 무관, 항상 렌더). ⚠ PrimaryNav는 엔트리 번들이라 여기 추가하는 건 경로 문자열·lucide per-icon만 — 티커 **콘텐츠/페이지**는 여전히 lazy 청크에만(빌드 확인: SCHD 서사 문자열이 entry `index-*.js`에 없고 `registry-*.js` lazy 청크에만 존재). 상세 페이지의 유일 `<h1>`은 히어로 티커 제목이라 shell의 PrimaryNav는 `brandAs='span'`(기본값)으로 둔다. 관련 티커 링크는 서버 `renderRelatedTickers`와 동일하게 `findTickerContentBySlug`로 게이팅(콘텐츠 없으면 데드엔드 링크 대신 비링크 텍스트). 근거 pages/Ticker/components/TickerPageShell, components/PrimaryNav/PrimaryNav.tsx, pages/Ticker/TickerDetailPage/TickerDetailPage.utils.ts(hasContent).
- [2026-07-22][seo] 크롤러 HTML 서버렌더 랜딩(`server/handlers/TickerHtml/` → `api/ticker-html.js`, `PostHtml.ts` 미러). ⚠ **위 "404로 처리해야 한다" 항목을 문자 그대로 HTTP 상태코드로 읽지 말 것** — 실제로 착지한 계약은 **콘텐츠 없는 티커·`name` 파라미터 부재 = 404가 아니라 무치환 셸 200 + no-store**다(post-html의 "글 없음=404"와 의도적으로 다르다). 이유: `TICKER_CONTENT_REGISTRY`는 코드에 커밋된 정적 데이터라 "이 티커는 아직 콘텐츠가 없다"가 DB 글처럼 삭제·비공개 같은 예외 상태가 아니라 **흔한 정상 상태**이고, 다음 배포로 콘텐츠가 생기면 다음 크롤에 바로 반영돼야 하므로 캐시도 안 한다(no-store). 위 "부분집합 → 404" 문장은 클라이언트 SPA 라우트(`pages/Ticker/TickerDetailPage`가 `viewModel` 없으면 `/ticker/all`로 `<Navigate replace>`하는 것)를 가리킨 것으로 재해석한다 — SPA 클라이언트 전환에는 애초에 HTTP 상태코드가 없다. `/ticker/all` 허브는 `name==='all'`을 개별 조회보다 **먼저** 분기해야 한다(레지스트리 키와 충돌 안 하는 예약어). 성공 캐시는 `s-maxage=86400`(post-html의 300보다 훨씬 길다 — 콘텐츠는 배포에만 바뀌고 DB 조회가 없어 신선도 걱정이 다르다). JSON-LD(`FinancialProduct`+`FAQPage`, 허브는 `ItemList`)는 이 서버 경로가 유일한 삽입 지점(런타임 JS 삽입은 크롤러가 못 읽음). 사이트맵은 `vite.config.ts`가 `shared/constants/tickers`를 `loadEngine`과 같은 esbuild 메모리 평가 기법으로 읽어 `TICKER_CONTENT_LIST`에서 파생 — `ROUTES` 배열에 하드코딩하지 않는다(티커 추가 시 `dist/sitemap-pages.xml` 자동 갱신). 근거 server/handlers/TickerHtml/TickerHtml.ts, vite.config.ts loadTickerRoutes, test/api/tickerHtml.test.ts.

## 카피 규칙 (전 에이전트·전 콘텐츠 공통 — 사용자 명시)
- [2026-07-22][copy] **앱 이름 "Snowball Income"은 그냥 캐치하게 지은 이름일 뿐 — 콘텐츠를 앱 이름과 연관시키지 않는다.** "눈덩이 / 스노우볼 / 눈덩이를 굴린다" 같은 비유·표현을 **어떤 카피에도 쓰지 않는다**(제목·본문·CTA·메타·FAQ·도움말 전부). 같은 개념(배당 재투자 복리)은 **복리·시간·재투자**라는 직접적 언어로 푼다. 예외: 브랜드 워드마크와 타이틀 suffix "Snowball Income"은 **사이트 식별자(브랜드명)**라 모든 페이지 공통으로 유지한다 — 이건 "테마 연관"이 아니라 그냥 이름이다. 위반 실사례: `pages/Ticker/TickerDetailPage/TickerDetailPage.view.tsx`의 CTA "…로 눈덩이를 굴리면 얼마가 될까"(2026-07-22 수정). portfolio-post-writer 정의에 있던 눈덩이 금지 규칙과 동일 취지이며, 이제 **전 에이전트 공통 규칙**으로 승격. 근거: 사용자 지시 2026-07-22.

## 도메인 (배당 시뮬레이터)
- [2026-07-15][engine] 배당은 가격에 앵커: `priceGrowth = dividendGrowth`, `totalReturn = dy + dg`(파생값). 티커 3분류: 가격·배당률·주기=관측 사실(크론 갱신) / expectedTotalReturn=사람의 가정(자동화 금지) / dividendGrowth=파생(etr−dy, 과거 CAGR로 덮어쓰기 금지).
- [2026-07-18][data] **티커 자동 갱신 기본 프로바이더 = Yahoo Finance**(무료·무키, `scripts/tickerRefresh/provider/yahooProvider.ts`), FMP는 `--provider=fmp` 옵션으로만 유지(유료 키 보유자용). 근거는 pitfalls.md "FMP 무료 티어 불가" 항목. 워크플로(`.github/workflows/refresh-tickers.yml`)도 기본 Yahoo로 전환돼 `FMP_API_KEY` 시크릿 없이 주간 크론이 돈다.
- [2026-07-18][data] ~~**티커 갱신 = 월~금 요일 분할(5버킷) + 주간 누적 PR**~~ **(대체됨 → 아래 월간 자동머지)**. `partitionTickers`(정렬 후 `index%5`, 순수 함수)와 CLI `--bucket=N`은 수동용으로 **남겨둠**(테스트도 유지). `--only`가 `--bucket`을 완전히 대체하는 원칙도 유지.
- [2026-07-18][data] **티커 갱신 = 월 1회 전체 갱신 + 자동 머지**(사용자 요청으로 위 요일분할을 대체). 매월 1일 크론(`0 21 1 * *` = 2일 06:00 KST)이 유니버스 전체를 한 번에 갱신하되, `--delay=2000`(요청 2s 간격, ~68종 ≈ 2–3분)으로 러너 IP 버스트를 막는다 — 레이트리밋 방어를 **요일분할 대신 요청 딜레이**로 달성. 게이트(`tsc -b tsconfig.build.json` + `test:ci`) 통과 후에만 `chore/refresh-ticker-data` 브랜치에 커밋→PR 생성→**자동 머지(`gh pr merge --merge --delete-branch`) = Vercel 배포**. 스케줄 실행은 항상 자동머지, 수동 dispatch는 `auto_merge=false`로 PR만 열고 검토 대기 가능. 브랜치는 매월 fresh(푸시 전 remote 브랜치 삭제 → `--force` 불필요). **⚠ 이 자동머지는 [[ask-before-deploy]] '배포는 승인 후' 원칙의 명시적 예외 — 사용자가 티커 데이터에 한해 자동 배포를 승인한 것**. 근거 `.github/workflows/refresh-tickers.yml`.
- [2026-07-20][engine] 시나리오 payload(`{portfolio, investmentSettings}`) **실행 단계는 `SnowballScenarioRun.ts`의 `runScenarioPayload` 단 하나**다(zod 해석 → 가중치 정규화 → 티커별 `runSimulation` → 연간 합산 → `{profiles, weights, values, outputs, yearly}`). 원래 `buildScenarioSimSummary` 안에 인라인돼 있던 것을 PDF 리포트 트랙에서 추출했고, `buildScenarioSimSummary`/`buildSnowballReport` 둘 다 이걸 호출한다. **payload를 읽는 새 요약 함수는 반드시 이 함수를 경유**할 것 — 복붙하면 두 표면(카드/OG/PDF)이 조용히 다른 숫자를 낸다.
- [2026-07-20][engine] 정합 모델에서 **"배당 성장 기여분 vs 주가 성장 기여분" 분해는 원리적으로 불가능**하다 — `priceGrowth === dividendGrowth`(자유도가 하나)라 두 기여를 가르는 정의 자체가 없다. 리포트가 채택한 대체 분해는 **원금 + 재투자된 배당 + 시세 평가이익 = 최종자산** 항등식(`totalContribution` / `totalCostBasis − totalContribution` / `unrealizedGain`)이고, 세 항의 합이 `finalAssetValue`와 정확히 일치함을 테스트로 고정했다(test/snowball/report.test.ts). "성장 기여도 차트" 요구가 다시 오면 이 3분해로 응답할 것.
- [2026-07-20][engine] 리포트 **YoC(Yield on Cost) = 그 해 세후 연배당 ÷ 그 시점 누적 투입원금**(재투자분 제외한 "내 돈")으로 정의했다. 포트폴리오 **명목 가중평균 배당률**(Σ 배당률×비중, 세전·시점무관)과는 분모·세전세후가 모두 달라 **두 값을 나란히 비교하면 안 된다** — PDF 카피에서 "배당률이 3.5%→9%로 올랐다"식 서술은 오해를 부른다.
- [2026-07-26][data] **`ticker:paydates` 큐 공정성 확정 설계**(브랜치 fix/paydates-rotation): ①**무상태 날짜 회전** — `payDatesQueue.prioritize`가 미승급/승급 두 그룹 각각을 `rotationDay(UTC epoch-day) % group.length`만큼 좌회전, `--only` 시 무회전. 스냅샷에 lastCheckedAt류 **매런 변하는 타임스탬프 기록 금지**가 제약이었다("변경 없으면 커밋 0" 가드가 무력화돼 매일 노이즈 커밋+Vercel 배포 발생). ②**`payoutMonthsSource: 'none'`** = "권위 소스(Alpha Vantage 실지급일)를 조회했으나 배당 기록 없음" 영구 마커(무배당 종목 ANET의 일일 쿼터 낭비 차단). 불변식: `'none'` ⇒ `payoutMonths` 없음(월이 있는 ex/pay 엔트리는 빈 응답이어도 skipped — 출처를 거짓말하지 않는다). 미승급 큐에서 제외되고 후순위 그룹에서 회전 재확인, Yahoo가 월을 새로 추론하면(배당 개시) refresh.ts에서 `'ex'`로 자연 해제. 근거 scripts/tickerRefresh/{payDatesQueue.ts, payDates.ts, refresh.ts, payDatesCli.ts}.
- [2026-07-16][product] 아이콘은 lucide-react, strokeWidth 1.8 통일. 클리셰 글리프(🚀 로켓, 안락의자 등) 금지 — "올드함"의 원인은 라이브러리가 아니라 글리프 선택.
- [2026-07-25][design] **표시 통화(원↔달러) 토글의 자리 = 투자 설정 카드 안, "배당 재투자" 바로 아래**(사용자가 두 번 반복 요청 + 위치·스타일 후속 지시로 확정). 구 자리(결과 컬럼 최상단 `ResultsToolbar`)는 **폴더째 삭제**됐다 — 되돌리려면 사용자 승인. ⚠ 이 이동으로 컨트롤이 `pages/Main/components/` → **`components/CurrencyToggleField/`(재사용 레이어)** 로 옮겨졌다: `components/InvestmentSettings`가 소비하므로 페이지 폴더에 두면 역방향 의존이 된다. 시각 통일 요구("같은 박스의 다른 토글과 같은 컨트롤로 보여라") 때문에 라벨 줄을 자체 조립하지 않고 **공용 `ToggleField`** 를 쓰되, 시각 라벨("표시 통화")과 스위치 접근명("결과를 달러로 표시")이 달라야 해서 `ToggleField`에 **옵셔널 `accessibleName`** 을 신설했다(미지정=기존과 동일, 호출부 8곳 무영향). 유지되는 계약: 그룹 `role="group" aria-label="결과 표시 통화"`, 캡션 `role="status"` **항상 마운트**, 환율 없으면 `disabled`, on/off 텍스트 원/달러 + `controlWidth="60px"`. 알려진 트레이드오프(사용자 인지함): 좌패널은 ≤960px에서 드로어로 접히므로 **모바일에서는 드로어를 열어야** 통화를 바꿀 수 있다. 빈 상태(티커 0개)에서도 토글이 보이는 것은 의도 — 결과의 부속이 아니라 환경설정이라 첫 결과부터 원하는 통화로 나오게 한다. 근거 components/CurrencyToggleField/, components/InvestmentSettings/InvestmentSettings.tsx, test/main/displayCurrencyPropagation.test.tsx.
- [2026-07-22][design] 포트폴리오 비중 UI의 **두 "잠금" 개념을 아이콘 메타포로 분리 확정**(사용자 승인): ①전역 "비율 조절 잠금" 토글(A, 모든 슬라이더 disabled, 모바일 기본 ON — `isLocked` 초기값 유지)은 **자물쇠 계열**(lucide `Lock`=잠김 / `Pencil`=조절중), onText"잠금"/offText"조절"·label 불변. ②종목별 "고정" 버튼(B, 그 종목 비중 고정+나머지 재분배)은 **핀 계열**(lucide `Pin`, active=brand 채움 유지)로, 텍스트 "고정" 유지하되 핀 글리프로 자물쇠와 시각 분리. `Lock`≠`Pin` 글리프 대비가 1차 구분자다("잠금"·"고정" 단어가 유사해 생긴 혼란 해소). **되돌려 두 개념을 같은 글리프로 합치지 말 것.** 슬라이더 비활성 4원인(isLocked / 단일종목 / 자기고정 / `adjustableTickerCount<=1`)은 **범례 하단 단일 힌트 줄로 우선순위 표시**(무음 비활성 금지, user-profile "이유 표시" 요구) + `fixedCount>=1 && !isLocked`일 때 "고정 전체 해제"(lucide `PinOff`) 단축 액션 노출. 카피 상수 신설처 = `shared/constants/allocation/`. 근거 components/PortfolioComposition/PortfolioComposition.tsx:49-111, Main.shared.styled.ts:1336(AllocationFixButton).

## 배당 캘린더 (/dividend/calendar, 2026-07-25)
- [2026-07-25][product] **배당 지급 캘린더 = 실제 월간 주×일 달력**(사용자 피벗 확정 — v1 연간 12칸 보드는 같은 날 폐기, 브랜치 feat/dividend-calendar의 v1 커밋 a8b2915에만 존재). `<table>` 시맨틱(요일 축이 실재), **`role="grid"` 금지**(지키지 않을 키보드 계약 선언 금지), 오늘=`aria-current="date"`+"오늘" 배지, **6주 고정 그리드**(레이아웃 튐 방지), 월 네비는 연 경계를 넘고 "이번 달" 버튼은 현재 월에서 disabled. 모바일도 DOM 동일(7열 유지, 셀 칩은 CSS로 감춤) + **AgendaList(날짜순 목록)를 전 폭 상시 렌더** — +N 오버플로 착지점이자 스크린리더 선형 텍스트이자 **jsdom 행동 테스트의 기준면**. 일자 추정 불가 종목은 그 월 "날짜 미정" 점선 카드(임의 날짜 배치 = 날조 금지). 스펙 정본 2부 = scratchpad dividend-calendar-spec.md(v1, 유지 항목)·-v2.md. ⚠ **2026-07-25 후속 개편으로 "아젠다 전 폭 상시 렌더"는 "기본 탭"으로 완화**됐다(아래 항목) — 테스트 기준면 역할은 그대로지만 "날짜 미정"을 보려면 탭 전환이 필요하다.
- [2026-07-25][data] **`estimatedPayDayByMonth` 계약**(marketData 스냅샷, 관측 전용 — 엔진 미접촉): 키='1'~'12'(**지급월**, payoutMonths 부분집합 보장), 값=1~31("이 날쯤" 추정). **`payoutMonthsSource==='pay'` 20종에만 존재·전 지급월 커버**, lag 없는 종목은 필드 자체 없음→날짜 미정. **2월 최대 28(연도 무관 필드) — UI가 윤년 29로 보정 금지, 값 그대로.** 파생=scripts/tickerRefresh derive(월별 배당락 일자 중앙값+exToPayLagDays, 월 경계 넘김 처리).
- [2026-07-25][state] 선택 저장 = **IndexedDB 전용 DB `snowball-dividend-calendar`**(기존 `snowball-income-db`에 스토어 추가 금지 — 버전 범프가 자동저장 하위 호환 리스크). read는 null(이력 없음)/[](빈 저장) 구분, write 실패는 조용히 resolve. **초기 우선순위 URL `?tickers=` > IndexedDB > 빈 상태**이고 **URL 유입 선택은 방문자 저장소에 기록하지 않는다**(공유 링크 열람이 내 저장을 덮으면 안 됨) — write는 사용자 조작에만. URL 동기화는 `setSearchParams(replace:true)`+타 파라미터 보존. IndexedDB 로드 경합은 userInteractedRef 가드(로드 resolve 시 사용자 조작 있었으면 stored 스킵).
- [2026-07-25][design] 캘린더 화면 2차 개편(사용자 연속 지시로 확정, 중간에 **철회된 지시 2건 포함** — "{N}종목 목록 아코디언화"와 "2열 그리드"는 되돌렸다. 다시 제안하지 말 것): ①**월간 달력 표는 선택이 0종이어도 항상 렌더**(빈 달력이 화면의 뼈대 — "지급 없음" 주장은 표가 아니라 요약 줄·빈 상태 안내·경고 배너가 한다), ②아젠다 ↔ 날짜 미정을 **한 줄 전환**(role=tab 아님 — MonthlyCashflow ViewToggleGroup 관례대로 `aria-pressed` 버튼, 미정 0건이면 버튼 자체를 렌더 안 하고 뷰가 'agenda'로 접어 읽는다), ③**종목 선택 = 우측 슬라이드 드로어**(`pages/DividendCalendar/components/PickerDrawer`) + 달력 위 필터 버튼(선택 수 배지, aria-expanded/controls) — 상단 필터 밴드는 제거, 빈 상태 안내는 드로어 **밖**에 남긴다(닫은 채로도 상태를 알 수 있어야 함). **"선택 N종" 시각 텍스트는 삭제**(배지와 중복 — 사용자 지시) → 개수는 배지(장식, aria-hidden)와 **버튼 접근명 `picker.open(n)`·라이브 리전**이 말한다. ⚠ 테스트의 선택 개수 앵커도 `getByText('선택 N종')` → `getByRole('button',{name:/현재 N종 선택됨/})`로 이동했다, ④검색 입력의 **시각 라벨 제거**(aria-label로만), 결과 목록은 **1열·행 높이 46px 균일·gap 구분**(밀도보다 가독 우선이 최종 결정), ⑤시각 시스템: 티커 시리즈 색(`utils/tickerColor.ts`)을 달력 칩·아젠다·미정·범례 점까지 일관 확장, 티커 열 **6ch 고정폭**으로 배지 시작선 통일, 배지 톤은 대비 검증 쌍만(success/successSurface·accentAltText/accentAltSubtle·textMuted/surfaceMuted). 삭제된 카피: `picker.unavailableHint`(배지가 이미 말한다), 아젠다 날짜별 건수. 근거 pages/DividendCalendar/DividendCalendarPage/DividendCalendarPage.view.tsx.
- [2026-07-25][product] **PrimaryNav "배당 캘린더" 링크 + 메뉴 순서 재배치는 메인 세션이 사용자 직접 지시로 작업한 별도 트랙**(캘린더 미션 범위 밖) — 캘린더 브리핑의 "나브 링크 금지(사용자 결정 전)"는 그 지시로 해소됐다. 관련 파일 components/PrimaryNav/PrimaryNav.tsx·shared/constants/community/copy.ts.
- [2026-07-31][design] **빈 상태 = 예시 미리보기 격자 + 그 위에 뜬 안내 카드**(리모델 §4.A-3 2안, V2/P2). 위 [2026-07-25]①"표는 0종이어도 항상 렌더"는 유지하되 **"칸이 비어 있다"는 부분만 대체**됐다: 선택 0종이면 대표 4종의 **실제** 예상 지급일을 `opacity .45`+`saturate(.5)` 로 깔고(`previewMonth`, 표현 전용 — 선택·IndexedDB·주소 어디에도 안 들어간다), 안내 카드를 격자 **상단 1/3에 앵커**한다(≥761px만 겹침, ≤760은 카드가 위·격자가 아래). 🔴 **예시임은 색이 아니라 텍스트가 말한다** — 보이는 라벨 `예시 · 실제 데이터가 아닙니다` + 표 접근명 `{월} 예시 달력, 실제 데이터가 아닙니다` + 캡션 3중. 예시 칩은 버튼이 아니다(누를 실체가 없다). **예시 종목은 스냅샷에 `estimatedPayDayByMonth` 가 있는 것만** — 그래서 스펙이 지목한 SCHD·O 는 못 쓰고(지급'월'만 안다 → 격자에 놓일 날짜가 없다) `CALENDAR_QUICK_PICK_TICKERS` 를 **JEPI·DGRO·KO·ABBV 4종으로 축소**해 칩과 예시를 같은 목록으로 묶었다(누르면 흐린 것이 그 자리에서 선명해진다). 히어로의 예상 지급일 고지 3줄은 **하단 `FootNoteCard` 로 이동**(`role="note"` 유지) — 이 화면은 `PageHero` 의 `notice` 슬롯을 쓰지 않는다. 실측 1280 문서 1,603→1,359px(−15%), 히어로 222→174px, 틴트 면 2→1개. 근거 pages/DividendCalendar/DividendCalendarPage/DividendCalendarPage.view.tsx, test/dividendCalendar/calendarEmptyPreview.test.tsx.

## 목표 달성 페이지 (/dividend/goal, 2026-07-27, feat/goal-page) → **Portfolio로 흡수됨(2026-07-28)**
- [2026-07-28][product] 🔴 **독립 목표 달성 페이지는 폐지됐다 — `/dividend/portfolio`(내 포트폴리오)의 "목표 달성 카드"가 그 역할을 전담한다**(사용자 확정, feat/portfolio-domain). 프로덕션에 나간 적이 없어 **리다이렉트를 만들지 않는다**(부재를 강제하는 테스트가 있다: test/portfolio/portfolioRouteNav.test.tsx). 내비는 "목표 달성"을 빼고 "내 포트폴리오"를 넣어 **6항목 유지**. 시뮬레이터 결과 카드의 목표 서사·진행률·설정 CTA도 함께 제거했다(**차트의 목표선·마커는 유지**) — 같은 이야기를 두 화면이 하지 않는다. **"현재 월배당"의 정의가 바뀌었다**: 보유 종목 실측이 1순위, 보유·수량·환율이 없을 때만 시뮬 파생 D1로 폴백하고 **어떤 기준으로 계산했는지 문장으로 밝힌다**(무음 폴백 금지). 목표 값 쓰기는 여전히 시뮬레이터 한 곳(아래 프리필 커밋 계약 그대로). **아래 [2026-07-27] 항목들은 계약·수치 근거로는 유효하되 "Goal 페이지"라는 장소 서술만 이 항목으로 대체해 읽어라.**
- [2026-07-27][product] **라우트 `/dividend/goal`**(구 `/goal`에서 사용자 변경, 캘린더의 형제) · 내비 라벨 "목표 달성"(lucide Target, 시뮬레이터 바로 다음, end 없음) · 데이터 경로 = `readPersistedAppState` 직접 read → 활성 시나리오 → `runScenarioPayload` 순수 재계산(전역 폼 atom 미소비, 영속/공유 스키마 무변경). **현재 예상 월배당 D1 정의 = 오늘 기준 직전 12개월 세후 합÷12, 12개월 미경과·시작 전은 1년차 월평균 폴백**(`currentMonthlyDividend`, isFallback로 힌트 카피 분기). v2 로드맵상 실보유 포트폴리오 기반 정의는 Portfolio 도메인 랜딩 후 교체(docs/ROADMAP-v2.md 부록).
- [2026-07-27][product] **목표 값 쓰기는 Goal 페이지에서 직접 하지 않는다 — 프리필 커밋 계약 확정**(state-engineer 조건부 안전 판정): 칩/직접 입력 커밋 → `location.state`(buildFocusTargetMonthlyDividendState) → 시뮬레이터 이동 → **하이드레이션 게이트 하위 `TargetFocusRequest`에서만 setField 커밋**+포커스+state 소거. 양쪽 동일 `sanitizeTargetMonthlyDividend`(상한 1억), 상세는 pitfalls [2026-07-27][state] 항목. Goal 잔류 즉시반영 UX는 별도 미션(사용자 결정 대기).
- [2026-07-27][product] **목표 칩 = 50/100/200/300만원 + 직접 입력(v2 확정)** — 구 100/300/500만원 결정을 사용자 승인으로 대체. 정본 상수 `TARGET_MONTHLY_DIVIDEND_QUICK_VALUES`(shared/constants/targets) 한 곳(리터럴 재정의 금지). ~~SimulationResult QUICK_TARGETS·GoalSetupPanel이 공유~~ → **2026-07-28: 결과 카드 칩·CTA 철거로 제품 코드 소비처는 `GoalSetupPanel` 하나**(상한의 만원 환산도 `TARGET_MONTHLY_DIVIDEND_MAX_MAN_WON` 파생 상수 한 곳에서 읽는다 — 안내 문구와 입력 max가 갈리지 않게). Goal 직접 입력만 하한 1만원(0 커밋=목표 삭제 행위 방지, 공용 sanitize는 0 허용 유지).
- [2026-07-27][design] 목표 페이지 확정 요소: 상태 7종(A~G) 전부 명시 렌더, GoalMeter(CSS 미터, 숫자 중립색, gradientAurora 필), **남은 금액 = 3번째 StatTile**(목표−현재, target≤0·도달 시 미렌더), D상태 CTA "매월 얼마를 더 넣으면 닿을까요?"(C는 "언제 목표를 달성할까요?" — D는 이미 '기간 내 미도달'로 답이 나와 같은 질문 반복은 모순이라는 reviewer 승인 이탈), 도달 판정은 `reachedMonth`/`isAlreadyReached`만(현재값>=목표 정확 비교 금지). GA 신규 `GOAL_WIDGET_VIEW`(1회, has_target=false면 progress_bucket·reached_in_range 미전송).
- [2026-07-30][design] **`DESIGN.md`(레포 루트) = 디자인 정본.** 다만 §10 "폴리시 원칙 채택/이탈" 표의 `🆕 채택` 은 **"규칙으로 적었다"는 뜻이고 "코드에 적용했다"는 뜻이 아니다** — 표는 fix 커밋(af897ec·170d0d1) **이전에** 작성돼 이후 한 번도 갱신되지 않았다(`git log -- DESIGN.md` = 2295b3a 단 1건). §10 을 근거로 "이미 됐다"고 판단하지 말고 **반드시 호출부를 grep 해라**. 감사 실측(2026-07-30): 동심 라운드 `surface()`/`nestedRadius()`/`hitArea()`/`SEPARATE_SURFACE_PADDING`/`iconOpticalAlign()` = **호출부 0곳**, `pressable` = 3곳(누를 수 있는 요소 ~74개 중), `hitAreaWithin` = 3곳(손으로 적힌 44px 히트영역 ~10곳 잔존). ⚠ **DESIGN.md 는 CLAUDE.md·.claude/knowledge/INDEX.md 어디에서도 링크되지 않아** 에이전트가 스스로 찾아 읽지 않는다 — 새 UI 미션 브리핑에 경로를 명시로 넣어라. 근거 DESIGN.md:287-307, shared/styles/index.ts:39-57.
- [2026-07-30][design] 의도적 이탈로 **확정**된 것(되돌리지 말 것): ①아이콘 획 **1.8 고정**(폴리시 원칙 #17 의 "글자 굵기에 맞춰 1.5/2" 이탈 — 예외 1건 `ThemePresetSwitcher` 2.4, `test/shared/iconConsistency.test.ts` 가 강제) ②누름 축소는 `transform: scale()` 이 아니라 **독립 `scale` 속성**(대상 컨트롤 여럿이 `transform` 을 위치잡기에 이미 쓴다) ③헤딩 줄바꿈은 `keep-all` + `balance` **한 짝**(balance 단독은 한국어를 더 나쁘게 만든다), 본문은 `pretty` 만(keep-all 금지). 근거 shared/styles/pressable.ts:12-18, shared/styles/globalStyles.ts:144-180, DESIGN.md:305.
- [2026-07-31][design] **틴트 면(§2-6 상한 2개)은 "강한 톤"에만 준다 — 약한 톤은 중립 면 + 1px 톤 테두리 + 색 아이콘.** 적용: `Banner tone='info'`(구 accent-subtle 틴트 + 3px 좌측 바 → `surface` + 1px `accent` 테두리, 좌측 바 폐기)와 목표 카드의 **성공** 상태 줄(구 successSurface 틴트 → `surface` + 1px `success` 테두리). `warning`/`danger`(및 미도달 상태 줄)는 **틴트 유지** — 경고는 눈에 띄어야 하고 한 화면에 여럿 뜨지 않는다. 테두리는 `accentBorder`(1.44~2.70:1, 장식 플로어)가 아니라 **표시색 `accent`**(3:1 게이트 `['accent','surface']`)를 쓴다 — 면을 뺀 뒤에는 테두리가 톤을 말하는 유일한 신호라 장식 급으로는 사라진다. 규칙의 단일 지점 = `components/common/Banner/Banner.styled.ts` 의 `TONE`. 실측 `/dividend/portfolio` 1280px 3개 → 2개(390px도 2개), 세는 도구 `npm run tintscan`. 새 대비 쌍 `['success','surface']`(최저 5.22:1) 추가.
  - **2026-08-01 확장: `StatTile status='success'`(결과 요약 카드의 목표 도달 타일)도 같은 어법으로 내렸다** — 구 `success-surface` 틴트 면 → 중립 `surface-muted` + 1px `success` 테두리 + 체크 글리프. 같은 브랜치가 세운 규칙을 같은 브랜치가 깼던 자리다(리뷰 B1). W1 연출("한 번만" 잠금·`iconSwapIn` 글리프 등장)은 **그대로 살아 있고** 전환 대상만 면 → 테두리 250ms 로 바뀌었다. 숫자는 여전히 중립. 실측 4개 → **3개**(1280·390 동일). 새 대비 쌍 `['success','surface-muted']`(타일 면은 `surface` 가 아니다). 대비 쌍 `text/text-secondary/text-muted × success-surface` 는 소비처가 0이 됐지만 **지우지 않는다**(성공 틴트 면을 다시 만들 때의 플로어). ⚠ **남은 3개는 선재 부채다**(히어로 `gradient-hero` + hero 타일 `accent-subtle` + 종합과세 `warning`). 2로 내리려면 hero 타일 액센트를 중립으로 내려야 하는데 결과 카드의 주인공 표시가 약해지므로 **사용자 결정 대기**이고, 그때까지 `tools/dev/tintscan.mjs` 의 그 시나리오 기준선이 3이다.

## 모션 표준 · 와우 순간 (2026-07-31 — 리워크 P3/F2·F3)
- [2026-07-31][design] **이징 토큰 3종 체계 확정**(기존 `ease` 는 유지·교체 아님): 진입/퇴장/호버 = `motion.ease`(0.2,0,0,1) · 화면 **안에서 이동** = `motion.easeInOut`(0.77,0,0.175,1) · 드로어 = `motion.easeDrawer`(0.32,0.72,0,1). 지속시간 = 누름 100–160 · 팝오버 125–200 · 드로어 200–500 · **UI 전환 300ms 미만**. **퇴장은 진입의 60%** (`motion.exit` = 120ms, `base` 200ms 의 60%)이고 이동 거리도 절반(8px→4px). 아이콘 등장은 `scale .25→1 · opacity 0→1 · blur 4→0`(공용 믹스인 `iconSwapIn`, **아이콘 전용** — 면·패널은 0.9~0.97에서 시작). 팝오버는 트리거 기준 `transform-origin`, **모달은 예외(중앙)**.
- [2026-07-31][design] 🔴 **퇴장 중인 오버레이는 다이얼로그가 아니다** — 닫는 순간 `role="dialog"`/`aria-modal`/`aria-labelledby` 를 떼고 `aria-hidden` 을 건다. **(2026-08-01) 이 처리의 정본은 공용 `components/common/Modal` 셸이다** — 셸이 `phase` 를 받으면서도 처리를 안 해서, 다음 사람이 `phase` 를 넘기는 순간 "닫았는데 열려 있는 대화상자"가 생기는 구멍이 있었다(리뷰 m2). 백드롭 클릭도 exit 에서는 끊는다. 🔴 **닫을 때 포커스는 열기 트리거로 되돌린다** — 안 그러면 퇴장 120ms 동안 `document.activeElement`(닫기 버튼)가 `aria-hidden` 서브트리 안에 남고(axe `aria-hidden-focus`) 그 뒤 `<body>` 로 떨어진다. 포커스 이펙트 deps 는 **열림 상태만**(`onClose` 를 넣으면 인라인 화살표 때문에 열려 있는 중에 포커스를 빼앗는다). 화면에만 120ms 더 남는 순수 장식이다(보조기기가 "닫았는데 열린 대화상자"를 읽으면 안 되고, 그 사이 다음 오버레이가 열리면 다이얼로그가 둘로 보인다). 🔴 **`useOverlayEscape`/`useDrawerBackClose` 에는 잔류값이 아니라 원래 열림 상태를 넘긴다** — 퇴장 동안 스택에 남으면 두 겹이 한꺼번에 닫히는 회귀가 되살아난다. `prefers-reduced-motion` 에서는 잔류 시간 0.
- [2026-07-31][design] **W1 목표 도달** = 결과 요약 카드의 목표 타일이 `success-surface` 면 + `success` 테두리 + 라벨 왼쪽 체크 글리프(접근명 "달성")로 250ms 전환. 🔴 **숫자에는 색을 넣지 않는다**(면과 글리프만). 대비 게이트 3쌍 신설(`text`·`text-secondary`·`text-muted` × `success-surface`, 16조합 통과). 발동은 **세션 중 미달성→달성 전이 1회**뿐 — 새로고침·복원에서는 정적 성공 상태만 남고 모션은 없다.
- [2026-07-31][design] **W3 첫 결과 등장** = 결과 그리드 자식이 80ms 스태거로 `translateY 8px + opacity`(지속 100ms, 지연 0/80/160, **총 260ms**). 🔴 **새로고침·복원·공유 링크·첫 방문 프리필에서는 돌지 않는다**(그건 금지된 페이지 로드 오케스트레이션이다). 프리필은 **건너뛰기이지 잠금이 아니다** — 그 뒤 사용자가 종목을 비우고 직접 프리셋을 고르면 그때 한 번 돈다. 연출이 끝나면 선언 자체를 내린다(결과 이미지 저장이 살아 있는 그리드를 그대로 찍으므로 연출과 겹치면 반투명 그림이 나온다).
- [2026-07-31][design] **W6 로딩** = 시뮬레이터 결과 자리는 스피너가 아니라 **결과 그리드 모양 스켈레톤**(`MainContentLoader variant='result'`: 요약 카드 → 차트 → 표). 설정 패널 자리는 형태를 미리 알 수 없어 `variant='plain'`(스피너) 유지. reduced-motion 에서 셔머는 **되찾지 않는다**(모양이 정적 단서 — 스피너/스켈레톤 경계는 `test/shared/reducedMotionCues.test.ts` 가 소유).

## 404 · 공용 푸터 · 다음 배당 D-Day (2026-07-31 — 리워크 P4 신규 기능 3종)
- [2026-07-31][product] 🔴 **`router/routes.tsx` 의 `*` 는 이제 404 화면이다**(구 `<Navigate to="/" replace />` 폐기). 잘못된 주소가 조용히 홈으로 가면 오타·죽은 링크·없어진 페이지가 전부 "메인이 정상적으로 떴다"로 보이고 주소창 기록마저 사라진다. 화면은 `pages/NotFound`(lazy, `TickerPageShell` 재사용) = 요청한 주소 그대로 표시 + 갈 곳 3개(시뮬레이터·내 포트폴리오·종목 둘러보기). ⚠ **`/ticker/:name` 의 콘텐츠 없는 티커는 여기로 오지 않는다** — 그 라우트는 매칭에 성공하고 `TickerDetailPage` 가 `/ticker/all` 로 보낸다(서버가 무치환 셸 200 을 주는 SEO 결정과 한 쌍, decisions §티커 SEO). 실측: `/ticker/NOPE` → 허브(불변) · `/ticker`·`/community/nope`·`/does-not-exist` → 404(전부 구 동작은 홈). **hue 는 배정하지 않는다** — 404 는 어느 섹션도 아니라 `usePageHue` 매핑에 넣으면 없는 섹션을 가르치게 된다(미배정 폴백 brand 그대로). 가드 `test/router/notFoundRoute.test.tsx`(렌더) + `test/community/routes.test.ts`(라우트 객체 — 이 파일은 예전엔 **반대 방향**을 잠그고 있었다).
- [2026-07-31][seo] **404 의 색인 제외는 런타임 `meta[name=robots]` 를 `noindex, follow` 로 바꾸는 것뿐이고, 서버는 여전히 200 을 준다**(`vercel.json` 의 `/(.*) → /index.html` catch-all). 즉 **soft 404 상태이며, 진짜 404 상태코드는 이 트랙 범위 밖의 서버 변경이 필요하다**(사용자 결정 대기). 완화 요인 둘: ①JS 를 실행하는 크롤러는 `noindex` 를 본다 ②JS 를 실행하지 않는 크롤러는 `index.html` 의 **정적 canonical `/`** 를 본다(`RootLayout` 이 런타임에 canonical 을 현재 경로로 바꾸지만 그건 JS 경로다). 사이트맵은 안전 — `vite.config.ts` 의 `ROUTES` 가 **명시 허용목록**이라 새 라우트가 자동으로 들어가지 않는다.
- [2026-07-31][design] **공용 `components/common/PageFooter` 신설 — 2026-07-28 의 "10단계로 기각" 을 여기서 해제한다**(기각 사유가 "소비처가 없거나 전 페이지 동시 변경이 필요하다"였고, 지금이 그 전 페이지 동시 변경이다). 흡수 대상 = 시뮬레이터 `LandingDisclaimer`(**폴더째 삭제**) · 포트폴리오/캘린더의 로컬 `FootNoteCard`+`FootNote`(styled 삭제). 모양의 정본은 **좌측 2px 레일**(캘린더·포트폴리오가 쓰던 것)이고 가운데 정렬 문단은 버렸다. 🔴 **페이지별 각주 문구는 `notes` 슬롯으로 원문 그대로** — 면책은 법적 성격이 있어 "비슷하니까" 합치지 않는다(그 대가로 포트폴리오에서 "투자 자문이 아니며"가 두 번 나온다 = 알려진 중복, 카피 결정 대기). 🔴 **법무 문서 링크(개인정보처리방침·이용약관)는 만들지 않았다** — 레포에 문서가 0건이고 없는 페이지로 가는 링크는 404 보다 나쁘다(사용자 결정 필요). 소비 4화면 = `/`·`/dividend/portfolio`·`/dividend/calendar`·`/ticker/all`(+404). `/ticker/:name` 은 자기 `Disclaimer` 유지(SEO 본문 순서 무변경).
- [2026-07-31][product] **`/dividend/portfolio` 히어로에 다음 배당 D-Day**(`PageHero` 의 `notice` 슬롯 = 그 슬롯의 첫 실소비처). 🔴 **새 데이터·새 계산 0** — 요약 타일 #7 과 **같은 순수 함수**(`selectNearestPayout`, `PortfolioPage.nextPayoutTile.ts`)가 지급을 고른다. 두 표면이 각자 정렬하면 한 화면은 "8월 28일", 다른 화면은 "D-3"(=8월 18일)을 말하게 된다. 렌더 조건 3중 부정: **보유 0 · 예상 '일자' 미상(`month-only`) · 하이드레이션 중** 이면 `dDay === null` → **줄 자체가 없다**("— 단독 금지"의 연장). 값은 **중립 토큰**(`color.text` + `surfaceSunken` 알약) — 남은 일수는 손익이 아니다. 카피는 예정형("지급 예정일까지")이고 오늘이면 문장 자체가 바뀐다(`D-DAY`). '오늘'은 컨테이너가 마운트 시 1회 고정(`now`)해 순수 계층으로 내려주고, D-Day 는 **로컬 자정끼리** 빼서 자정 경계에서 흔들리지 않는다.

## 법무 고지문 — 개인정보처리방침 · 이용약관 (2026-08-01 신설)
- [2026-08-01][product] 🔴 **`/privacy`·`/terms` 신설로 2026-07-31 의 "법무 문서 링크는 만들지 않았다"를 해제한다**(그 기각 사유가 "레포에 문서가 0건"이었다). 문서는 `pages/Legal/`(lazy, `TickerPageShell` 재사용) — 화면은 `components/LegalDocument` **한 벌**이고 내용은 `copy/privacyCopy.ts`·`copy/termsCopy.ts` 가 데이터로 소유한다(문장만 바뀌는 개정이 마크업 diff 와 섞이면 "무엇이 바뀌었나"가 안 읽히는데, 법무 문서는 그게 곧 고지 의무의 대상이다). 라우터는 **배럴이 아니라 각 페이지 폴더를 직접 lazy** 한다 — 배럴을 lazy 하면 방침만 열어도 약관까지 내려받는다. `usePageHue` 매핑에는 **넣지 않았다**(404 와 같은 근거 — 법무 문서는 어느 섹션도 아니다).
- [2026-08-01][seo] 🔴 **법무 문서에는 `noindex` 를 걸지 않는다 — 404 의 `useNoIndex` 를 복사해 오지 마라.** 구글 OAuth 동의 화면 심사가 개인정보처리방침 URL 을 직접 열기 때문이고, 색인을 막으면 그 검토가 **화면상 아무 일도 없어 보이는 채로** 막힌다. 같은 이유로 `vite.config.ts` 의 `ROUTES`(사이트맵 명시 허용목록)에 두 경로를 **일부러 등재**했다(priority 0.2 / changefreq yearly — 검색 유입 목적이 아니라 발견 가능성과 크롤 깊이 때문이다. 푸터 링크로만 도달하면 깊다). 가드 `test/router/legalRoutes.test.tsx` 가 robots 메타 불변까지 본다.
- [2026-08-01][copy] **개인정보 보호책임자는 성명·직책·이메일 3줄로 완결한다 — 전화번호 줄 자체를 두지 않는다**(빈 칸·하이픈·"없음"도 금지). 개인 프로젝트 운영자의 개인 번호를 공개 문서에 박는 대신 이메일 단일 창구를 택한 **사용자 확정 결정**이다. ⚠ 시행령의 "연락처"는 통상 전화번호를 포함해 해석되므로 **변호사 검토 1순위 항목**이고, 문서 본문에서 이 선택을 변명·주석하지 않는다(고지문에 들어갈 내용이 아니다). 가드가 그 절 안에 "전화"가 없음을 확인한다.
- [2026-08-01][copy] **법무 문서의 면책은 앱 안에 이미 있던 고지의 톤·주장을 승계하되 문장을 통일하지 않는다.** 승계원 = `PageFooter` 의 사이트 공통 고지(무료·비자문·비영리) · `portfolioCopy.footnote`(추정치·예상 지급일) · 티커 `disclaimer`(과거 성과 · 본인 책임). 규율은 "**주장하는 사실이 어긋나지 않게**"이지 "문장을 같게"가 아니다 — 화면 고지는 짧게, 약관은 조문으로. 어느 한쪽을 다른 쪽에 맞춰 고치지 마라.
- [2026-08-01][docs] **`public/llms.txt`·`llms-full.txt` 의 "서버·계정·수집되는 개인 데이터가 없다 / no backend, no account, no personal data collection"은 **사실이 아니었고** 이번에 고쳤다**(커뮤니티·클라우드 저장·공유 스냅샷·GA4 가 전부 서버·수집이다). 방금 쓴 방침과 정면으로 어긋나는 문장이라 그대로 둘 수 없었다. 새 문구는 "계산은 브라우저·기본 저장은 기기, 선택 기능을 쓸 때만 서버"이고 `/privacy`·`/terms` 를 가리킨다. ⚠ **`llms-full.txt` 의 "JSON export/import"는 아직 확인되지 않은 서술이다** — 코드에서 내보내기/가져오기 경로를 찾지 못했다(미수정, 보고만).

## 테마 축소 · 가계부 · 라우트 이전 (2026-08-01 야간)

- [2026-08-01][design] 🔴 **테마 선택을 "라이트/다크 토글 하나"로 축소했다 — 삭제가 아니라 UI 에서만 감추기다**(사용자가 화면을 직접 보고 내린 최신 판단: "설정을 고를 수 있는 옵션이 너무 많다"). 8프리셋의 hex·토큰·**16조합 대비 게이트(1469 케이스)는 전부 그대로 산다**. 노출을 막는 지점은 `shared/constants/palette/index.ts` 의 `VISIBLE_PALETTE_PRESET_IDS` **한 배열**이고, `toVisiblePalettePresetId` 는 **읽기 전용 폴백**이라 저장값(`snowball:palette`)을 절대 덮어쓰지 않는다(감추기를 되돌리면 `grape` 가 그대로 복원된다 — `test/persistence/paletteVisibilityRestore.test.tsx` 가 증명). ⚠ 이 결정은 user-profile 의 "여러 버전을 전환·비교하길 원한다"와 표면적으로 충돌하지만 **화면을 보고 내린 최신 판단이라 이쪽이 우선**한다. 🔴 **되돌리기 비용은 "한 줄"이 아니라 1 상수 + 6 지점**이다(스위처 소비처가 0이 됐으므로 배열만 되돌리면 화면엔 아무 일도 안 일어난다) — 체크리스트가 그 배열 주석에 있다.
- [2026-08-01][design] **가계부는 사용자 본인의 구글 시트를 DB 로 쓴다 — 우리 DB·IndexedDB 에 가계부 행을 저장하지 않는다.** 사용자가 수기 입력을 강하게 거부했고("금액을 유저가 셀프로 입력해야 한다면 그 기능은 후보에서 빼라") 그 관심사는 노동이 아니라 **데이터 소유권·락인**이었다(같은 자리에서 "우리 DB 에 복사 저장하지 않는다"를 못 박았다). 🔴 **스코프는 `drive.file` 하나뿐** — Sheets API v4 의 `create`/`batchUpdate`/`values.*` 가 **메서드별 공식 레퍼런스에 `drive.file` 을 직접 나열**한다(2026-08-01 확인). `spreadsheets*` 는 민감 스코프라 앱 검증 수 주가 걸리므로 **절대 요청하지 않는다**. 인증은 GIS 토큰 클라이언트(Supabase `provider_token` 은 갱신 미보장 + localStorage 저장 + 카카오·네이버 사용자 배제로 기각). 토큰은 **메모리에만**, 수명은 `expires_in` 을 읽는다(하드코딩 금지).
- [2026-08-01][design] **가계부 라우트 `/ledger`, hue `accentAlt`, ~~헤더 nav 에는 넣지 않는다~~ → 🔴 nav 제외는 같은 날 사용자 지시로 폐기(바로 아래 항목이 정본).** 라우트·hue 부분만 유효하다. `/dividend/*` 는 배당 도메인이라 가계부가 들어갈 자리가 아니다(진짜 배당 가계부 트랙 ⑬이 나중에 그 자리를 요구한다 — 그쪽을 "배당 기록"으로 개명 권고). hue 는 `/dividend/portfolio` 와 **같은 축**(자산과 현금흐름은 "내 실측 데이터" 한 묶음이고 사용자가 오가는 짝이다 — hue 는 라우트마다 유일할 필요가 없다). **nav 제외 근거 3가지**: ①구글 동의를 통과해야 도달하는 소수 모수 ②env 로 꺼진 배포가 존재해 항목이 나타났다 사라진다(nav 는 일관된 지도여야 한다) ③8번째(랜딩)가 예약돼 있다. 진입점은 `/dividend/portfolio` 카드 + 프로필 드롭다운. ~~**nav 승격 조건 = 연결 후 7일 재방문율 40% 가 GA 로 증명된 뒤.**~~
- [2026-08-01][design] 🔴 **위 "헤더 nav 에는 넣지 않는다"는 사용자 지시로 뒤집혔다 — 가계부는 nav 7번째 항목이다**(사용자 원문 "가계부도 메뉴를 만들어줘", GA 승격 조건 미충족 상태에서의 직접 결정). 자리 = **내 포트폴리오 바로 뒤**(accentAlt 를 공유하는 같은 축이라 붙인다) · 아이콘 `ReceiptText` · 라벨 '가계부'(프로필 드롭다운과 **같은 낱말**) · **라우트와 같은 `isGoogleSheetsEnabled` 플래그로 갈린다**(라우트가 없는데 메뉴만 남으면 404 죽은 링크). 구 기각 근거 ①②③ 중 ②(env 로 나타났다 사라진다)는 그대로 남아 있는 비용이다. **실측 대가(1024~1066px 대역만)**: 내비 트랙 711px 에 내용이 753px → 42px 초과로 "ETF 소개"가 스크롤 뒤로 숨고, 스크롤러 스크롤바 6px 때문에 헤더가 **65→71px**(≤80 상한·64~72 목표 대역 안이라 headerprobe 는 통과). 1067px↑ 는 65px·가려짐 0, ≤1023 두 줄은 105/111 그대로. 🔴 **8번째를 넣기 전에 묶음(드롭다운) 설계가 먼저다** — 지금 예산이 이미 음수다. 근거 components/PrimaryNav/PrimaryNav.tsx:57, router/routes.tsx:137.
- [2026-08-01][arch] 🔴 **쓰기 안전은 관례가 아니라 구조로 만들었다.** ①**여러 열을 걸치는 범위 생성기를 아예 만들지 않았다** — `a1.ts` 에 그 함수가 없으니 행 단위 덮어쓰기를 실수로 할 경로가 없고, 범위 파서가 두 열 범위에 `null` 을 돌려주는 성질로 위반을 **기계 검출**한다. ②쓰기 API 는 행 번호가 아니라 **`LedgerRowRef{snapshotId,rowNumber}`** 만 받고, 물리 삭제 **성공** 시 그 스냅샷을 폐기해 이후 쓰기가 `stale-snapshot` 으로 거부된다(**실패한 삭제는 폐기하지 않는다** — 아직 아무것도 안 밀렸다. 이 비대칭이 핵심). ③**`values.append` 를 쓰지 않는다** — 기본 `OVERWRITE` 는 표 아래 사용자 내용을 덮고 `INSERT_ROWS` 는 아래 행을 민다. 대신 `lastDataRow+1` 명시 범위 + **쓰기 직전 그 칸들의 공백 재확인**.
- [2026-08-01][arch] 🔴 **`/simulator` 를 추가했지만 `/` 는 아직 시뮬레이터다 — 이것이 설계가 지정한 "가장 안전한 중간 상태"(P2)다.** 두 경로가 같은 화면을 그리고, 내부 링크 **11곳**은 전부 `/simulator` 를 가리키며, middleware `matcher` 는 `['/', '/simulator']` 로 넓혀 **양쪽 공유 링크의 OG 가 다 산다**. 🔴 **P3 를 revert 하면 앱이 이 P2 상태로 안전하게 돌아가도록 커밋을 분리하라.** ⚠ **(2026-08-01 갱신) `/` 는 이제 랜딩이고 `RootRoute`·`resolveShareRedirectPath` 는 없다** — 아래 "공유 링크 리다이렉트 폐기" 항목이 정본.
- [2026-08-01][arch] 🔴 **`/` 의 공유 링크 → `/simulator` 리다이렉트를 폐기했다(`RootRoute` + `resolveShareRedirectPath` + `test/router/shareLinkRedirect.test.tsx` 삭제).** 사유: 사용자가 옛 `/?share=` 링크의 **실사용자 없음을 확인**했다(verbatim "share를 메인에서 안 해도 돼. simulator를 그냥 붙여. 어차피 사용하는 사람이 없었어"). **대가**: 옛 `/?share=…`·`/?s=…` 는 이제 랜딩에 착지하고 시나리오가 열리지 않는다(실측 확인). 미들웨어 `matcher: ['/', '/simulator']` 는 **유지** — `/simulator` 는 새 링크의 OG, `/` 는 랜딩 자신의 OG 때문이다(공유 파라미터가 없으면 그냥 next). **되살리는 법**: `router/routes.tsx` 의 `'/'` 라우트 주석에 사유·절차가 있고, 함수는 git 이력에 있다. `docs/simulator-route-migration-compat.md` §1 은 폐기 표기됨(§0·§2~§11 은 유효).
- [2026-08-01][arch] **공유 링크 실패는 "값"으로 다룬다 — 유효성으로 조기 차단하지 마라(리다이렉트가 사라진 뒤에도 유효).** `decodeSharedScenarioResult` 가 실패를 값으로 돌려주고 `shareLinkFailureAtom` → `ShareLinkFailureNotice` 가 안내를 띄우는 경로가 이 앱의 유일한 실패 통보다. 어디서든 깨진 페이로드를 "무시"하면 **무음 실패**가 된다. `/simulator?share=zz` 에서 안내가 뜨는 것을 실측으로 확인(uiprobe, 2026-08-01). ⚠ **랜딩의 통합 검색은 `s`·`share`·`sv` 를 쿼리 파라미터로 쓰면 안 된다** — 튕기지는 않지만 `middleware.ts` 가 `/` 를 매칭해 OG 치환 경로로 새고, 복원 소비자는 경로를 안 보고 `window.location.href` 를 직독한다. `q` 를 쓴다.

## 가계부 신선도 (2026-08-02 — B-2)

- [2026-08-02][design] 🔴 **가계부 화면은 폴링하지 않는다 — 자동 확인은 "창 포커스 복귀 + 마지막 읽기에서 5분" 단 하나다.** 인터벌 타이머를 만들지 마라(구글 시트 읽기는 429 가 실제로 나는 자원이고, 탭을 열어 둔 것만으로 할당량·배터리를 태운다). 5분은 주요 지수 탭 복귀 규약과 같은 값이다. 예외 조건 4개는 전부 "사용자 밑에서 화면을 바꾸지 않는다"에서 나온다: 폼·삭제 다이얼로그 열림 / 저장 실패 잔류 / 만료 / 429 대기. 근거 pages/Ledger/hooks/useLedgerFreshness.ts shouldAutoRefresh.
- [2026-08-02][design] **변경 감지는 "달라졌다"까지만 말한다 — 행 단위 diff 는 만들지 않는다.** 물리 삭제가 행 번호를 밀면 옛 행과 새 행의 대응이 원리적으로 불확실해서, "3행이 바뀌었습니다"는 그럴듯한 날조다. 비교 재료는 (건수, 행별 `seen` 값)뿐이고 순서는 정렬하지 않는다(시트에서 순서가 바뀐 것도 변경이다). 🔴 **자기가 건 재조회만 비교한다** — 저장·삭제 뒤 재조회까지 비교하면 사용자 자신의 수정을 "시트가 갱신되었습니다"라고 알린다.
- [2026-08-02][design] **한 사실은 한 자리에서만 말한다 — 히어로의 연결 요약(meta)을 없앴다.** 종전 `연결한 시트 {탭 제목} · {HH:MM}에 읽었습니다` 는 B-1 탭 줄("{제목} 탭을 보고 있습니다")과 제목을, B-2 목록 카드 헤더("09:30 기준")와 시각을 각각 중복했다. 최종 배치 = **탭 줄 = 어느 장부 / 목록 카드 헤더 = 언제 기준 + 새로고침 / 월 네비 = 어느 기간.** ⚠ 히어로 meta 를 시트(파일) 이름으로 바꾸는 대안은 **불가능**했다 — `connectSpreadsheet` 반환에 파일 제목이 없고 데이터 계층은 이번 배치에서 불가침이었다. 근거 pages/Ledger/LedgerPage/LedgerPage.view.tsx, copy/ledgerCopy.ts hero.

## 가계부 블렌딩 데이터 층 (2026-08-02 — B-3 L3a)

- [2026-08-02][arch] 🔴 **"날조 금지"의 이 레포 정본 처방은 이제 "판별 유니온으로 갈래를 나눈다"다 — 같은 달에 세 번째로 같은 형태가 나왔다**(B-4 `LedgerDividendBody`, B-3 `LedgerBlendBody`, 그전 `LedgerResult`). 규칙: **없는 값을 0/빈 문자열로 채우지 않는 것만으로는 부족하고, 값이 존재할 수 없는 상태에서는 그 필드가 타입에 아예 없어야 한다.** 블렌딩에서 합산 3숫자는 `kind:'ready'` 갈래에만 있고, 한쪽 실패(`partial`)·양쪽 실패(`unavailable`)·로딩에는 **필드 자체가 없다** — UI 가 반쪽 합계를 그리려면 갈래를 통째로 바꿔야 해서 실수로 샐 수 없다. 새 모델을 설계할 때 "이 필드가 의미 없는 상태가 있나?"를 먼저 물어라. 근거 pages/Ledger/types/ledgerTypes.ts `LedgerBlendBody`.
- [2026-08-02][arch] 🔴 **새 로컬 키를 더할 때 기존 키의 불변을 지키는 방법은 테스트가 아니라 구조다 — 새 저장 파일이 기존 저장 API 를 *import 하지 않게* 만들어라.** `pages/Ledger/utils/ledgerBlend.ts` 는 `loadSheetLinks`/`saveSheetLink`/`LEDGER_LINK_STORAGE_KEY` 중 무엇도 부르지 않고, 대조에 필요한 링크 목록을 **인자로만** 받는다(`resolveLedgerBlendConfig(config, links)`). 그 결과 `snowball:ledger:links` 를 다시 쓰는 코드 경로가 존재하지 않는다 — 왕복 바이트 동일 테스트는 그 사실의 **확인**이지 유일한 방어선이 아니다. 소스 가드가 이 import 금지를 잠근다. 같은 처방을 다음 새 키(`snowball:ledger:*`)에도 그대로 적용하라.
- [2026-08-02][design] 🔴 **블렌딩은 라우트가 아니라 `/ledger` 안의 "보기 방식"이다 — 켜면 단일 가계부 본문(요약·배당·목록·쓰기 액션 전부)이 통째로 대체되고, 월 커서는 그대로 이어받는다.** 새 라우트를 만들지 않은 근거 셋: ①이 화면은 이미 상태 기계(`LedgerConnectionState`)로 단계를 다루지 뒤로가기 스택으로 다루지 않는다 ②모드를 바꿨다고 보던 달이 튀면 두 가계부를 비교하러 온 사용자가 길을 잃는다 ③해시 라우팅 금지가 확정 결정이라 경로를 늘리면 lazy 언마운트·hue 깜빡임을 새로 떠안는다. **월 요약(주역 카드)은 두 모드가 같은 렌더 헬퍼를 공유한다** — 복제하면 `cardElevation('raised')` 정확히 1건이라는 위계 가드가 죽는다. 근거 pages/Ledger/LedgerPage/LedgerPage.view.tsx `renderSummaryCard`·`isBlending`.
- [2026-08-02][design] **블렌딩 뷰의 "이 가계부에서 열기"는 지금 연결된 *같은 스프레드시트의 탭*에만 준다**(`openableSources`). `drive.file` 은 주소 붙여넣기로 파일을 열 수 없어 다른 파일로 가려면 피커를 다시 거쳐야 하므로, 되지 않는 버튼을 그리는 대신 **왜 없는지를 문장으로** 말한다. 읽기 실패 문구도 `error.*`(저장 실패)를 재사용하지 않고 `blend.view.reason` 을 따로 뒀다 — 읽기만 하는 화면에서 "저장하지 못했습니다"는 사실과 다르다.
- [2026-08-02][product] **블렌딩 구성이 가리키는 두 링크 중 하나라도 사라지면 구성 *전체*가 무효다 — 남은 한쪽으로 이어 가지 않는다.** "우리 가계"라고 이름 붙인 화면이 조용히 한 사람 것이 되는 편이, 블렌딩이 꺼지는 것보다 나쁘다. 같은 이유로 **두 자리가 같은 링크인 구성은 저장·파싱 양쪽에서 거부**한다(모든 숫자가 두 배가 되는데 화면은 정상으로 보인다). 라벨 기본값은 중립 문구(`가계부 1/2`)이고 **시트 파일명·탭 제목을 기본값으로 쓰지 않는다** — 준PII 를 로컬에 저장하는 뒷문이 된다(D3-3).
- [2026-08-02][product] 🔴 **탭 전환을 막는 것은 "저장 실패 대기열(추가 실패)"과 "폼 열림" 둘뿐이다 — 행 실패(수정·삭제 실패 잔류)는 막지 않는다.** 리뷰가 지적한 비대칭(자동 새로고침은 행 실패에도 막는데 탭 전환은 안 막는다)을 검토한 결과 **고치지 않기로 했다**. 가르는 축은 "누가 시작했나"와 "유일한 사본인가" 둘이다: ①자동 재조회는 사용자 밑에서 일어나므로 실패 표면을 지우면 안 되지만 탭 전환은 사용자가 의도한 이동이다 ②대기열은 **그 데이터가 존재하는 유일한 사본**이라 잘못된 탭에 들어가면 되돌릴 수 없는 반면, 행 실패는 시트에 이미 있는 행을 가리키고 탭이 바뀌면 재시도 자체가 조용한 no-op 이 된다(`entryById` 조회 실패로 early return — 잘못된 쓰기는 구조적으로 불가). 행 실패로도 막으면 **영구 실패(권한 없음 등) 하나가 탭 전환을 영원히 잠그는 함정**이 생긴다. 근거 pages/Ledger/components/LedgerTabPicker/LedgerTabPicker.utils.ts, pages/Ledger/hooks/useLedgerWrite.ts runUpdate/runRemove.
- [2026-08-02][design] **블렌딩 진입 자체는 막지 않는다 — 막는 것은 "이 가계부에서 열기"(=탭 전환) 하나다.** 대기열이 남아 있어도 두 가계부를 **보는 것**은 안전하고, 읽기 전용 화면을 통째로 잠그면 사용자가 문제를 이해할 화면조차 잃는다. 대신 저장 실패 목록이 이 화면에는 서지 않으므로 차단 사유에 **나가는 길 한 문장**(`blend.view.openBlockedHint`)을 덧붙인다 — 사유가 가리키는 "아래 목록"이 없는 화면에서 막기만 하면 사용자가 갇힌다.

## 법무 문서 · 가계부 로그인 (2026-08-01 — 브랜치 feat/legal-and-ledger-enable)
- [2026-08-01][product] 🔴 **가계부의 두 층은 분리 유지 — 합치지 마라.** **앱 신원**(Supabase 세션, 구글·카카오·네이버 무관) ↔ **구글 리소스 권한**(GIS 액세스 토큰, `drive.file`)은 서로 다른 층이고 "중첩 문제"는 존재하지 않는다. `shared/lib/googleSheets/auth.ts` 가 Supabase `provider_token` 을 **의도적으로 안 쓴다**(①Supabase 가 provider token 을 갱신하지 않는다고 공식 명시 ②Supabase 세션은 localStorage 저장이라 "토큰은 메모리에만"을 구조적으로 깬다 ③카카오·네이버 사용자를 전부 배제한다). 네이버로 로그인한 사용자는 앱 로그인을 유지한 채 구글 동의만 따로 받는다. 화면은 **비로그인 → 앱 로그인 유도(인라인 3버튼) / 로그인됨(제공자 무관) → 곧바로 시트 연결**이고, 구글로 로그인한 사용자에게도 **"시트 접근은 별개 권한"을 한 줄로 설명**한다(가장 흔한 혼란 지점). 근거 pages/Ledger/hooks/useLedgerAppAuth.ts:39, pages/Ledger/components/LedgerSignInPanel/
- [2026-08-01][product] **가계부 로그인 게이트는 모달이 아니라 인라인 패널이다** — 페이지 전체가 게이트라 뒤에 볼 것이 없는데 `openLoginPrompt` 모달을 자동으로 띄우면 배경이 빈 화면이다. `CommunityMyPostsPage` 딥링크 게이트와 같은 처방. `useCommunityAuth` 는 **파일 이동 없이 재사용**한다(셸이 이미 `CommunityAuthProvider` 를 마운트하고, 세션 하이드레이션·LoginModal·OAuth 실패 안내·GA 귀속이 전부 거기 모여 있어 직접 `signInWithOAuth` 를 부르면 **두 번째 로그인 경로**가 생긴다).
- [2026-08-01][seo] 🔴 **`/privacy`·`/terms` 에 `noindex` 를 걸지 않는다** — 구글 OAuth 심사가 이 방침 URL 에 실제로 접근해야 한다. 라우트는 **배럴이 아니라 각 페이지 폴더를 직접 lazy import**(배럴이면 두 문서가 한 청크). 푸터 링크는 **`Link` 가 아니라 `<a href>`** 여야 한다 — `MainPage` 를 Router 없이 마운트하는 테스트가 수십 개다. 사이트맵은 `priority 0.2`·`changefreq yearly`. AI 표면 3종(`llms.txt`·`llms-full.txt`·`ai-overview.json`)의 **"서버·계정·개인 데이터가 없다"를 바로잡은 사실 정정은 되돌리지 않는다**(앱에 Supabase 계정·커뮤니티·클라우드 저장이 실제로 있다) — 드리프트는 이제 `test/shared/legalSurfaceConsistency.test.ts` 가 잠근다.
- [2026-08-01][copy] 🔴 **가계부의 "로그인 필요"는 고지 5표면에 동시에 적는다** — 약관 **제2조**(정의에 가계부)·**제4조**(로그인 필요 목록 + 구글 권한은 별개라는 줄)·**제11조**(본인 시트 기록 면책), 방침 **개요절**(로그인 조건 줄 → 시트 저장 줄 순서)·**1.의 마**(첫 문장이 두 층을 다 말한다), 그리고 AI 표면 3종. 근거: 비로그인은 시트 연결 화면조차 못 보므로(`LedgerPage.view.tsx` `isGated`) **가계부 이용 = 계정 생성 = 개인정보 수집**인데, 종전 문안은 개요절에서 "서버 저장은 로그인 기능만"과 "가계부는 본인 시트" 를 나란히 두어 **가계부를 서버 저장이 없는 쪽으로 읽히게** 했다. 🔴 두 층(앱 로그인 ↔ 구글 `drive.file` 권한)은 문장에서도 분리한다 — 합치면 "구글로 로그인하면 시트가 자동 연결된다"로 오독된다.
- [2026-08-01][a11y] 🔴 **가로 스크롤 표의 처방을 하나로 통일했다 — `tabindex="0"` + `role="region"` + 접근명.** 이름은 보이는 `<caption>`/제목이 있으면 **`aria-labelledby` 로 그것을 가리키고**(같은 문자열을 `aria-label` 로 또 적으면 스크린리더가 제목을 두 번 읽는다), 없으면 `aria-label`(캘린더 지급 월 표 — 제목 역할은 바깥 `summary` 가 한다). `group` 을 기각한 이유: ①랜드마크가 아니라 랜드마크 탐색으로 도달 못 하는데 이 상자는 "찾아가서 밀어야" 내용이 보이는 자리이고 ②법무 문서는 이미 절마다 `<section aria-labelledby>` = 랜드마크라 표만 비랜드마크로 두면 한 문서에 규칙이 두 벌이 된다. 실측(Chrome 150, `/privacy` @360): 표 6개 전부 `role=region` + 이름 출처 `relatedElement:aria-labelledby`. 새 스크롤 표를 만들면 이 세 벌을 함께 준다. 근거 pages/Legal/components/LegalDocument/LegalDocument.tsx:89, pages/DividendCalendar/components/ScheduleLegendTable/ScheduleLegendTable.tsx:47
- [2026-08-01][product] 🔴 **개인정보처리방침은 `[확인 필요]` 가 남아 있는 한 배포하지 않는다**(사용자 합의). 현재 **12건**(방침) / 2건(약관) — 가계부 개정으로 10→12 로 늘었고 가드 `test/router/legalRoutes.test.tsx:118-126` 을 동반 갱신했다. 늘어난 2건 = ①구글(가계부 연동) 데이터센터 소재 국가 ②구글 계정에서 접근 권한을 회수하는 화면 주소. 개정 문장은 전부 `shared/lib/googleSheets/` 구현을 읽고 쓴 것이며 **"가계부를 이용하시는 경우" 조건형**이라 env 로 꺼진 배포에서도 허위가 되지 않는다.
- [2026-08-02][product] 🔴 **가계부 블렌딩(두 가계부 합쳐보기)이 만든 새 사실 "타인이 공유한 시트를 읽을 수 있다"를 법무·AI 표면에 반영 완료** — 근거는 `shared/lib/googleSheets/picker.ts:4-6`(피커로 고른 파일은 소유 여부와 무관하게 접근권이 생긴다, `drive.file` + Picker 의 설계). 방침(개요절·1.의 마·9.), 약관(제2조 정의에 "블렌딩"·"배당 겹쳐 보기" 신설, 제4조·제10조·제11조), `llms.txt`/`llms-full.txt`/`ai-overview.json` 세 표면 모두에 ①블렌딩 시트가 공유받은 것일 수 있고 연결은 이용자 행위·책임이라는 사실 ②배당 겹쳐 보기는 포트폴리오 실보유 기준 **추정치**이며 시트에 안 쓰고 가계부 합계에도 안 더한다는 사실을 추가했다. `[확인 필요]` 개수는 불변(방침 12·약관 2 — 전부 이미 확정된 사실만 서술). `test/shared/legalSurfaceConsistency.test.ts` 의 `LEDGER_SURFACES.facts` 에 표면당 4개씩 추가하고 3표면×2뮤턴트로 감도 확인. 아직 못 한 것: **피커가 "공유 문서함"(공유받은 파일)을 실제로 목록에 보여주는지는 미실측**(`docs/ledger-advanced-spec-2026-08-02.md` §8) — 표면 문구는 "그럴 수 있다"는 메커니즘 사실만 쓰고 "된다/안 된다" 단정은 하지 않았다(날조 금지). 스파이크 결과가 나오면(성공/실패 무관) 문구를 다시 볼 필요는 없다 — 이미 조건부("공유받으신 경우")로 썼기 때문.

## 랜딩 페이지 (트랙 ⑦ / P3, 2026-08-01 — 화면·부품만. `/` 승격은 별도 트랙)
- [2026-08-01][arch] 🔴 **프리셋 13종의 집이 `shared/constants/portfolioPresets/` 로 옮겨졌다** (구 `pages/Main/.../PortfolioPresetBoard/PortfolioPresetBoard.constants.ts`·`.utils.ts`). 이유는 하나 — 랜딩이 같은 데이터를 쓰는 순간 **페이지가 페이지를 import** 하는 구조 규칙 위반이 된다. 화면 변화 0이고 `PortfolioPresetBoard` 는 새 폴더에서 읽는다. **최상위 배럴(`shared/constants/index.ts`)에는 연결하지 않는다** — `PRESET_ICON_BY_ID` 가 lucide 13개를 끌고 오므로 배럴에 얹으면 전 페이지가 그 비용을 진다(`tickers`·`community`·`routes` 와 같은 격리). 구 배럴(`MainRightPanel/components`)은 **호출부 파급 최소화를 위해 re-export 만 남겼다** — 새 소비처는 shared 폴더를 직접 쓴다.
- [2026-08-01][design] 🔴 **랜딩이 렌더해도 되는 프리셋 필드는 `title`·`hook`·`allocations` 뿐이다.** `expectedMonthlyDividend`("목표 월배당 약 40~50만원")·`monthlyInvestment`·`targetInvestment`·`investmentPeriod` 는 **엔진 계산이 아니라 손으로 적은 큐레이션 문구**라, 로그인 없이 크롤러가 읽는 지면에 쓰면 근거 없는 수익 약속이 된다. `coreType` 도 금지 — `allocations` 의 사본이라 어긋날 때 어느 쪽이 맞는지 알 수 없다. 비중 막대는 **장식**(`aria-hidden`, `var(--sb-chart-series-N)`)이고 정보는 그 아래 비중 텍스트가 전부 말한다(막대 위 텍스트 금지 — 시리즈 색은 16조합에서 명암이 갈린다). 가드 `test/landing/landingDataDiscipline.test.ts`. ⚠ **시뮬레이터 프리셋 카드의 `expectedMonthlyDividend` 노출은 여전히 살아 있다** — 별건이고 사용자 결정이 필요하다.
- [2026-08-01][arch] **랜딩 종목 검색은 `shared/constants/tickerPages` 의 경량 인덱스(`{symbol, slug}` 11종, 의존성 0)만 본다.** 원본 `shared/constants/tickers` 는 한국어 서사+FAQ 로 서버 번들 실측 416KB 라 첫인상 지면이 질 무게가 아니다. 한글명은 **복제하지 않고** `PRESET_TICKER_KOREAN_NAME_BY_TICKER` 에서 조인한다(단일 출처). 복제의 대가인 드리프트는 `test/landing/tickerPageIndexParity.test.ts` 가 **양방향 1:1 + 한글명 결손 0 + 폴백 3종 생존 + import 0건**으로 잠근다 — 12번째 티커 페이지를 추가하며 배열을 빼먹으면 그때 빨개진다.
- [2026-08-01][design] 🔴 **랜딩 검색의 쿼리 파라미터는 `q` 다.** `s`·`share`·`sv` 는 공유 링크 복원 전용 예약어다(구 사유였던 `RootRoute` 리다이렉트는 2026-08-01 폐기됐지만 예약은 유효 — `middleware.ts` 가 `/` 를 매칭해 OG 치환 경로로 샌다). URL 반영은 **디바운스 300ms + `replace`**(히스토리를 쌓지 않는다)이고 **결과는 즉시** 그린다 — 11종을 훑는 동기 함수라 늦출 이유가 없다(디바운스는 주소창의 사정이지 화면의 사정이 아니다). 결과 패널은 **흐름 안 목록**이고 팝업이 아니다(`role="dialog"` 0개 · combobox 패턴 금지 — 지키지 않을 키보드 계약을 선언하는 것이 된다). 무결과 폴백 3종의 제목은 **"소개 글이 준비된 종목"** 이다("추천 종목"이라고 쓰면 투자 권유가 된다).
- [2026-08-01][design] **히어로 CTA 순서는 카피 상수 배열이 소유한다**(`LANDING_HERO_CTAS`). 현재 값은 스펙 D1 **A안**(시뮬레이터 primary)이고 이는 `docs/design-refresh-plan.md:357`(RP 5-2)을 뒤집는 **사용자 승인 대기 제안**이다 — B안 확정 시 **배열 순서와 `variant` 두 값만 뒤집으면 되고 마크업 diff 는 0**이다. 프로브 앵커 `data-landing-cta` 는 배열의 `id` 에서 파생되므로 순서가 뒤집혀도 같은 요소를 가리킨다.
- [2026-08-01][design] 🔴 **랜딩의 틴트 면은 정확히 2개다** — 히어로 `gradient-hero` + S7 시작 준비 카드 `gradient-hero-soft`. 나머지 색은 전부 **면이 아니라 글리프**(섹션 배지 36px · 순서 배지 24px · 12칸 리듬 셀 26px)여야 한다. 세 번째 면을 넣고 싶어지면 대신 배지·1px 테두리·색 아이콘을 써라. **주역 `raised` 카드는 0개** — 이 화면의 주역은 히어로다. ⚠ **정정(2026-08-01 실측)**: 이 항목은 원래 "프리셋 비중 막대 8px 도 하한(180px)에 안 걸린다"고 적었는데 **거짓이었다.** tintscan 의 면 판정은 `폭 ≥180 && 높이 ≥8` 이고 막대 높이는 정확히 8px 라 **폭만 넘으면 걸린다.** 같은 날 격자가 3열→2열이 되며 카드가 333→510px 로 넓어져 `SCHD 40%` 조각이 117→**187px** 가 됐고 `/ @1280` 이 3면으로 실패했다. 🔴 **처방 확정(2026-08-01, 사용자 취향 우선 판정): 막대와 카테고리 색을 그대로 두고 `AllocationBar` **컨테이너**에 `max-width: 176px` 을 건다**(`ALLOCATION_BAR_MAX_WIDTH`, PresetBrowser.styled.ts). 설계 스펙의 C-1(막대를 8×8 색 견본 목록으로 교체)은 **기각** — 이 페이지에서 색 면적을 가장 크게 줄이는 안이라 `user-profile.md:3`("절제된 단색은 허접") 과 정면 충돌한다. **캡을 조각이 아니라 컨테이너에 걸면 기하로 airtight 하다**(조각 ⊆ 컨테이너 < 180px → 어떤 비중·폭·열 수에서도 면이 될 수 없다). 실측 after: 최대 조각 187→**70px**, `/ @1280` **2면(통과)**. 176 은 3열 시절 색 비율(117/333≈35%)을 2열 카드(176/510≈35%)에서 재현한 값이다. ⚠ `AllocationText`(막대 아래 중복 텍스트)는 **유지** — 중복 정리는 별건이고 게이트 실패가 아니다.
- [2026-08-01][seo] 🔴 **`index.html` 은 이제 랜딩의 문서다 — 문구 정본은 `pages/Landing/copy/landingCopy.ts`, `index.html` 은 그 사본이다**(반대 방향 금지). 확정된 배정: `WebSite`·`FAQPage` 의 `url` = `/`, **`WebApplication` 의 `url` = `/simulator`**(앱이 사는 주소가 바뀌었다), `.app-shell-fallback` 본문 = 랜딩 S1·S3·S4·S5·S7 + 승인된 FAQ 2문항 + 내부 링크 4개(JS 미실행 크롤러에게는 그 목록이 사이트를 도는 유일한 링크다). **`FAQPage` 에는 `landingCopy.faq` 중 `needsApproval: false` 인 문항만 싣는다** — JSON-LD 는 검색 결과에 그대로 인용되는 공개 약속이고, 승인 대기 6건은 화면에만 둔다. 마크업한 문항은 **정적 셸에도 보이게** 둔다(보이지 않는 콘텐츠 마크업 = 구글 정책 위반). 가드 `test/seo/landingFaqStructuredData.test.ts`(문자 단위 일치 + 대기 문항 부재 + 가시성), `test/seo/machineReadableSurfaces.test.ts`(3표면 `/simulator` 인지 · 사이트맵 정본 1.0/0.9 · 티커 CTA 한 쌍 · 비유 0).
- [2026-08-01][design] 🔴 **히어로 검색·"이어서 계산하기"는 `PageHero` **밖**, 히어로 바로 아래 형제다**(2026-08-01 P3 리뷰 교정). 화면상 순서(①제목 ②CTA ③리드 ④검색 ⑤이어서)는 그대로이고 바뀐 것은 **소속**뿐이다. `meta` 슬롯에 넣으면 그 슬롯이 항상 갖는 hue 밑줄·`justify-self: start`·데이터 서체가 따라오는데 **자식에서 취소되지 않는다**(밑줄은 상속 속성이 아니고, 폭은 내용에 맞춰 줄어 검색 폼의 520px 이 무효가 된다). 히어로에 랜딩 전용 prop 을 뚫는 안은 기각 — **앱의 유일 히어로**가 그 prop 을 영원히 진다. 접힘 예산 불변(390×664 시뮬레이터 CTA 하단 **257px**, before=after). 근거 pages/Landing/LandingPage/LandingPage.view.tsx, LandingPage.styled.ts HeroBlock/HeroExtras.
- [2026-08-01][design] **랜딩 프리셋 격자는 ≥`tabletSm` 에서 2열이다**(구 3열). 묶음당 초기 노출 2장 × 3열이면 네 묶음 전부 오른쪽 한 칸이 비고 '특화'는 숨은 카드가 0이라 그 빈 칸이 영구였다. **초기 노출 2장(`PORTFOLIO_PRESET_VISIBLE_PER_GROUP`)은 시뮬레이터 보드와 공유하는 확정 동작이라 그대로 두고 열 수만 내렸다.** `auto-fit` 은 펼칠 때 카드 폭이 510→333px 로 점프해 기각. 근거 pages/Landing/components/PresetBrowser/PresetBrowser.styled.ts.
- [2026-08-01][seo] 🔴 **`/simulator` 는 자기 문서 메타를 스스로 선언한다**(`SIMULATOR_COPY.meta` + `useDocumentMeta`). `/` 를 랜딩이 가져간 뒤 `index.html` 의 정적 메타는 **랜딩의 것**이라, 선언이 없으면 두 라우트의 제목이 완전히 같아지고(주력 키워드 "배당 재투자 시뮬레이터" 소실) GA4 `page_title` 까지 랜딩 제목으로 기록된다. 문구는 `/` 가 시뮬레이터이던 시절 `index.html` 에서 **회수**했고 "(스노우볼)" 삽입구만 뺐다(비유 전면 금지). 가드 `test/seo/routeDocumentTitles.test.tsx`. 근거 shared/constants/simulator/copy.ts, pages/Main/Main.tsx:15.
- [2026-08-01][design] **랜딩에는 스크롤 진입 애니메이션이 0건이다**(확정 금지의 재확인). 허용 모션은 호버·누름·`<details>` 펼침뿐이고, S6 "더 보기" 디스클로저는 **높이 전이 없이 즉시 표시**한다(문서 길이를 바꾸는 펼침에 애니메이션을 걸면 스크롤 위치가 흔들린다). FAQ 는 네이티브 `<details>/<summary>` 라 키보드·`aria-expanded` 를 브라우저가 준다 — 커스텀 아코디언을 만들지 마라.
- [2026-08-01][seo] 🔴 **`FAQPage` 는 사이트 그래프에서 분리한 단독 script(`#faq-structured-data`)이고, `/` 가 아닌 라우트에서는 DOM 에서 통째로 떼어 낸다.** `index.html` 은 SPA 전 라우트가 공유하는 셸이라 그냥 두면 `/simulator`·`/community/board`·`/ticker/*` 의 렌더 결과에도 남는데, `WebSite`·`WebApplication` 과 달리 `FAQPage` 는 **페이지 수준 타입**이라 «보이지 않는 콘텐츠 마크업» 판정 대상이 된다(그 판정 한 번이 FAQ 리치 결과를 사이트 전체에서 무효화할 수 있다). 처방은 **한 지점**뿐이다: `RootLayout` 이 `syncFaqStructuredData(location.pathname)` 를 부른다 — 라우트별 분기를 다른 곳에 심지 마라. 구글 판정이 관대한 것으로 밝혀지면 **그 한 줄만 지우면 원복**이다. 노드는 버리지 않고 보관했다 되붙인다(본문 정본은 `index.html` 하나 — 코드에서 재생성하면 FAQ 카피가 두 벌이 된다) ⚠ JS 미실행 크롤러는 이 제거를 보지 않지만 그쪽엔 셸 본문의 FAQ 가 함께 내려가 «보이지 않는 콘텐츠»가 아니다. 가드 `test/seo/faqStructuredDataScope.test.tsx`(`/` 존재 · `/simulator`·`/privacy`·404 부재 · 복귀 재부착) + `landingFaqStructuredData.test.ts`(사이트 `@graph` 에 FAQPage 부재).
- [2026-08-01][product] 🔴 **랜딩 히어로 1순위 CTA 는 "시뮬레이터"다 — `docs/design-refresh-plan.md:357`(RP 5-2)의 "내 월배당 확인하기 → 포트폴리오 1순위"를 사용자 승인으로 뒤집었다.** RP 5-2 는 이미 보유 종목이 있는 사람을 전제했는데, 랜딩의 대상은 **SNS 로 처음 들어와 보유 종목이 0인 사람**이라 포트폴리오로 보내면 빈 화면을 만난다. 되돌리려면 `LANDING_HERO_CTAS` 의 **순서와 `variant` 두 값만** 바꾸면 된다(마크업 diff 0). ⚠ 확정 결정을 뒤집은 건이니 다시 뒤집을 때도 사용자 승인이 필요하다.
- [2026-08-01][product] 🔴 **프리셋 카드는 적용 전에 "받게 될 금액"을 적지 않는다** — `buildPresetMetrics` 에서 `expectedMonthlyDividend`("약 40~50만원")를 뺐다(`portfolioPresets.utils.ts`). 그 값은 엔진 계산 결과가 아니라 **손으로 적은 큐레이션 문구**라, 적용 전 카드에 얹으면 근거 없는 수익 약속으로 읽힌다. 랜딩에서 막은 규칙을 **시뮬레이터 13장에도 동일 적용**(사용자 결정). 남은 `investmentPeriod` 는 약속이 아니라 그 구성이 전제하는 조건이라 적용 전에도 참이다. 실제 숫자는 적용 후 결과 카드가 말한다. 가드: `test/main/portfolioPresetGroups.test.ts` 가 라벨 집합과 "값에 expectedMonthlyDividend 가 없음"을 함께 잠근다.
- [2026-08-01][design] 🔴 **랜딩 시각 언어 = 등급 4단이고 등급이 만드는 차이는 정확히 셋뿐이다**(정본 `docs/landing-visual-language-spec.md`). **A 무대**(S1 히어로) · **B 챕터**(S4 복리 · S6 프리셋) · **C 보조**(S3·S5·S7) · **D 참조**(S8 FAQ · S2 지수). 차이 = ①제목 크기(A 만 다르다) ②**h2 아래 2px `color.identity` 룰의 유무(A·B 만 갖는다)** ③여백 그룹 안에서의 위치. **배지·색·본문 크기는 전 섹션 동일하다 — 등급을 늘리거나 새 장치를 발명하지 마라.** `LandingSection` 의 `emphasis` 는 **필수 prop**(기본값 금지 — 새 섹션이 등급을 안 정하면 `tsc` 가 막는다). 🔴 **`bolder.md` 의 "quiet everything around it" 을 *채도 제거*로 집행하지 않는다** — `user-profile.md:3`("절제된 단색은 허접")과 정면 충돌하고 직전 배치에서 같은 방향의 처방이 기각됐다. 여기서 "조용히"는 **구조를 주지 않는 것**이다. 여백 리듬 = 그룹 경계 `clamp(48,6vw,88)` ↔ 그룹 안 `clamp(24,3vw,40)`(실측 75.9 ↔ 37.95 = **2.0배**, before 1.0). 그룹 편성 G1 무대 / G2 배우기(S3·S4·S5) / G3 고르기(S6·S7) / G4 참조·마무리(S8·S2·ClosingCta). 가드 `test/landing/landingVisualLanguage.test.ts`(chapter = compound·presets 둘뿐), `test/landing/landingHierarchy.test.tsx`.
- [2026-08-01][design] **랜딩은 카드를 두 곳에서 버렸다 — S3 개념(3장 → 구분선 격자)과 S8 FAQ(상자 8개 → 구분선 목록).** `main` 안 면 **28 → 17**. `design-taste-frontend` 의 "긴 목록은 더 긴 목록이 아니라 **다른 컴포넌트**로"의 직접 적용이다. ⚠ **카드를 지울 땐 카드의 바깥쪽 가로 패딩도 같이 지워라** — S3 이 24px 패딩을 남겨 랜딩에서 유일하게 왼쪽 마진 선을 깼다(첫 열 배지 left 137 vs 섹션 루트 113). FAQ 는 네이티브 `<details>/<summary>` 유지(커스텀 아코디언 금지)이고 마커 `+`/`−` 는 `::after` 의사요소 + `color.identity`, `min-height: 44px`.
- [2026-08-01][design] **랜딩 히어로는 `PageHero` 를 편집하지 않고 랜딩 로컬 자손 선택자로 키운다**(1280 h1 30→**44px**, `clamp(3xl, 0.6rem+3.2vw, 6xl)`, ≥`mobileWide` 에서만). **390 은 21.42px 로 불변** — 접힘 예산(시뮬레이터 CTA 하단 **258px**)이 확정이라 좁은 폭에서는 키울 수 없다. 🔴 **이 override 는 `PageHero` 의 DOM 구조에 결합돼 있고 깨지면 무음이다**(슬롯이 하나만 앞에 끼면 30px·우측 CTA 로 조용히 복귀 · 잉크 보정 `-0.1` 은 `shared/styles/heroTitleRow.ts` `INK_ABOVE_LINE_BOX.display` 의 손 복제). 가드 `test/landing/landingHeroOverrideCoupling.test.tsx` 가 **소스의 선택자 목록을 뽑아 실제 DOM 에 실행**해 양방향으로 결박한다(뮤턴트 8종 KILL). **영구 해법은 `PageHero` 가 크기 변형을 소유하는 것 — 사용자 판단 대기(스펙 §G-1). 그 트랙이 들어오면 이 테스트 파일은 통째로 삭제하는 것이 맞다.**
- [2026-08-01][design] **`/` 는 마무리 CTA(`pages/Landing/components/ClosingCta`)로 닫는다** — 종전엔 마지막 인터랙티브 요소가 FAQ 8번째 `summary` 였다. 상단 2px `color.identity` 룰(히어로 4px hue 리본과 수미상관) + 확정 카피 `'지금까지 본 내용을 직접 계산해 보실 수 있습니다.'` + 버튼 라벨은 **`LANDING_HERO_CTAS` 의 `simulator` 에서 읽는다**(한 의도에 한 라벨 — 문구가 갈라질 수 없다). 앵커는 `data-landing-closing-cta`(**`data-landing-cta` 를 붙이지 마라** — 그 속성이 정확히 2개임을 가드가 단정한다) · 헤딩 없음(h2 순서 계약 보호) · 계측 `cta_name: 'landing_closing_simulator'`. 🔴 **`gradient-cta` 채움 위에 `filter: brightness(1.05)` 호버를 걸지 마라** — 아래 pitfalls §M 참조. 호버 연출은 `Button` 이 소유한다. 가드 `test/landing/landingClosingCta.test.tsx`(카피 **정확일치 리터럴**).
- [2026-08-01][design] **S2 주요 지수는 히어로 바로 뒤가 아니라 FAQ 뒤 참조 구역이다**(`FAQ → 지수 → 마무리 CTA`). 배당을 처음 접하는 사람에게 S&P 500 수치는 서사의 두 번째 자리가 아니다 — `index.html` 의 크롤러 셸 본문도 애초에 S2 를 담지 않아 그 판단이 이미 한 번 내려져 있었다. ⚠ 대안 `FAQ → 마무리 CTA → 지수`(액션 뒤 시세가 잔향)는 **JSX 한 줄 이동**이라 A/B 가 싸다 — 사용자 판단 대기.
- [2026-08-01][product] **FAQ 8문항 중 6문항은 화면에만 두고 JSON-LD 색인에서 뺀 상태를 유지한다**(사용자 결정). 색인은 공개 약속이라 무료·비영리 / 가입 불요 범위 / 저장 위치 / "계산은 브라우저에서" / 시세 갱신 주기 / 세율 15.4% 여섯 문항은 **개인정보처리방침·이용약관과 문장 단위로 대조한 뒤에만** 넣는다. 지금 넣으면 법무 문서와 어긋날 위험이 있다.
- [2026-08-01][product] 🔴 **제품명을 "Hungry Hippo" 로 리브랜딩할 예정이지만 지금은 코드를 바꾸지 않는다**(사용자 결정). 개발할 것이 많아 뒤로 미룬다. ⚠ **누구도 먼저 착수하지 마라** — 리브랜딩은 워드마크·헤더·`index.html` 메타·`public/llms.txt`·`llms-full.txt`·`ai-overview.json`·사이트맵·OG 카드(`server/handlers/Og/`)·법무 문서 본문·카피 전반·`VITE_SITE_URL` 까지 **한 트랙으로 묶어야** 하는 작업이다. 흩어서 하면 이름이 두 개인 상태가 오래 남는다. ⚠ Google Cloud Console 의 **OAuth 동의 화면 앱 이름은 이미 "Hungry Hippo"** 다 — 코드의 "Snowball Income" 과 다르다는 것을 알고 있는 상태(의도된 과도기).
- [2026-08-01][infra] 🔴 **구글 OAuth 동의 화면을 프로덕션으로 게시하면 동의 화면이 500 으로 죽는다 — 개인정보처리방침·이용약관 URL 과 승인된 도메인을 실제로 검사하기 때문이다.** 테스트 모드는 이 검사를 하지 않는다. 우리 법무 문서는 `[확인 필요]` 12건 때문에 **아직 배포되지 않았다** → 프로덕션 게시는 그 문서가 확정·배포된 뒤에만 가능하다. 그때까지는 **테스트 모드 + 테스트 사용자 등록**으로 개발한다(최대 100명, 즉시 적용).

## 주요 지수 스트립의 자리 (2026-08-02)
- [2026-08-02][design] 🔴 **지수 스트립의 최종 자리는 「시세가 실제로 쓰이는 세 화면(시뮬레이터·내 포트폴리오·배당 캘린더)의 본문 맨 위」다.** 같은 날 랜딩 참조 구역 → 랜딩 히어로 아래 → **앱 헤더** → 지금 자리로 네 번 옮겼고, 헤더 안은 **구현까지 마친 뒤 되돌렸다**(작업트리 mtime 02:04 배선 → 02:06 철회). 기각 사유 둘: ①헤더는 전 라우트 상시 표시라 시세가 무의미한 화면(커뮤니티·티커 소개·법무)까지 따라다닌다 ②**물리적으로 자리가 없다** — 1280 실측 브랜드↔컨트롤 트랙 902px 중 라우트 메뉴가 753px 을 쓰고(≤1024 는 이미 넘쳐 스크롤) 6칸 티커는 1,100px 대가 필요하다. 전폭 한 줄을 새로 얹으면 헤더 65→90px 로 `headerprobe` 상한(≥1024 에서 80px)을 깬다. 랜딩은 **되돌리지 않는다**(초심자 서사에 지수는 소음). 가드 `test/shared/marketIndexStripPlacement.test.ts` — 자리를 바꾸려면 거기부터 고쳐라. ⚠ 헤더 재시도는 **사용자 판단으로 열려 있고**, 그래서 축소형 `variant='header'` 와 레지스트리의 `shortLabel` 은 소비처 0인 채로 남겨 두었다.
- [2026-08-02][design] **등락 표기의 채널은 넷이다: 색 · 부호(+/-) · 모양(▲/▼) · 스크린리더 문장.** 보합에는 마크를 그리지 않는다("0.00%" 하나로 충분하고 en-dash 금지 규칙과 겹친다). 마크는 반드시 변동률의 **형제 요소** — 안에 넣으면 정확일치 계약이 깨지고 `::before content` 는 복사·번역 경로에서 사라진다. 근거 components/MarketIndexStrip/MarketIndexStrip.tsx(IndexCell), shared/constants/marketIndex/copy.ts(directionMark).
- [2026-08-02][product] **참고 시세 메타는 "전일 대비 · 실시간이 아닌 참고용 시세"** — "참고용"만으로는 지연이 읽히지 않는다(엣지 캐시 15분 + 탭 복귀 5분 throttle). 값이 매매 판단에 쓰일 수 있는 표면이라 지연 고지를 문장에서 빼지 마라.
