# 구조 지도 — CLAUDE.md 이후 추가분 (2026-07-17 기준)

> CLAUDE.md의 코드 지도가 기본. 여기는 그 이후 생긴 시스템만.

## 시뮬 상태의 라우트 가시성 (2026-07-26, 목표 페이지 정의 중 확정)
- [2026-07-26][arch] **jotai atom은 전역이지만 하이드레이션(IndexedDB→atom)은 Main 페이지에 묶여 있다** — `usePortfolioPersistence`의 hydrate effect가 MainLeftPanel 마운트에서만 돈다(pages/Main/hooks/persistence/usePortfolioPersistence.ts:177, 호출처 MainLeftPanel 3곳). 즉 **Main 밖 별도 라우트로 직행하면 전역 atom을 읽어도 기본값**이다. 별도 라우트에서 저장 데이터가 필요하면 ①`readPersistedAppState`(appStateStorage) 읽기 전용 direct read → 순수 계산, 또는 ②하이드레이션 앱 레벨 hoist(자동저장·클라우드 sync 연쇄 위험 — 사용자 승인 필요) 중 택1. 자동저장 루프가 없는 라우트에선 ①이 안전(기본값이 디스크를 덮어쓰는 사고 원천 차단).
- [2026-07-26][arch] `simulationAtom`(jotai/snowball/selectors/simulation.ts:7)은 **폼 단일 티커 시뮬**이고, 포트폴리오 합산은 `useMainComputed`(pages/Main/hooks/business) 페이지 훅에 있다. **페이지 밖에서 포트폴리오 결과가 필요하면 정본은 `runScenarioPayload`/`buildScenarioSimSummary`(shared/lib/snowball — 순수, payload→합산)** — sim_summary·OG와 같은 경로라 표기 일치가 공짜로 따라온다.
- [2026-07-26][arch] **새 최상위 라우트 하나를 추가하면 건드리는 곳은 6군데**(캘린더 선례로 실확인): ①`router/routes.tsx`(lazy 라우트) ②`components/PrimaryNav/PrimaryNav.tsx`의 `NavLinkItems` ③`shared/constants/community/copy.ts`의 `nav.*` 라벨(커뮤니티 비활성 배포에서도 항상 렌더되는 링크는 이 파일이 정본) ④페이지 컨테이너의 `useDocumentMeta`(`@/pages/Ticker/hooks`) ⑤`TickerPageShell`(`@/pages/Ticker/components`) — **이름과 달리 티커 전용이 아니라 비-시뮬레이터 페이지 공용 셸**이다(캘린더가 이미 재사용) ⑥`pages/index.ts` 배럴. ⚠ `vite.config.ts`의 사이트맵 `ROUTES`(vite.config.ts:42)는 자동이 아니고 **`/dividend/calendar`는 지금도 미등록**이다 — 새 페이지의 색인 여부는 별도 결정(넣지 않으면 nav로만 도달하는 페이지가 된다).

## 목표 달성 = 내 포트폴리오 화면의 카드 (/dividend/portfolio, 2026-07-28 갱신)
> ⚠ 2026-07-27의 독립 페이지 `/dividend/goal`·`pages/Goal/`은 **삭제됐다**(미배포라 리다이렉트 없음). 아래가 현행이다.
- `shared/lib/snowball/SnowballGoal.ts` — 월 해상도 목표 계산 순수 함수 3종(aggregateMonthly/findTargetMonth/currentMonthlyDividend, now 주입·additive). `runScenarioPayload` 반환에 `monthly` 필드 추가(기존 필드 불변). **흡수 후에도 그대로 재사용**(test/snowball/goal.test.ts 197건 무변경).
- `pages/Portfolio/components/GoalCard/` — 목표 달성 카드(tsx/styled/types/**utils** `buildPortfolioGoalCardModel`·`resolvePortfolioGoalBasis`). **렌더 게이트를 뷰가 아니라 순수 함수가 소유**한다(모델이 `null`이면 카드 미렌더) — 판정이 두 곳으로 갈리지 않게.
- `pages/Portfolio/components/GoalMeter/`·`GoalSetupPanel/` — 구 `pages/Goal/components/`에서 이동(파일 세트·배럴 그대로). 이름의 "Goal"은 사라진 라우트가 아니라 **도메인 개념(목표)**을 가리킨다 — 리네임 불필요(카피 네임스페이스 `PORTFOLIO_COPY.goal`·GA `GOAL_WIDGET_VIEW`·엔진 `SnowballGoal`이 같은 어휘).
- `pages/Portfolio/hooks/useGoalScenario.ts` — 시뮬 저장본 **읽기 전용** 재계산(이름과 달리 "목표"가 아니라 저장된 활성 시나리오를 읽는 훅). `measuredCurrentKrw` 주입 시 실측 우선, null이면 D1 시뮬 파생 폴백.
- `pages/Portfolio/utils/portfolioFormat.ts` — 페이지·카드 공용 날짜 포매터(최하층, 상대 import 0). `PortfolioPage.utils.ts`가 `formatPortfolioSnapshotDate`로 re-export.
- `shared/constants/targets/` — 칩 정본 `TARGET_MONTHLY_DIVIDEND_QUICK_VALUES`(50/100/200/300만)·상한 `TARGET_MONTHLY_DIVIDEND_MAX`(1억)·파생 `..._MAX_MAN_WON`·`sanitizeTargetMonthlyDividend`·프리필 state 헬퍼(build/read). **제품 코드 소비처는 `GoalSetupPanel` 하나**(결과 카드 칩·CTA는 철거됨).
- `pages/Main/components/MainRightPanel/components/TargetFocusRequest/` — 프리필 수신(1회 소비→setField 커밋→포커스→state 소거). **MainRightPanel 하위 고정 = 하이드레이션 게이트**(위로 올리면 사고 — pitfalls 참조). 발신처는 이제 `pages/Portfolio/PortfolioPage`.
- 테스트 면: test/snowball/goal.test.ts(엔진 197) + **test/portfolio/**(카드 상태·기준 판정 7행·목표 커밋·실측/폴백·훅) + test/main/(프리필 수신) + `test/snowball/simulationResultTargetNarrativeRemoved.test.tsx`(결과 카드 부재 계약) + PrimaryNav 회귀. 구 `test/goal/`는 소멸.

## 시뮬레이터(`/`) 셸 구조 (2026-07-28 대개편 후 — 구 2단 컬럼 서술은 전부 폐기)
- 조립: `Main.view.tsx` → **`components/AppHeader`**(2026-07-29 전 페이지 공용 헤더 — 구 `SimulatorHeader` 삭제. 설정 진입 버튼은 헤더에 **없다**) → `FeatureLayout`(**`container-type` 없음** — 컨테인먼트만 제거하고 max-width·패딩은 유지) → `<main display:contents>` > `SimulatorHero` → `SettingsDrawer` → `MainRightPanel`(Fragment: `ScenarioTabsRow` → `MainResultGrid` → 포털 모달 4종) → `MarketDataAsOf` → `LandingDisclaimer`(둘 다 `<main>` 밖 = contentinfo).
- **공용 부품 3종**: `components/common/SideDrawer`(딤 정책·뒤로가기 닫기·Escape defaultPrevented·포커스 복귀·`container-type: inline-size` 본문 — 소비처는 아직 시뮬레이터 1곳, 피커 2벌 통합은 10단계) · `components/common/ResultGrid`(12열 `ResultGrid` + `ResultGridCell<{$span}>`) · `components/common/PageHero`(`titleAs` 기본 `h2` — h1은 여전히 워드마크. **`stat` 슬롯 없음**).
- **페이지 부품**: `pages/Main/components/{SimulatorHero, SettingsDrawer, SettingsEntryButton, ScenarioTabsRow, MainResultGrid}`. 설정 진입점은 `SettingsEntryButton` **한 컴포넌트의 2 variant**(`hero`/`inline`)가 `aria-controls`·`aria-expanded` 계약을 공유한다 — 진입점을 새로 만들지 말고 variant를 늘려라. (구 `header` variant는 2026-07-29 삭제 — 헤더 버튼이 사라지고 히어로 버튼이 `useStickyHeroAction`으로 상시 진입점이 됐다.)
- **결과 카드 배치는 `MainResultGrid` 한 곳**이 소유. (2026-07-29 갱신: 7:5 페어와 `compositionSpan` 분기는 **없어졌다** — 사용자 결정으로 **구성 → 실지급 월별 → 월 평균** 순서에 전부 span 12. 한 행을 나눠 쓰는 페어는 자산 6 : 누적 6 하나뿐.) `MainRightPanel`은 **데이터 조립만**(557줄, 로직은 훅 5개로 분리). ⚠ 결과 카드 추가는 "span 표 한 줄"이 아니라 `MainResultGrid.types.ts` + `.tsx` + `MainRightPanel` 전달 **3파일 세트**다 — 제네릭 `cells[]` 로 일반화하면 배치 지식이 소비처로 새므로 의도적으로 named prop을 유지한다.
- **드로어 안 오버레이는 반드시 `createPortal(document.body)`** — `SideDrawerBody`의 `container-type`과 `ResultGrid`의 `contain: layout style`이 fixed 자손의 컨테이닝 블록을 가로챈다. 현재 `ShareToast`·`CloudReconcileModal`·`ScenarioTabTooltip`·모달 4종이 전부 포털이라 안전하지만 **강제 장치는 없다**.
- **삭제됨**: `components/MobileMenuDrawer` · `components/common/{ContentLayout, HeaderDescription, ConfigDrawer}` · `components/SimulationResult` · `MainRightPanel.styled.ts`(`ResultsColumn`) · `DrawerToggleButton` · `isConfigDrawerLayout()`. 고아로 남은 것 = `pages/Main/components/ModelChangeNotice`(렌더처 0, 테스트 8건 — 네이버 검수 이력과 얽혀 pm-po 판단 후 별건), `MainLeftPanel` 이름(좌 컬럼 없음 — 순수 rename 단독 커밋으로 분리 권장).
- **테스트 헬퍼**: `test/helpers/`(배럴 `@/test`) — `openSettingsDrawer(user)`·`settingsDrawerPanel()`·`stubViewportWidth(px)`·`restoreMatchMedia()`. 구 복붙 스텁은 질의 문자열 정확일치라 같은 폭을 다르게 쓴 질의가 조용히 false였다 → 새 헬퍼는 폭을 정하고 질의를 실제로 평가한다.

## 테마 프리셋 시스템 (2026-07-17)
- `shared/styles/presets.ts` — THEME_PRESETS 레지스트리 8종(각 light/dark 69키), 라벨·스와치 포함. **모든 색 값의 단일 원천.**
- `shared/constants/palette/` — PALETTE_PRESET_IDS(노출 순서), PalettePresetId, DEFAULT='velog', normalize.
- `jotai/snowball/atoms/ui/` — palettePresetAtom(파생 write: localStorage `snowball:palette` + dataset.palette 동기 반영), useApplyPalettePreset(초기/탭 간 동기화).
- `shared/styles/globalStyles.ts` — `:root[data-palette=…]` 스코프 CSS 변수 블록 생성(레지스트리 순회 — 프리셋 추가 시 자동).
- `shared/styles/chartTheme.ts` — getChartTheme().series (canvas는 var() 불가 → computed style 읽기). 차트 소비처는 palettePresetAtom 구독으로 리빌드.
- `shared/styles/contrast.test.ts` — 레지스트리 자동 순회 WCAG 검증(**1129건**, 2026-07-28 기준). 프리셋 추가 시 무수정 확장. 그라디언트 문자열은 `parseGradientStops`+`worstGradientRatio`(경로 9샘플 **최악 지점**)로 검사한다 — 스칼라 사본이 아니라 **배포되는 문자열 자체**를 파싱하는 게 요점.

## 타이포 시스템 (2026-07-28 아이덴티티 패스)
- `shared/styles/tokens.ts` — `font` **4역할**(`sans`/`display`/`heroNumeric`/`dataNumeric`) + `numeric`(tabular-nums 문자열). **여기가 서체의 단일 원천** — 컴포넌트에서 `font-family` 하드코딩 금지.
- `shared/styles/selfHostedFonts.css` — **생성물**(93 `@font-face`). `main.tsx`가 npm Wanted Sans CSS와 함께 import. 직접 편집하지 마라.
- `tools/fonts/build.mjs` — 공식 배포처 다운로드 → 서브셋 → woff2 → **콘텐츠 해시 파일명** → CSS 생성까지의 **재현 스크립트**(순수 Node, npm 의존 0, `python -m fontTools.subset` 필요). 산출물은 커밋하므로 `npm run build`는 네트워크·python을 타지 않는다. 수동 재생성 전용.
- `public/fonts/` — woff2 93 + `OFL-*.txt` 4(**OFL 조건2 고지 의무** — 서브셋이 폰트 안 라이선스 레코드를 지우므로 필수) + `README.md`(출처 URL·라이선스 인용·재생성 방법). ⚠ `vercel.json`이 `/fonts/`에 immutable 1년 캐시를 건다.
- `shared/styles/typography.test.ts` — 4역할 스택의 **문자열 계약**(1순위 서체·한글 폴백 존재·제네릭 종결·역할 간 분리·Pretendard 잔재 0). jsdom이 글리프를 못 그려서 **이게 서체의 유일한 자동 방어선**이다.
- ⚠ **CSS family 명 ≠ 원본 폰트 이름**(2026-07-28 개명): `font.display` = **`'Snowball Display'`**(원본 Gmarket Sans) · `font.dataNumeric` 1순위 = **`'Snowball Numeric'`**(원본 Inter). 파일명·`OFL-*.txt`·저작권 고지는 원본 이름 그대로다 — 문서·주석에서 원본 이름을 볼 때 "옛 표기"로 오해하지 마라. 개명 시 동시 수정 = `tools/fonts/build.mjs` → CSS 재생성 → `tokens.ts` → `typography.test.ts` → `api/{og,share-html}.js` 재번들.
- 적용 지점: `globalStyles.ts`(`h1~h6`→display / `input[type=number]`·`table`·`th`·`td`→dataNumeric), `chartTheme.ts`(fontFamily→dataNumeric, 화면·인쇄 2경로), `StatTile.styled.ts`(hero→heroNumeric·default→dataNumeric), `GoalMeter.styled.ts`, `DataTable.styled.ts`.
- OG(Satori)는 **woff2를 못 읽는다** → `vite.config.ts`가 `node_modules/wanted-sans/fonts/otf/`의 otf 2개를 `dist/fonts/`로 복사하고 `server/handlers/Og/Og.tsx`가 런타임 fetch한다(해시 없는 고정 파일명 — 의도).
- `components/ThemePresetSwitcher/` — 변형 3종(`popover` 기본 / `inline` 드로어 2열 / `menu` 임베드), radiogroup 접근성 계약. **소비처는 `AppHeader` 하나**(2026-07-31): 오른쪽 컨트롤 묶음에 `popover` 로 상시 노출된다. 구 진입점(`HeaderOverflowMenu` 의 "테마" 디스클로저 → `variant="menu"`)은 **제거**됐다 — 되돌리면 진입점이 둘이 된다. `inline`·`menu` 변형은 현재 소비처 0이지만 테스트가 계약을 지키고 있어 남겨 둔다.
- index.html — 프리페인트 스크립트(224~) + `html[data-palette]` 스코프 배경(FOUC 방지). **인라인 스크립트 유효값 목록은 수동 동기.**

## 커뮤니티 (Stage 2, 2026-07-16~)
- `pages/Community/` — CommunityLayout / GalleryPage(카드=velog 포맷 ScenarioCard, 리스트=ScenarioRow) / DetailPage(CommentSection 포함) / WritePage(라우트 `/community/write`, 첨부 3상태 UX).
- `components/community/` — ScenarioCard·Row, SimBadge, CommunityHeader(sticky 글래스), LoginModal, AuthControl, Avatar, LikeButton, RichText 에디터/컨텐츠 등.
- `shared/lib/supabase/` — queries(시나리오·댓글·좋아요·조회수 RPC), comments(buildCommentTree 순수), pagination(keyset), auth, payload 검증. 게시/수정 뮤테이션이 sim_summary를 payload 변경 시에만 (재)계산해 저장, 목록·상세 쿼리가 싣는다(payload는 목록 계속 제외).
- `shared/lib/snowball/SnowballScenarioSummary.ts` — buildScenarioSimSummary/parseScenarioSimSummary (sim_summary jsonb 계약 v1, 게시 시점 고정). 갤러리 뷰가 파서 경유로 카드/행에 주입.
- DB: `supabase/migrations/` 2개 실행 완료(실 Supabase). ⚠ **미실행** 마이그레이션 4개 — 20260717000000_scenario_sim_summary.sql(sim_summary 컬럼+GRANT), **20260717000001_scenario_search_facets.sql**(정밀 검색 G1: sim_summary 파생 generated STORED 컬럼 final_monthly_dividend/target_monthly_dividend/duration_years + 부분 B-tree 인덱스, GRANT 불필요), 20260718000000_user_app_states.sql(클라우드 저장 테이블), 20260719000000_profile.sql(avatars 버킷+storage 정책, insufficient_privilege 가드=실패 시 대시보드 수동). 실행 전 배포 시 해당 컬럼/테이블 select·insert가 즉사한다(실행 후 이 경고 삭제). 댓글 레이트리밋(1분 10개)·대댓글 1단계·소프트 삭제는 DB 트리거가 강제.
- 갤러리 정밀 검색 G1 데이터 레이어(2026-07-17): 갤러리 조회는 **PostgREST 쿼리빌더**(RPC 아님)라 필터는 클라이언트 `.gte/.lte`로 얹는다. 순수 `buildGalleryFacetFilters`(pagination.ts)가 `GalleryFacetFilters`(원/년 단위, monthlyMin/Max·targetMin(≥ 단일)·durationMin/Max — types.ts) → 범위 경계 목록을 만들고 `fetchGalleryPage({facets})`가 적용(검색·키셋·정렬과 AND 공존). 티커 필터는 G2(후속). generated facet 3컬럼은 `ScenarioDbRow`(=ScenarioRow+ScenarioSearchFacets)에만 얹어 `.gte` 타입만 통과시키고 앱 프로젝션(ScenarioListItem/WithAuthor)은 무변경.
- 갤러리 정밀 검색 **UI 랜딩**(2026-07-18): `components/community/PrecisionSearch`(폴더경로 import, 자기완결형 팝오버/인라인) + 순수 URL↔필터 계약 `shared/constants/community/galleryFilters.ts`(`GalleryFilters` 원·년 + parse/serialize/countActiveFilters/hasAnyFilter/`toFacetFilters`). URL 파라미터 mdmin/mdmax/tgtmin/durmin/durmax + `TICKER_FILTER_ENABLED=false`(config.ts). ⚠ **2026-07-31 정정**: `CommunitySearchBar` 의 `variant='desktop'|'mobile'` 과 `PrecisionSearch` 의 `layout='popover'|'inline'` 은 **삭제됐다** — 그 분기는 검색이 헤더 인라인 + 헤더 아래 펼침 바 **두 인스턴스**로 살던 시절의 것이고, 검색이 갤러리 본문 툴바 한 벌로 내려오며 인스턴스가 하나가 됐다(정밀 검색은 전 폭 앵커 팝오버·아이콘 전용 트리거). `useGallery`: URL→facets 조회 배선(loadFirst/loadMore, 의존성=필터 원시값) + `filteredEmpty` status + `clearFilters`(sort/q 보존, filteredEmpty CTA용). copy.ts gallery에 `filter*` 키. `CommunityIcons`에 `FilterIcon`(SlidersHorizontal)·`AlertIcon`. 헤더 로고는 BrandMark→`/app_icon.png` `<img>`(BrandMark 파일은 api/og.tsx 소비로 존치).
- `shared/constants/community/copy.ts` — 커뮤니티 전 카피의 단일 원천.
- **갤러리 본문 툴바 = 검색 줄 + 컨트롤 줄**(2026-07-31, `pages/Community/CommunityGalleryPage`). `SearchRow`(`CommunitySearchBar` 한 벌, ≤1023 sticky `top: var(--sb-app-header-h)`, `z-index: stickyAction`) → `ControlBar`(좌 정렬 탭 / 우 `ControlActions` = 뷰 토글 + 글쓰기). **`CommunityHeader` 는 이제 `contentGutter` 만 넘기는 한 줄짜리**(검색·모바일 돋보기 토글·펼침 바·글쓰기 전부 하차, `AppHeader` 의 `center`/`below` 슬롯도 삭제). 게시판은 자기 `BoardHeader` 에 글쓰기를 이미 갖고 있다.
- **본문 첫 줄 = `pages/Community/components/CommunityTopBar`**(2026-07-29 신설). 좌 "← 목록"(공용 Button `secondary`) + 우 `actions` 슬롯. **헤더에 있던 뒤로가기를 여기로 내렸다** — 좁은 폭에서 워드마크·글쓰기·프로필·더보기와 폭을 다투던 문제. 상세는 이 슬롯에 수정(secondary)·삭제(danger)를 넣고(소유자 한정), 글쓰기·프로필·내가 쓴 글은 빈 슬롯으로 같은 줄만 쓴다. **레이아웃(`CommunityLayout`)이 아니라 각 페이지가 렌더**한다 — 상세만 액션이 붙는 예외를 셸의 라우트 분기가 아니라 슬롯으로 표현하기 위해서다. 상세는 `DetailShell`(max-width 760)이 상단 바와 글 카드를 함께 감싸 좌우 경계를 맞춘다(폭 제한은 그 한 곳뿐).
- 소셜 로그인 버튼: `components/community/SocialLoginButton`(provider prop=google/kakao/naver, 규정색·인라인 SVG 마크·정본 카피 조립, **폴더 경로 import**·배럴 미등록, 포커스 링은 전역 :focus-visible 상속=버튼별 재정의 없음). 네이버는 **2026-07-17 실연동 랜딩** — **config-gated 실동작**: `isNaverEnabled`(=isCommunityEnabled && VITE_NAVER_CLIENT_ID 존재)이면 구글·카카오와 **같은 경로**(onSelectProvider/onLogin→login→signInWithOAuth→auth.ts가 'naver'를 startNaverLogin으로 가로챔)로 보낸다. ⚠ **미설정이어도 버튼을 숨기지 않는다**(사용자 리포트 "네이버 버튼이 사라졌어" 반영, 2026-07-17 정정): env 없으면 **`pending={!isNaverEnabled}`로 "준비 중"(aria-disabled + 배지) 노출 + 클릭 무동작 가드**. 구글/카카오는 늘 보이는데 네이버만 사라지면 회귀로 인지하기 때문. 즉 `pending` prop 은 이제 **네이버 미설정 상태 표시**로 게이트에서 쓰인다(계약 불변). **`copy.login.{google,kakao,naver}`는 로그인 게이트가 공유**(2026-07-18 MySavePanel 제거로 **3곳**): LoginModal·CommunityWritePage·CommunityProfilePage 딥링크 전부 **구글 → 네이버 → 카카오** 순(네이버 항상 렌더). 구 중립톤 `ProviderButton` styled는 레포에서 전부 제거. login.google 한 글자 바꾸면 3곳 다 바뀐다. 라벨 assert는 리터럴 말고 `COMMUNITY_COPY.login.*` 참조(writeAttach·CommunityProfileView·LoginModal 테스트 모두 참조 방식).

## 클라우드 저장 (Stage 1, 2026-07-17~ / cloud-save-proposal.md)
- 개인 워크스페이스 동기화. 커뮤니티 scenarios와 **분리 테이블**(user_app_states) — 게시는 스냅샷 복사라 자동저장이 게시물을 건드리지 않는다.
- `supabase/migrations/20260718000000_user_app_states.sql` — owner-only RLS, autosave 1인1개(partial unique, name is null), 이름 체크포인트 20개 쿼터, payload≤128KB. **미실행**(위 경고).
- `shared/lib/supabase/userAppStates.ts` — IO(client 주입): fetchCloudAutosave/pushCloudAutosave(자동 슬롯 select→update/insert upsert)만. **구 이름슬롯 IO(list/fetch/delete)는 2026-07-18 제거**(MySavePanel 삭제, 사용자 승인). payload는 로컬과 **같은 스키마**(PersistedAppStatePayload) — 읽기는 반드시 normalizePersistedAppState 통과.
- `jotai/snowball/cloud/` — 순수 엔진(createCloudSyncScheduler 디바운스 4s + createAutosavePush 게이팅 / **syncCloudWorkspaceAtSessionStart** 조용한로드+안전마이그레이션 IO주입) / 마이그레이션 플래그(cloudMigrationFlag: user id별 localStorage) / 상태 atom(cloudSyncStateAtom — 구 cloudSavedStatesAtom·CloudSavedStateSummary는 2026-07-18 제거) / 훅(useCloudSync=scheduleCloudSave·flush, useCloudSavedStates=pullAutosave·**pushAutosave**(마이그레이션 즉시 push) — **saveCheckpoint·refresh·load·remove는 2026-07-18 삭제**, 명시 체크포인트·이름슬롯 관리 폐기). ⚠ **`jotai/snowball` 배럴에 미연결**(supabase 초기번들 격리). 소비는 `@/jotai/snowball/cloud` 폴더 경로.
- `jotai/snowball/persistence/appStatePayloadDiff.ts` — **공용** payload 비교(배럴 → `@/jotai`, supabase 무관): isSamePersistedPayload(조용한로드/read-back) + **의미있는 부분집합**(serializeMeaningfulPayload/isSameMeaningfulPayload — activeScenarioId·최상위미러·뷰토글·selectedTickerId 배제). no-op 게이트가 usePortfolioPersistence 자동저장 effect에서 ref로 직전 예약과 비교(동일=클라우드 스킵). autosave 슬롯 단독 삭제=`deletePersistedAppStateAutosave`(appStateStorage).
- 하위 호환: 로컬 IndexedDB 자동저장·이름 슬롯·?share= URL·JSON 가져오기 **전부 무변경**. 클라우드는 추가 계층. 비로그인은 클라우드 skip(로컬만).
- **Stage 1 UI**: `components/CloudSyncIndicator`(저장 상태 아이콘+문장, describeCloudSyncState 순수 매퍼). 자동저장 배선·buildPayload/applyPersistedPayload/retryCloudSave 노출은 usePortfolioPersistence에 추가.
- ⚠ **2026-07-18 MySavePanel(데이터 저장 패널) 완전 제거** — 자동저장이 대체(decisions.md). `components/MySavePanel/` 폴더·툴바 "데이터 저장" 버튼·`onOpenMySave`·usePortfolioPersistence의 이름슬롯/JSON 함수·`savedName.ts`·`appStateStorage.ts` 이름슬롯 IO(list/read/write/delete-ByName) 삭제. **CloudSyncIndicator는 헤더로 이전**(`header` variant: idle 미렌더, 실패에서만 라벨+재시도). 재시도는 MainLeftPanel이 소유+`onRegisterRetryCloudSave`로 Main.view ref에 등록(훅 hoist 금지). 구 정보(클라우드/이 기기 탭·데이터관리·body 포털·LOGIN_NUDGE_TEXT·MySavePanel 로그인 게이트)는 전부 소멸.
- ⚠ **2026-07-17 클라우드 중심 전환 → 2026-07-19 충돌 화해 재도입으로 부분 무효**: 구 `components/CloudConflictPrompt`는 여전히 삭제됐지만, `useCloudWorkspaceSync`는 **다시 `{conflict,summary,resolve*,defer}` 화해 API를 반환**한다(무음 last-write-wins → 3-way 화해, decisions.md 2026-07-19). UI 배선(2026-07-19 landing):
  - **`components/CloudReconcileModal`**(신설, 순수 뷰) — 좌 이 기기/우 클라우드 요약(탭 개수·이름칩·상대 편집시각·"최근 편집" 태그) + 3택(둘 다 합치기[비파괴·기본포커스·"합치면 N개 탭"=previewBlend] / 이 기기 / 클라우드). Esc·바깥클릭=이연(deferConflict). Modal 프리미티브(`@/components/common/Modal`) 재사용.
  - **MainLeftPanel**이 API를 캡처해 모달을 body에 createPortal. 열림=`conflict!==null && !dismissed`(이연은 conflict atom을 남기고 로컬 dismissed만 true — 헤더 재개봉용). 재개봉 트리거는 `onRegisterResumeConflict` ref로 Main.view에 등록(retryCloudSave 패턴).
  - **CloudSyncIndicator**에 `'conflict'` case + `warning` 톤 + `conflict` glyph(GitMerge) 추가, header variant는 충돌을 **클릭 가능한 버튼**('동기화 보류 — 확인 필요')으로 띄워 `onResume`으로 모달 재개(저장중/실패의 정적 role=status와 구분). 기존 5-상태 렌더 불변.
- 세션 배선: MainPage를 **CommunityAuthProvider로 감싸 재사용**(Provider 파일 수정 없이 consume-only) → 헤더 AuthControl(§8.2, isCommunityEnabled 게이트)이 세션 atom을 읽는다. 툴바 퀵액션 = **[Share] 하나**(Coffee 숨김) — 데이터 저장은 자동저장으로 대체 제거, ~~Capture는 2026-07-18 전면 삭제~~ → **2026-07-29 결과 이미지 저장으로 부활**(구 capture/*·capturePage.ts 는 그대로 죽었고, 새 구현은 결과 그리드 한 곳만 찍는 ResultCaptureButton — §공유·캡처), 로컬 이름 슬롯은 IO 코드까지 제거.

## 소셜 로그인 (2026-07-17 표준화 / social-login-spec.md)
- `components/community/SocialLoginButton/` — 공용 컴포넌트(provider=google/kakao/naver). 브랜드 규정색 **하드코딩**(테마 토큰 예외 — 8프리셋 무관 고정: 구글 흰+4색G+회색테두리, 카카오 #FEE500, 네이버 #03C75A). 로고 **인라인 SVG**(CSP). pending prop(딤+"준비 중" 배지)은 네이버 config-gated 실연동에서 **`pending={!isNaverEnabled}`로 미설정 상태 표시**에 쓰인다(2026-07-17 정정 — 버튼 숨김 금지, 위 §커뮤니티 소셜 로그인 항목). 카피 정본 = copy.ts login 섹션(구글 "Google 계정으로 계속하기").
- **로그인 게이트 4곳 모두 이 컴포넌트 사용**(2026-07-18 MySavePanel 제거로 4→3, 2026-07-25 my-posts 신설로 다시 4, pitfalls 참조): LoginModal / CommunityWritePage / CommunityProfilePage / CommunityMyPostsPage. 구 ProviderButton은 레포에서 완전 제거됨. 네이버 실연동 seam: `shared/lib/supabase/naver.ts`(startNaverLogin/completeNaverCallback/isNaverEnabled/NAVER_CALLBACK_PATH), 서버 `api/naver-auth.ts`, 엔트리 배선 main.tsx(isNaverCallbackPath 분기), 콜백 라우트 router/routes.tsx(isNaverEnabled 게이트).

## 글쓰기 첨부 (2026-07-18 "첨부" 토글 + 1단계 피커 / 구 write-attach-picker-spec.md 2단계·"첨부 안 함 라디오" 폐기)
- CommunityWritePage: 섹션 제목 "시뮬레이션" 우측 **"첨부" 토글**(common ToggleField, 뷰 로컬 `attachEnabled`)이 첨부 활성/해제를 쥔다. 토글 ON이면 **워크스페이스 시나리오 탭 전부를 radiogroup 카드로 택1**(useScenarioCandidates → composer.attachScenario), 카드를 고르면 **1단계 즉시 첨부**(별도 버튼 없음). 토글 OFF(기본)=미첨부·피커 숨김(optional). 무효 payload=비활성 카드+사유. 수정 모드 외부 첨부(후보와 매칭 안 되는 서버 payload)는 effect로 토글 자동 ON + 요약 카드만(해제=헤더 토글). 요약은 SimSummaryStats 재사용. 게시 저장 스키마·sim_summary·publish·검증 **무변경**. UI는 **surface 패널 card화**(WriteForm surface+shadow.e1+radius.lg, 옵션 카드 surfaceSunken, PickerGrid 스크롤/gutter 제거로 폼 폭 정렬), 게시 설정 공개/비공개 안내는 토글 옆(VisibilityRow flex). FormSection은 title 우측 슬롯 미지원이라 이 섹션 헤더만 로컬(AttachSection*). 상세는 decisions.md 2026-07-18.

## 결과 화면의 "표기 전환" 확장점 (2026-07-24, 표시 통화 토글 설계 중 확인)
- [2026-07-25][design] ~~"결과 전체에 걸리는 표시 컨트롤은 좌패널 카드에 두면 안 된다"~~ **철회 — 사용자가 트레이드오프를 알고도 투자 설정 카드 안으로 확정**(decisions.md 참조). 원래 근거(좌패널은 ≤960에서 드로어로 접혀 **결과를 보면서** 못 바꾼다 — MobileMenuDrawer.tsx:34 `ConfigDrawerColumn`)는 **여전히 사실**이지만, 사용자는 "표시 통화는 환경설정"이라는 판단으로 설정 카드를 택했다. 남는 규칙: ⚠ `ScenarioTabsWrap`(Main.shared.styled.ts:230) 안에는 어떤 컨트롤도 두지 말 것 — `overflow-x:auto`+스냅이라 탭이 많으면 스크롤 밖으로 밀린다.
- [2026-07-24][frontend] 금액 표기를 통째로 바꾸는 변경의 실제 blast radius는 **차트 쪽에 몰려 있다**: `SimulationResult`는 이미 `formatResultAmount`를 **prop으로 주입**받아(SimulationResult.types.ts:9, MainRightPanel.tsx:795) 컴포넌트 무수정으로 표기를 갈아끼울 수 있는 반면, `pages/Main/utils/charts.ts`는 `formatKRW`/`formatApproxKRW`를 **5곳 하드코딩**(:42 축 기본, :170 파이 중앙, :239·:301 툴팁, :263·:326 축)하고 `MonthlyCashflow`는 chartOption을 자기 안에서 빌드(MonthlyCashflow.tsx:25)+합계 문자열도 자체 포맷(:53)한다. 확장 방식은 리포트 트랙이 쓴 **optional 파라미터 추가(미지정=기존 동작)** 관례를 그대로 따르면 기존 호출부·PDF가 무변경으로 산다. 캔버스는 CSS/클로저를 다시 읽지 않으므로 표기 상태는 옵션 `useMemo` 의존성에 반드시 추가(팔레트 stale-by-one과 동형 함정).

## 모바일 드로어 공통 (2026-07-27, fix/mobile-drawer-ux — 커밋 bb48ae9)
- **`shared/hooks/useDrawerBackClose/`** — 드로어 뒤로가기 닫힘 공용 훅(레포 최초의 shared/hooks 폴더). `history.pushState(state, '')`(URL 인자 생략=URL 완전 불변, 해시 금지 결정 준수) + popstate, 닫기 시 back()을 setTimeout(0) 예약으로 되감기 — 재열기 경합은 RewindState 상태머신(마커 재사용/needsRepush)이 방어. 소비처 3곳: MobileMenuDrawer(≤960px matchMedia 게이트), PickerDrawer(캘린더), PresetFilterDrawer(TickerModal 안 중첩 — back 1회=필터만 닫힘). ⚠ `shared/index.ts` 루트 배럴에는 의도적 미등록(그 배럴은 React-free 서버 경로가 import) — 소비는 `@/shared/hooks`로. ⚠ 인스턴스 간(서로 다른 드로어) 같은 틱 교체 경합은 미방어 — 그런 동선이 생기면 상태를 모듈 단위로 승격(훅 JSDoc 참조).
- **`shared/hooks/useOverlayEscape/`** (2026-07-29 신설) — 위 훅의 **키보드 쌍둥이**. 모듈 스코프 스택으로 "Escape 1회 = 최상위 오버레이 한 겹"을 보장한다(`defaultPrevented` 존중 → 안쪽 입력의 검색어 지우기가 먼저, 반응 시 `preventDefault` → 비참여 핸들러 보호, 리스너는 `document` 버블 1곳). 참여 층 4개: `SideDrawer`·`TickerModal`·`HelpModal`·`ShareDialog`. **비참여**: `PresetFilterDrawer`(자기 `nativeEvent.stopPropagation()`으로 이미 격리) / `HoldingPickerDrawer`·`PickerDrawer`(자기 페이지에 위층 오버레이가 없음) / 드롭다운·툴팁류(전면 오버레이가 아님). 새 전면 오버레이를 만들면 **이 훅에 참여시켜라** — 직접 window/document 리스너를 달면 층 순서가 DOM 전파 순서로 결정돼 두 겹이 함께 닫힌다(pitfalls 참조). 계약 테스트 `test/main/overlayEscapeNesting.test.tsx`.
- 드로어 X 닫기 버튼은 **원형 보더 없는 담백형이 확정 스타일**(사용자 명시 요청): ConfigDrawer.styled·PickerDrawer.styled 통일, 터치 타깃(38/36px)·focus-visible 링은 유지.
- **TickerModal도 포함**(커밋 bec12b3, 사용자 정정 "스크롤 미작동은 티커 생성쪽"의 산물): 스크롤 복구(위 pitfalls 거터 항목) + `useDrawerBackClose` 적용(back 1회=필터 드로어만, 1회 더=모달만) + 배경 잠금은 ≤960px에서만 **documentElement**를 잠근다(드로어의 body 잠금과 대상 분리 — pitfalls 순차 닫힘 항목, 데스크톱은 무잠금 유지=레이아웃 점프 방지). 이중 잠금 계약 테스트 = test/main/overlayScrollLock.test.tsx.
- **헤더 동기화 상태 표시의 모바일 배치**(커밋 e1f8a91): ≤960px에서 지속 상태(저장 실패·동기화 보류)는 `HeaderSlot`(`$ownLine` → `flex:1 0 100%`)으로 "설정 열기" **아래 줄**에 접히고, 전이 상태(저장 중 스피너)는 같은 줄 유지(디바운스마다 헤더 높이 출렁이는 CLS 방지 — 줄바꿈 결정은 상태를 아는 CloudSyncIndicator가 자기 래퍼로 한다, 부모 슬롯이 아니라). 충돌 카피는 shortLabel "동기화 보류" + detailLabel "확인 필요"(HeaderTextDetail, 모바일 display:none) 분할 — 데스크톱 문구 불변. aria-label에서 "확인 필요"가 빠진 건 designer 의도(발화 정리). 충돌 버튼 44×44 히트 영역은 `::before` 확장(시각 32px 유지). ⚠ idle/saved/offline `return null`은 래퍼 **앞** 유지가 테스트 계약(빈 래퍼 = toBeEmptyDOMElement 3건 실패).
- 미해결 꼬리: ①`ConfigColumn`의 `contain: layout paint style`이 드로어 폭에 살아 있어 그 안의 ShareToast(fixed)가 드로어 기준으로 뜸 ②HelpModal 등 **나머지 모달**엔 뒤로가기 닫기 없음(범위 확장 금지 지시) — 티커 모달 위 도움말이 뜬 채 back을 누르면 밑의 티커 모달이 닫히는 엣지(reviewer 관찰) + 중첩 열림 중 백드롭 클릭 닫기 시 죽은 히스토리 엔트리 1개(dead press 1회, 훅 JSDoc 문서화된 트레이드오프) ③다른 모달들의 max-height는 여전히 vh(dvh 미승격).

## 기타
- 아이콘: lucide-react 1.24.0 (기설치), strokeWidth 1.8 관례. 프리셋 카드 매핑 = MainRightPanel.tsx PRESET_ICON_BY_ID.
- `api/og.tsx` + middleware.ts + **api/share-html.ts**(트랙 F) — 공유 링크의 동적 OG 카드. `?share=`(lz-string, middleware inline 치환)·`?s=`(DB key, middleware→api/share-html rewrite) 양쪽 지원. `shared/lib/og/`(metaHtml 순수치환 + shareKey 패턴 + sharedSnapshotRest anon REST 조회)를 middleware(Edge)·api(Node)가 공유. OG 텍스트 조립(og:title 3분기+target 가드)은 `pages/Main/utils/ogCard.ts` buildOgShareText(shared/lib은 pages import 불가라 여기). 실 미리보기 검증=사용자 배포 몫. (sim_summary와의 표기 일치는 백로그.)
- 파비콘/PWA 아이콘: public/app_icon.png(1024²)이 단일 원본 — 재생성 시 전부 여기서.

## 프로필 기능 (2026-07-17 완료 / profile-ui-spec.md)
- `pages/Community/CommunityProfilePage/` — `/community/profile` 전용 페이지. **2단 레이아웃**: 좌=프로필 편집 통합 카드(아바타+닉네임, **이메일 미노출** — 사용자 지시), 우=위험 영역(회원 탈퇴). 모바일 세로 스택은 편집→탈퇴 순(위험 아래로). AuthControl 드롭다운 "프로필 설정"로 진입.
- `components/community/DeleteAccountDialog` — 재확인 "탈퇴" 입력해야 활성, 기본 포커스=취소, danger. **POST /api/account-delete(Bearer)는 200일 때만 로그아웃**(성공 위장 금지). CommunityModal.initialFocusRef 옵셔널 확장(기존 트랩 테스트 그린).
- `shared/lib/community/{profile,avatar,accountDelete}.ts`(profile=닉네임 검증, avatar=**서버 탈퇴 청소용 AVATAR_BUCKET·avatarStorageFolder만** — v2에서 클라 리사이즈/업로드/MIME 제거, accountDelete=분기), `api/account-delete.ts`(Vercel, SERVICE_ROLE_KEY admin, 탈퇴 시 avatars 폴더 청소), CommunityAuthProvider.refreshProfile()(저장 성공 후 호출 — 낙관적 갱신 금지). analytics: profile_updated(**field=nickname만**)/account_delete_started/account_deleted.
- ⚠ 탈퇴 서버 삭제 E2E는 Vercel preview 필요(로컬 유닛은 분기만). **v2: 아바타 업로드/제거 UI·훅·auth IO(uploadMyAvatar/removeMyAvatarFile/AvatarStorageUnavailableError·updateMyProfile.avatarUrl) 삭제** — profiles.avatar_url 컬럼·avatars 버킷은 역가역 유지(표시만 이니셜로 통일).
- 카카오 로그인: auth.ts OAUTH_SCOPES 카카오 = **`profile_nickname` 만**(이메일 KOE205 회피 + v2에서 profile_image 제거).
- [2026-07-25] **"내가 쓴 글" = 독립 화면 `pages/Community/CommunityMyPostsPage/`**(`/community/my-posts`, 진입점=AuthControl 드롭다운 "프로필 설정" 바로 아래). 목록은 그 안의 `components/MyPostsSection/`(컨테이너 `.tsx` ↔ 순수 뷰 `.view.tsx`, 데이터는 `hooks/useMyPosts.ts`) — **프로필 설정 안의 섹션이었다가 같은 날 통째로 옮겨졌다**(사용자 요청). ⚠ 이 섹션은 **페이지 viewModel에 얹지 않고 자기 데이터를 스스로 쥔다**(PrecisionSearch 선례) — 코드만 봐선 안 보이는 이유: `CommunityProfileViewModel`은 `test/community/CommunityProfileView.test.tsx`가 **전 필드를 손으로 조립**해서, 필수 필드를 하나 늘리면 그 테스트가 tsc에서 깨진다(여러 트랙이 동시에 그 파일을 만지는 중이면 남의 작업과 충돌). 프로필에 무관한 섹션을 붙일 땐 VM 확장보다 자기완결 컴포넌트가 싸다. 상세 경로 규칙(`kind==='board' ? /community/board/:id : /community/portfolio/:id`)은 PostCard·PostRow·MyPostsSection 3곳에 각자 인라인돼 있다(공용 헬퍼 없음 — 라우트를 바꾸면 3곳 다 고쳐야 한다).

## GA4 계측 (analytics.ts)
- 로그인 계측 이원화 주의: login_completed 정본 발화점은 CommunityAuthProvider SIGNED_IN(프로필 트랙 소유)인데, 클라우드 트랙은 sessionStorage 마커로 OAuth 리다이렉트 넘어 귀속(useCloudSyncAnalytics). **auth 트랙이 SIGNED_IN에 직접 login_completed를 넣으면 이중 계측** — 단일화 필요(백로그).
- Phase 1 배선 랜딩(2026-07-19): 커뮤니티 6이벤트 + User Properties 5종 + scenario_shared + value_bucket. **커뮤니티 이벤트는 데이터 훅에**(useScenarioDetail=post_view[view등록 1회 게이트 안]·like, useComments=comment[createComment 성공], useScenarioComposer=post_published[mode!=='edit'만], CommunityGalleryPage 컨테이너=gallery_view[마운트], CommunityDetailPage 컨테이너=to_simulator). **User Properties**: has_account=CommunityAuthProvider 세션 감지(메인+커뮤니티 공통 choke, 세션복원에도 태깅), has_saved=useTickerActions TICKER_SAVED·useCloudSyncAnalytics saved 전이·usePortfolioPersistence RETURN_VISIT, is_returning=RETURN_VISIT, preferred_theme=ThemePresetSwitcher, community_active=발행/좋아요/댓글 성공 지점. 전부 멱등(매번 set). value_bucket=useSnowballForm VALUE_BUCKET_EDGES 맵(연속 금액/기간/세율만, 원값 value는 유지). scenario_shared=TickerCreation.handleShareLink 성공 분기(copy_link/show_link). 신규는 타입 `track()`, 배럴 아닌 파일 `@/shared/lib/analytics` 직접 import.

## 배당 캘린더 (2026-07-25, feat/dividend-calendar 브랜치 — 미푸시)
- `pages/DividendCalendar/` — 라우트 `/dividend/calendar`(routes.tsx lazy). `utils/`(calendarSchedule 조인/필터/월 이벤트 · calendarShareUrl `?tickers=` · calendarStorage IndexedDB 전용 DB · calendarDayGrid 월간 그리드) / `DividendCalendarPage/`(컨테이너↔뷰, TickerPicker·AgendaList·날짜미정 섹션) / `copy/`(형제 폴더 — 배럴 순환 회피). 테스트 test/dividendCalendar/(106케이스).
- 데이터: `scripts/tickerRefresh/derive.ts`가 `estimatedPayDayByMonth`(pay 20종, 지급월 키) 파생 — marketData 타입·zod·스냅샷 반영, api/og.js·ticker-html.js 전이 재생성. 계약 상세는 decisions.md §배당 캘린더.
- 커밋 v1 a8b2915(연간 보드, 폐기됨)·v2(월간 달력) — 헤더는 TickerPageShell 재사용, PrimaryNav 링크는 메인 세션 별도 트랙.

## 공유·캡처 (2026-07-29, feat/simulator-overhaul — 143f4d1·9283918·9960ecf)
- `components/common/ShareDialog` — 앱 공통 **공유 창**(링크 복사 1급 + X·페이스북·네이버 인텐트 + 읽기전용 주소 입력). 스스로 body 로 포털. `isNativeShareIdiomatic()`(터치 기기 판정)이 "OS 시트 vs 이 창"을 가르는 **단일 분기점** — 소비처는 `usePostShare`(커뮤니티 피드·상세)와 `TickerCreation`(시뮬레이터, **클립보드 실패 시에만**). 채널 추가는 `SHARE_CHANNELS` 한 곳 + GA `community_post_shared.method` 유니온.
- `pages/Main/hooks/interaction/htmlCapture.ts` — **DOM→캔버스 공용 저수준**(의존성 0, html2canvas 는 안에서 동적 import). PDF 리포트와 결과 이미지 캡처가 공유한다: `waitForImages`/`waitForFonts`/`nextFrame`/`captureElementToCanvas` + 마커 상수 2개(`RESULT_CAPTURE_ROOT_ATTRIBUTE`·`CAPTURE_EXCLUDE_ATTRIBUTE`). **마커 상수가 파이프라인이 아니라 여기 사는 이유**: 마커를 다는 쪽은 항상 화면 컴포넌트라, 파이프라인에 두면 그 컴포넌트가 캡처 청크를 정적으로 끌고 온다.
- `resultCapturePipeline.ts`(동적) / `resultCaptureError.ts`(정적, 사유 어휘) / `useResultCapture.ts` — PDF 3분할과 **같은 형태**. 버튼은 `pages/Main/components/ResultCaptureButton`, 자리는 `ScenarioTabsRow`의 `captureAction` 슬롯(간략히 토글 왼쪽). 캡처 대상 = `MainResultGrid`의 `ResultGrid` 하나이고 탭 바는 그 **형제**라 자연히 빠진다 — 둘 중 하나가 다른 하나 안으로 들어가면 그 순간 계약이 깨진다(`test/main/resultCapture.test.tsx`가 잠근다).

## 진행 중 (완료 시 이 파일 갱신할 것)
- 없음 (이번 세션 기능 트랙 전부 랜딩). 남은 것은 전부 사용자 배포 액션(마이그레이션 3개 실행·Storage 버킷·Vercel 환경변수)과 Stage 2(공유/OG 통합), 백로그 항목.

## 모션 공용 부품 (2026-07-31 — 리워크 P3)
- **`shared/hooks/useOverlayPresence`** — 닫힌 뒤에도 값을 `exitMs` 동안 붙잡아 퇴장 모션이 그려질 시간을 주는 훅(열림은 렌더 중 즉시 통과, reduce 선호면 잔류 0). 이 앱의 모달은 전부 호출부가 조건부 마운트라 이 훅 없이는 퇴장이 불가능하다. 소비처는 아직 `pages/Main/components/HelpModal` 하나 — 나머지 모달(`ShareDialog`·`CloudReconcileModal`·`InstallGuideModal`·`TickerModal`·MainRightPanel 3종)은 각자 트랙에서 같은 방식으로 붙이면 된다.
- **`components/common/Modal`** — `ModalBackdrop`/`ModalPanel` 이 `$phase`(`'enter'|'exit'`) transient prop 을 받는다. **주지 않으면 지금까지와 똑같이 진입만** 한다(옵트인). 퇴장 키프레임 `sb-modal-fade-out`/`sb-modal-sink`, 잔류 시간 상수 `MODAL_EXIT_MS`(=120, `motion.exit` 과 같은 값이어야 한다).
- **`shared/styles/iconSwap.ts`의 `iconSwapIn`** — 토글되는 아이콘 등장 믹스인(`pressable` 과 같은 관례: 토큰 import 없이 CSS 변수 사용). 소비처 3곳 = `ThemePresetSwitcher` 선택 ✓ · `PortfolioAllocation` 힌트 글리프(잠금/고정/단일 사유 전환) · `StatTile` 상태 글리프. ⚠ 클라우드 저장 배지처럼 **고빈도로 상태가 도는 아이콘에는 쓰지 않는다**.
- **`components/common/StatTile`의 `status`/`statusLabel`/`statusEnter`** — 타일 면 상태(현재 `'success'` 하나). `tone`(숫자 방향성)과 **다른 축**이고 값에는 닿지 않는다. `statusEnter` 는 "지금이 그 상태가 된 순간인가"이고 판정은 호출부가 소유한다(`useGoalReachCelebration`).
- **1회성 연출 판정 훅 2개** — `components/ResultSummaryCard`의 `useGoalReachCelebration`(W1) · `pages/Main/components/MainResultGrid`의 `useFirstResultReveal`(W3). 둘 다 `.utils.ts` 에 산다(파일 세트에 `.hooks.ts` 가 없다 — `test/shared/structureRules.test.ts`). 가드 `test/main/wowMomentsOnce.test.tsx`.

## 랜딩 페이지 `pages/Landing` (2026-08-01 — 트랙 ⑦/P3, **`/` 승격 완료**)
- 구조: `LandingPage/`(컨테이너 ↔ `.view.tsx` 순수 뷰) · `components/`(LandingSection·LandingSearch·ConceptLadder·CompoundExplainer·PayoutRhythm·PresetBrowser·StartChecklist·LandingFaq) · `copy/landingCopy.ts`(**전 섹션 문장 + FAQ 8문항의 단일 지점**).
- 컨테이너가 소유하는 것은 둘뿐이다: ①`hasStoredWorkspace()` 마커 읽기(`useState` 초기화 — 이펙트면 히어로가 한 프레임 흔들린다) ②CTA 라우팅·계측. **계산 엔진·폼 atom 을 건드리지 않는다.** (2026-08-02 이전에는 `useMarketIndicesSync()` 도 여기 있었다 — 아래 항목.)
- `LandingSearch` 는 **자기완결형**(`useSearchParams` 직접 read/write) — `CommunitySearchBar`·`PrecisionSearch` 와 같은 모델이다. 스펙 §11 은 컨테이너가 `q` 를 쥐라고 적었지만, 이 화면에서 상태를 가진 부품이 이것 하나뿐이라 끌어올리면 컨테이너·타입·뷰 세 파일이 같이 커진다.
- 🔴 **`MarketIndexStrip` 은 이제 랜딩에 없다**(2026-08-02 — decisions.md 「주요 지수 스트립의 자리」). 시세가 실제로 쓰이는 세 화면(시뮬레이터·내 포트폴리오·배당 캘린더)의 본문 맨 위로 갔고, 조회 드라이버도 그 세 컨테이너가 각자 부른다(서로 다른 라우트라 동시에 살지 않는다). 되돌리려면 `test/shared/marketIndexStripPlacement.test.ts` 부터 고쳐라. 배치 제약은 그대로 유효하다: 카드로 감싸지 않고(Root 가 투명, 셀이 각자 검증된 면을 갖는다) 자체 `<section>`+`<h2>` 가 있어 `LandingSection` 류로 감싸면 랜드마크가 이중이 된다.
- 신규 공유 상수 2개: `shared/constants/portfolioPresets`(프리셋 13종 — 최상위 배럴 미연결) · `shared/constants/tickerPages`(경량 인덱스, **import 0건 리프**).
- 🔴 `/` 는 `router/routes.tsx` 에서 **조건 없이 `LandingPage`** 다(2026-08-01 결정으로 `RootRoute` 공유 링크 분기 폐기 — decisions.md 참고. 옛 `/?share=` 는 랜딩에 착지한다). 임시 `/landing-preview` 라우트와 `usePageHue` 의 그 한 줄은 삭제됐다. **랜딩과 시뮬레이터는 같은 hue(identity)** 를 쓴다(정문 ↔ 그 문 안의 도구 — `usePageHue.utils.ts` 배정 주석에 근거).
- 테스트: `test/landing/{landingHarness.tsx, landingStructure, landingSearch, landingDataDiscipline, tickerPageIndexParity}`.
