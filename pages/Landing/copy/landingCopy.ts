import { SIMULATOR_PATH } from '@/shared/constants/routes';

/**
 * 랜딩 화면의 **모든 문장이 사는 한 파일**.
 *
 * 법무 문서(`pages/Legal/copy/*`)와 같은 근거다 — 문장만 바뀌는 개정이 마크업 diff 와 섞이면
 * "무엇이 바뀌었나"가 안 읽힌다. FAQ 는 다음 트랙에서 `FAQPage` JSON-LD 로 색인될 예정이라 특히 그렇다.
 *
 * ── 이 지면의 카피 규율 (어기면 되돌려라) ─────────────────────────────────────────
 *  1. 🔴 **"눈덩이 / 스노우볼" 비유 금지 — 예외 없음**(decisions.md `[2026-07-22][copy]` + 2026-08-03 개정).
 *     "브랜드명 suffix 는 예외" 조항은 폐기됐다(제품명이 구 이름이던 시절의 것). 제품명은 영문
 *     **Hungry Hippo** 하나이고, 같은 개념은 **복리 · 시간 · 재투자**라는 직접적인 말로 푼다.
 *  2. 🔴 **투자 권유 금지.** 문장은 조건부·예시형("~라고 가정하면 이렇게 계산됩니다")이고 약속형
 *     ("~를 받게 됩니다")을 쓰지 않는다. 특정 증권사·상품·매수 시점 언급 0, 외부 금융사 링크 0.
 *  3. 🔴 **지어낸 숫자 0.** 화면의 수치는 전부 실데이터에서 온다 — 지수(API) · 지급 월
 *     (`marketData` 스냅샷 런타임) · 프리셋 비중(`allocations`). 카피에 숫자를 박지 않는다.
 *  4. 격식체(합니다체) 통일.
 */

/** 검색 결과가 없을 때 대신 보여 줄 종목. 성격이 겹치지 않는 셋(배당성장 · 고배당 · 월배당). */
export const LANDING_SEARCH_FALLBACK = ['SCHD', 'VYM', 'O'] as const;

/**
 * 히어로 CTA — **순서와 variant 가 데이터**다.
 *
 * 🔴 지금 값은 스펙 D1 의 **A안**(시뮬레이터 primary)이고, 이것은
 * `docs/design-refresh-plan.md:357`(RP 5-2, 포트폴리오 primary)을 뒤집는 **사용자 승인 대기 제안**이다.
 * B안으로 확정되면 **이 배열의 순서와 두 `variant` 값만 뒤바꾼다 — 마크업 diff 0.**
 * 두 안 모두 시뮬레이터 CTA 가 접힘 위에 있으므로 어느 쪽이든 §2 북마크 보호 요구를 깨지 않는다.
 *
 * `id` 는 화면의 `data-landing-cta` 속성과 계측 이름의 출처다 — 순서가 뒤집혀도 프로브는 같은
 * 요소를 가리킨다.
 */
export const LANDING_HERO_CTAS = [
  {
    id: 'simulator',
    label: '배당 계산 시작하기',
    to: SIMULATOR_PATH,
    variant: 'primary',
    ctaName: 'landing_hero_simulator'
  },
  {
    id: 'portfolio',
    label: '보유 종목으로 계산',
    to: '/dividend/portfolio',
    variant: 'secondary',
    ctaName: 'landing_hero_portfolio'
  }
] as const;

export type LandingHeroCta = (typeof LANDING_HERO_CTAS)[number];

/**
 * 이 지면의 **차례**(2026-08-03 2차 리워크).
 *
 * 랜딩은 6장짜리 안내서인데 before 에는 목차가 없었다 — 방문자는 4136px(실측 @1280) 짜리 문서를
 * 스크롤로만 훑어야 했고, "이 화면이 무엇을 다루는가"는 끝까지 내려야 알 수 있었다. 이 배열이
 * ①히어로 아래 차례 ②각 장의 번호 ③앵커 주소를 **한 출처에서** 낸다.
 *
 * 🔴 `anchorId` 는 **손으로 적은 안정 문자열**이다. `useId` 파생을 쓰면 `:r3:-concept` 처럼
 * 콜론이 든 값이 주소창에 남고(렌더마다 달라진다) 북마크가 죽는다.
 * 🔴 `ordinal` 은 장식이 아니라 **차례와 장 머리가 같은 값을 말한다**는 계약이다 — 두 곳이
 * 갈라지면 목차가 거짓말을 한다. 그래서 두 화면이 같은 배열을 읽는다.
 * ⚠ 순서는 `LandingPage.view.tsx` 의 섹션 순서와 같아야 한다(문서 순서 = 차례 순서).
 */
export const LANDING_CHAPTERS = [
  /*
   * 🔴 **처음 온 사람의 길**이 1장이다(2026-08-06 사용자 지시). 종전 1장은 '주식·ETF·배당주'
   * 낱말 풀이였는데, 그 앞에 "계좌는 어떻게 여나 / 투자는 어떻게 시작하나"가 통째로 비어 있었다.
   * 개념을 모르는 사람이 계산기 앞에 서는 일을 막는 것이 이 장의 목적이다.
   */
  { key: 'start', anchorId: 'landing-start', ordinal: '01', label: '처음이라면 여기부터' },
  { key: 'concept', anchorId: 'landing-concept', ordinal: '02', label: '주식 · ETF · 배당주' },
  { key: 'compound', anchorId: 'landing-compound', ordinal: '03', label: '재투자와 복리' },
  { key: 'payout', anchorId: 'landing-payout', ordinal: '04', label: '배당이 들어오는 달' },
  { key: 'presets', anchorId: 'landing-presets', ordinal: '05', label: '많이 쓰는 구성' },
  { key: 'checklist', anchorId: 'landing-checklist', ordinal: '06', label: '시작하기 전에' },
  { key: 'faq', anchorId: 'landing-faq', ordinal: '07', label: '자주 묻는 질문' }
] as const;

export type LandingChapter = (typeof LANDING_CHAPTERS)[number];

/** 장 키 → 차례 항목. 뷰가 섹션마다 번호·앵커를 손으로 적지 않게 한다(두 곳이 갈라질 여지 0). */
export const landingChapter = (key: LandingChapter['key']): LandingChapter => {
  const found = LANDING_CHAPTERS.find((chapter) => chapter.key === key);
  if (!found) throw new Error(`랜딩 차례에 '${key}' 장이 없다`);
  return found;
};

/**
 * S5 리듬 표가 실제로 그리는 종목.
 *
 * 🔴 **지급 월을 여기 적지 않는다.** 12칸은 `marketData` 스냅샷의 `payoutMonths` 를 **런타임에**
 * 읽어 그린다 — 월 1회 크론이 그 값을 갱신하므로 카피에 "SCHD 는 3·6·9·12월"이라고 쓰면 다음
 * 갱신에 조용히 거짓이 된다(decisions.md 티커 SEO 의 "숫자를 문자열에 박지 마라"와 같은 근거).
 * 성격이 갈리는 셋을 골랐다: 분기 지급 2종 + 매월 지급 1종.
 */
export const LANDING_PAYOUT_RHYTHM_TICKERS = ['SCHD', 'VIG', 'O'] as const;

export const LANDING_COPY = {
  hero: {
    /*
     * 🔴 2026-08-04 개정 — 종전 '배당, 여기서부터 이해하고 계산합니다'.
     *
     * 사용자 지시: "배당으로 복리로 키워 나간다는 느낌". 종전 제목은 **입구 안내**("여기서부터")라
     * 이 지면이 무엇을 하는 곳인지는 말했지만 **왜 하는가**는 말하지 않았다. 이 화면의 본론 두 장이
     * 복리(S4)와 구성 고르기(S6)인데 정문이 그 서사를 한 글자도 예고하지 않았다.
     *
     * 낱말 선택의 근거:
     *  · **'배당으로 배당을 키우는'** — 재투자를 **사용자가 하는 행동**으로 쓴다. 자산이 저절로
     *    불어난다는 서술("배당이 배당을 낳는")은 약속형에 가까워 쓰지 않았다.
     *  · **'복리'** — 이 개념의 정본 낱말이다. 🔴 눈덩이/스노우볼 비유는 전 표면 금지이고,
     *    같은 개념은 복리·시간·재투자로 푼다(위 규율 1).
     *  · **'계산합니다'** — 🔴 이 두 글자를 빼지 마라. 제목에서 서비스의 정체(계산 도구)가 사라지면
     *    첫 화면에는 느낌만 남는다. 리드가 보충하지만 h1 은 검색·공유·스크린리더가 **단독으로** 읽는다.
     *
     * ⚠ 이 문자열은 세 곳이 함께 따라온다: `index.html` 의 og:title·twitter:title·정적 셸 h1
     * (`test/seo/landingShellCopyParity.test.ts` 가 문자 단위로 잠근다)과 `test/landing/landingHierarchy`.
     */
    title: '배당으로 배당을 키우는 복리, 여기서 계산합니다',
    /*
     * 리드가 **무슨 서비스인지**를 마저 말한다 — 입력(주식·ETF 개념)부터 출력(자산·월 배당의
     * 해마다 변화)까지. '자산과 월 배당'은 지어낸 문구가 아니라 시뮬레이터가 실제로 내는 두 축이다
     * (`shared/lib/snowball/SnowballSimulation.ts` 의 결과 필드). 조건부 서술('넣었을 때')이라
     * 약속형이 아니고, 숫자는 한 글자도 없다.
     */
    lede: '주식과 ETF가 무엇인지부터, 받은 배당을 다시 넣었을 때 자산과 월 배당이 해마다 어떻게 달라지는지까지 한 화면에서 확인하실 수 있습니다. 가입은 필요하지 않습니다.',
    searchLabel: '종목 검색',
    searchPlaceholder: 'SCHD, JEPI 같은 종목을 검색해 보세요',
    /*
     * 🔴 마커가 말할 수 있는 것보다 더 말하지 않는다. `snowball:has-workspace` 는
     * **"이 브라우저에서 시뮬레이터를 써 본 적 있음"** 이지 "저장된 계산이 있음"이 아니다 —
     * 자동 저장이 하이드레이션 직후 120ms 디바운스로 **사용자 편집 없이도** 한 번 돌기 때문에
     * (pages/Main/hooks/persistence/usePortfolioPersistence.ts) 한 번 열어만 봐도 마커가 남고,
     * 그 뒤 브라우저 저장소를 비웠어도 마커는 남는다(jotai/snowball/persistence/workspaceMarker.ts).
     * ⚠ **코드로 정확도를 올리지 마라** — 그 판단(IndexedDB 를 읽지 않는다)은 이미 끝났고 옳다.
     * 어긋난 쪽은 카피였다.
     */
    resumeNotice: '이 브라우저에서 시뮬레이터를 열어 보신 적이 있습니다',
    resumeAction: '이어서 계산하기'
  },

  /**
   * 차례 블록의 문구. 🔴 "목차"가 아니라 **"차례"** 다 — 이 지면은 문서이지 앱 메뉴가 아니다.
   *
   * ⚠ 2026-08-04 현재 **화면에 그려지지 않는다** — 사용자 지시로 히어로 아래 차례 블록을 걷었고
   * 그 자리는 "맨 위로" 버튼이 대신한다. 부품(`components/ChapterIndex`)과 이 문구는 남아 있지만
   * 소비처가 0이다. 되살릴 때 히어로 문구를 인용하지 마라 — 그 결합이 이번 개정에서 낡았다.
   */
  chapterIndex: {
    eyebrow: '이 안내서가 다루는 것',
    navLabel: '이 화면의 차례'
  },

  /** 검색 패널 — 상태별 문구. 무음 실패 금지: 결과가 없으면 **사유를 말한다**. */
  search: {
    resultCount: (count: number) => `검색 결과 ${count}건`,
    empty: (query: string) => `'${query}'와 일치하는 종목 소개가 없습니다.`,
    /** 🔴 "추천 종목"이라고 쓰면 투자 권유가 된다. 사실만 말한다 — 소개 글이 있다는 사실. */
    fallbackTitle: '소개 글이 준비된 종목',
    emptyHint: '찾으시는 종목이 없다면 시뮬레이터에서 직접 만드실 수 있습니다.',
    seeAll: (total: number) => `종목 전체 보기 (${total}종)`
  },

  /**
   * 🔴 `order` 는 **평범한 숫자**다 — `①②③`(유니코드 원문자)로 되돌리지 마라. 배지가 이미 원을
   * 그리고 있어 원문자를 넣으면 24px 배지 안의 12px "원 안의 원"이 되어 숫자가 읽히지 않는다
   * (2.2배 확대 캡처로 확인). 순서 자체는 내용이라(주식을 모르면 ETF 를 설명할 수 없다) 지우지 않는다.
   */
  /**
   * 처음 온 사람이 밟는 길(2026-08-06 신설).
   *
   * 🔴 걸음의 **제목·설명은 여기 적지 않는다** — `GUIDE_START_PATH` 의 가이드가 자기 제목을 갖고
   * 있고, 랜딩은 그것을 그대로 그린다. 두 곳에 적으면 가이드를 고칠 때 랜딩이 낡는다.
   */
  startPath: {
    title: '처음이라면 여기부터',
    lede: '계좌를 여는 일부터 계산까지, 순서대로 읽으실 수 있습니다. 각 걸음은 3분 안팎이면 읽힙니다.',
    readAction: '읽기',
    finalTitle: '내 조건으로 계산하기',
    finalLede: '위 내용을 읽으셨다면, 매달 넣을 금액과 기간을 넣어 배당이 어떻게 쌓이는지 확인하실 수 있습니다.',
    finalAction: '계산하기'
  },

  concept: {
    title: '배당을 알기 전에, 세 단어',
    items: [
      {
        id: 'stock',
        order: '1',
        title: '주식',
        body: '주식은 회사의 지분입니다. 한 주를 사면 그 회사의 아주 작은 일부를 갖게 됩니다. 회사의 가치가 오르면 주가도 오르고, 내리면 주가도 함께 내립니다.'
      },
      {
        id: 'etf',
        order: '2',
        title: 'ETF',
        body: 'ETF는 여러 종목을 한 바구니에 담아 주식처럼 사고팔 수 있게 만든 상품입니다. 한 주만 사도 수십에서 수백 개 종목에 나눠 담는 효과가 있어, 한 회사에 문제가 생겨도 전체가 같은 폭으로 흔들리지는 않습니다.'
      },
      {
        id: 'dividend-stock',
        order: '3',
        title: '배당주',
        body: '배당은 회사가 번 이익의 일부를 주주에게 현금으로 나눠 주는 것입니다. 배당주와 배당 ETF는 그 지급을 오래 이어 온 종목을 모은 것입니다. 다만 배당은 약속이 아니라 회사의 결정이라, 줄어들거나 멈출 수 있습니다.'
      }
    ]
  },

  /** 🔴 비유 0. "받은 배당으로 다시 산다 → 주식 수가 는다 → 다음 배당이 는다"는 **절차**로만 쓴다. */
  compound: {
    title: '배당을 다시 넣으면 무엇이 달라지나',
    paragraphs: [
      '받은 배당으로 같은 종목을 다시 사면 보유 주식 수가 늘어납니다. 주식 수가 늘어난 만큼 다음 배당도 늘어나고, 그 배당으로 또 사면 주식 수가 다시 늘어납니다. 이 되풀이가 오래 이어질수록 새로 넣는 돈이 없어도 배당이 늘어나는 폭이 커집니다. 이것을 복리라고 부릅니다.',
      '얼마나 늘어나는지는 배당률, 배당이 해마다 늘어나는 속도, 투자 기간, 세율에 따라 전부 달라집니다. 재투자는 자동으로 일어나지 않고, 배당을 받을 때마다 다시 사겠다고 결정해야 합니다.'
    ],
    factorsTitle: '계산에 필요한 값 네 가지',
    factors: ['배당률', '배당 성장률', '투자 기간', '배당소득세율'],
    linkText: '배당 시뮬레이터에서 직접 계산해 보실 수 있습니다.'
  },

  payout: {
    title: '배당이 들어오는 달은 종목마다 다릅니다',
    lede: '미국 배당 ETF는 대체로 세 달에 한 번, 일부는 매달 배당을 지급합니다. 지급하는 달이 서로 다른 종목을 함께 담으면 배당이 들어오는 달이 촘촘해집니다.',
    /** 🔴 필수 문장. 이것이 빠지면 이 섹션은 "자주 받는 것이 좋다"는 권유가 된다. */
    honesty:
      '다만 배당을 자주 받는 것과 자산이 많이 늘어나는 것은 다른 이야기입니다. 어느 쪽을 원하시는지는 직접 계산해 보고 정하시는 편이 좋습니다.',
    /** 지급 횟수는 스냅샷에서 세어 넣는다 — 카피에 "연 4회"를 박지 않는다. */
    frequencySummary: (count: number) => (count === 12 ? '매월 지급(연 12회)' : `연 ${count}회 지급`),
    monthsAria: (symbol: string, months: readonly number[]) =>
      `${symbol}, ${months.map((month) => `${month}월`).join('·')} 지급`,
    unknown: '지급 월 정보가 아직 없습니다',
    /**
     * 🔴 12칸의 색 부호를 **눈으로** 읽는 유일한 문장이다. 지우면 이 표는 화면에서 색으로만 말하게
     * 된다(행 `aria-label` 은 스크린리더에만 있다). "지급한"(과거)인 이유는 바로 아래 각주가
     * "과거 지급 이력에서 확인한 값"이라고 말하기 때문이다 — "지급하는"으로 쓰면 약속형이 된다.
     *
     * ⚠ `index.html` 의 정적 셸에는 **복제하지 않는다.** 셸에는 12칸 격자가 없어서 이 문장이
     * 가리킬 "칸"이 존재하지 않는다(없는 것을 설명하는 문장이 크롤러에게만 내려간다).
     *
     * 2026-08-02: 링과 굵기가 더해져 "진하게"만으로는 부족해졌다.
     */
    legend: '테두리가 둘린 진한 칸이 배당을 지급한 달이고, 나머지 옅은 칸은 지급이 없었던 달입니다.',
    footnote: '지급 월은 과거 지급 이력에서 확인한 값이며, 실제 지급 여부와 날짜는 달라질 수 있습니다.',
    linkText: '내 종목의 지급 예정일은 배당 캘린더에서 확인하실 수 있습니다.'
  },

  presets: {
    /** 개수는 데이터에서 센다 — 프리셋이 14개가 되면 이 문장이 저절로 따라온다. */
    title: (count: number) => `사람들이 많이 쓰는 구성 ${count}가지`,
    lede: '아래 구성은 시뮬레이터에 그대로 넣어 볼 수 있는 예시입니다. 특정 종목의 매수를 권하는 것이 아니며, 결과는 입력하신 조건에 따라 달라집니다.',
    /**
     * 묶음 캐러셀의 접근명(2026-08-07). 🔴 "더 보기"/"접기" 를 대신한 자리다 — 카드가 전부
     * 궤도 위에 있어 접을 것이 없어졌고, 개수는 여기서 소리로 말한다(화면에는 점과 숫자가 말한다).
     */
    carouselLabel: (label: string, count: number) => `${label} ${count}개`,
    allocationLabel: '구성 비중',
    cta: '시뮬레이터에서 골라 적용하기'
  },

  checklist: {
    title: '시작하기 전에',
    stepsTitle: '앱에서 해보는 순서',
    steps: [
      '관심 있는 종목을 고릅니다. 위 구성 중 하나를 골라도 되고, 직접 검색해 담아도 됩니다.',
      '매달 얼마를 몇 년 동안 넣을지, 세율은 얼마로 볼지 정합니다.',
      '자산과 월 배당이 해마다 어떻게 달라지는지 결과 화면에서 확인합니다.'
    ],
    stepsClosing:
      '가입은 필요하지 않습니다. 입력하신 값은 사용하시는 기기에 저장되고, 원하실 때 링크로 공유하실 수 있습니다.',
    cautionsTitle: '실제 투자 전에 스스로 확인할 것',
    /** 🔴 특정 증권사·상품·매수 시점을 언급하지 않는다. 외부 금융사 링크 0건. */
    cautions: [
      '해외 주식을 사려면 증권 계좌와 해외 주식 거래 신청이 필요합니다.',
      '원화를 달러로 바꾸는 과정과 환율 변동이 실제 결과에 영향을 줍니다.',
      '배당에는 세금이 붙고, 금액이 커지면 금융소득종합과세 대상이 될 수 있습니다.',
      '여기 숫자는 입력하신 가정을 계산한 결과일 뿐 수익을 보장하지 않으며, 원금 손실이 발생할 수 있습니다.'
    ]
  },

  /**
   * 자주 묻는 질문 8문항.
   *
   * 🔴 **다음 트랙이 이 배열을 `index.html` 의 `FAQPage` JSON-LD 로 옮긴다.** 그때는 검색 결과에
   * 그대로 인용되므로 **문장이 곧 공개 약속**이 된다. `needsApproval: true` 인 문항은 사용자 확인
   * 전에는 **JSON-LD 에 넣지 마라**(화면 노출은 기존 푸터 고지·법무 문서와 정합해 확인했다).
   * 승인 사유는 각 항목의 `approvalNote` 에 적어 두었다.
   */
  faq: {
    title: '자주 묻는 질문',
    items: [
      {
        id: 'free',
        question: '이 사이트는 무료인가요?',
        answer: '네, 전부 무료입니다. 유료 기능이나 광고가 없는 개인 프로젝트입니다.',
        needsApproval: true,
        approvalNote: '유료화·광고 도입 시 거짓이 된다. 현재는 PageFooter 사이트 공통 고지와 같은 주장이라 정합.'
      },
      {
        id: 'signup',
        question: '가입해야 쓸 수 있나요?',
        // ⚠ 답변은 **앱의 목소리**라 격식체다("아니요"가 아니라 "아닙니다") — 질문만 사용자 목소리다.
        answer:
          '아닙니다. 시뮬레이터, 종목 소개, 배당 캘린더는 로그인 없이 이용하실 수 있습니다. 로그인은 여러 기기에서 같은 내용을 이어 보거나 커뮤니티에 글을 쓰실 때만 필요합니다.',
        needsApproval: true,
        approvalNote: '커뮤니티가 꺼진 배포에서도 참인 문장인지 확인 필요.'
      },
      {
        id: 'storage',
        question: '입력한 내용은 어디에 저장되나요?',
        answer:
          '로그인하지 않으시면 사용하시는 기기의 브라우저에만 저장됩니다. 로그인하시면 저장한 내용이 계정에 함께 보관되어 다른 기기에서도 이어서 보실 수 있습니다. 자세한 내용은 개인정보처리방침에 있습니다.',
        needsApproval: true,
        approvalNote: '개인정보처리방침 본문(pages/Legal/copy/privacyCopy.ts)의 기기 저장 서술과 대조해 확인함.'
      },
      {
        id: 'where-computed',
        question: '계산은 어디에서 이루어지나요?',
        answer:
          '계산은 사용하시는 브라우저 안에서 이루어지며, 입력하신 조건이 계산을 위해 서버로 전송되지는 않습니다.',
        needsApproval: true,
        approvalNote: '"계산"에 한정된 문장인지 확인 필요(계산=브라우저는 참 · "서버가 없다"는 거짓).'
      },
      {
        id: 'numbers',
        question: '배당률이나 주가 같은 숫자는 어디서 오나요?',
        answer:
          '공개된 시세 정보를 정기적으로 받아 갱신합니다. 화면의 숫자는 그 갱신 시점을 기준으로 하며 실시간 시세가 아닙니다.',
        needsApproval: true,
        approvalNote: '"정기적으로"의 실제 주기(월 1회 크론)를 밝힐지 결정 필요.'
      },
      {
        id: 'guarantee',
        question: '계산 결과대로 배당을 받게 되나요?',
        answer:
          '아닙니다. 결과는 입력하신 가정을 그대로 계산한 값입니다. 실제 배당은 회사와 운용사의 결정에 따라 줄어들거나 멈출 수 있고, 주가도 오르내립니다.',
        needsApproval: false
      },
      {
        id: 'advice',
        question: '투자 자문인가요?',
        answer:
          '아닙니다. 특정 종목이나 상품의 매수를 권하지 않으며, 투자 판단과 그 결과에 대한 책임은 이용자 본인에게 있습니다.',
        needsApproval: false
      },
      {
        id: 'tax',
        question: '세금은 어떻게 계산되나요?',
        answer:
          '입력하신 배당소득세율을 배당에 적용해 계산하며, 기본값은 15.4%입니다. 금융소득종합과세처럼 개인 상황에 따라 달라지는 부분은 계산에 포함하지 않고 안내만 표시합니다.',
        needsApproval: true,
        approvalNote:
          '15.4% 는 shared/lib/snowball/SnowballForm.ts:109 의 폼 기본값과 일치함을 확인. 종합과세 안내 동작은 simulation-engineer 확인 대상.'
      }
    ]
  },

  /**
   * 페이지를 닫는 한 줄.
   *
   * 🔴 **버튼 라벨을 여기에 만들지 않는다** — 한 의도에는 한 라벨이라, 마무리 CTA 는 히어로 CTA 와
   * **같은 문자열**을 `LANDING_HERO_CTAS` 에서 읽는다. 여기에 라벨을 적는 순간 두 문구가 갈라질 수
   * 있는 상태가 되고, 갈라진 쪽은 아무도 신고하지 않는다.
   */
  closing: {
    note: '지금까지 본 내용을 직접 계산해 보실 수 있습니다.'
  },

  /** 푸터 각주 — **랜딩에서만 참인 문장**만 넣는다(사이트 공통 고지는 PageFooter 가 이미 갖고 있다). */
  footnotesTitle: '이 사이트에 대해',
  footnotes: [
    '이 화면의 설명은 배당 투자를 처음 접하시는 분을 위한 일반적인 소개이며, 특정 상품에 대한 권유가 아닙니다.',
    '지수 시세와 지급 월은 참고용이며 실시간 정보가 아닙니다.'
  ]
} as const;
