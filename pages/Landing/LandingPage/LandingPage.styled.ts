import styled from '@emotion/styled';
import { color, font, media, space } from '@/shared/styles';

/**
 * 랜딩 페이지의 세로 리듬과 히어로 무대.
 *
 * 🔴 이 파일은 **면을 하나도 만들지 않는다.** 2026-08-03 기준 랜딩의 틴트 면 장부는 이렇다:
 * **① 마무리 CTA(`ClosingCta` · brand-subtle) ② 푸터 브랜드 패널(`PageFooter` · 네이비)** — 2/2.
 * 둘 다 각자의 컴포넌트가 소유한다. 여기서 세 번째를 만들면 `tools/dev/tintscan.mjs` 의 `/` 항목이
 * exit 1 이다(상한 2 는 확정 결정이고 올릴 수 없다).
 *
 * ── 🔴 2026-08-03: 히어로 뒤 후광을 **걷어냈다**(사용자 지적 "상단 카드에 범위가 벗어나는 버그") ──
 *
 * 종전 이 블록은 `::before` 로 페이지 hue 28% 짜리 라디얼 후광을 깔았고, 그 상자는 `inset: -96px 0 18%`
 * 였다 — 즉 **히어로 카드보다 96px 위에서 시작하는 직사각형**이다. 흰 캔버스로 바뀌자 그 사실이
 * 그대로 눈에 보이게 됐다(실측 @1280: 카드 상단은 y=144 인데 y=100·x=300 의 배경이 이미
 * `rgb(203,227,239)` — 흰색 대비 **1.33:1 · ΔE76 15.1**, 이 지면에서 가장 진한 파스텔이었다).
 * 사용자가 "카드 범위를 벗어난다"고 본 것이 이 면이다 — 스티키 헤더 밑에서 하드한 가로선으로
 * 잘린 파스텔 띠가 카드 위·옆에 얹혀 있었다(좌우는 본문 컬럼에서 끊기고 위는 카드보다 96px 높다).
 *
 * 게다가 그 램프는 **의사요소라 `tintscan` 의 DOM 열거를 비껴간다** — 예산 밖에서 사는 면이었다.
 * 흰 캔버스의 이득이 절제에서 나온다는 원칙(`shared/styles/surfaces.ts` 머리말)에 정면으로 어긋난다.
 *
 * 🔴 **후광을 되살리지 마라.** 히어로가 "여기가 정문이다"를 말하는 채널은 이제 셋이고 전부 실측됐다:
 * ① 페이지 hue 테두리 48%(흰 면 위 **2.06:1**) ② 상단 4px hue 리본(**4.62:1**) ③ 24~40px 패딩과
 * 44px 제목. 카드 밖으로 새는 램프 없이도 히어로는 이 지면에서 가장 무거운 블록이다.
 * ⚠ 되살리고 싶어지면 그 면은 **예산 안에서** 세어져야 한다 — 그러면 마무리 CTA 나 푸터 중
 *   하나가 면을 내놓아야 하고, 둘 다 그럴 이유가 없다.
 */

/**
 * **그룹 경계**의 간격 — 이 스택의 직계 자식은 섹션이 아니라 서사 묶음이다
 * (히어로 · G2 배우기 · G3 고르기 · G4 참조와 마무리 · 푸터).
 *
 * 🔴 값이 하나뿐이면 그룹이 표현될 수 없다 — before 는 8곳 전부 같은 간격(실측 1280 50.6px ·
 * 390 32px)이라 8섹션이 균일한 카드밭으로 읽혔다. 이 값은 그룹 **안** 간격(LandingGroup)의
 * 2배 대역이라 "묶음"이 눈에 보인다.
 */
export const LandingStack = styled.div`
  display: grid;
  gap: clamp(56px, 6.4vw, 104px);
  min-width: 0;
`;

/**
 * 서사 한 묶음(G2 배우기 · G3 고르기 · G4 참조와 마무리).
 *
 * 🔴 **랜드마크가 아니라 리듬 장치라 순수 div 다** — heading·role 을 주지 마라. 섹션마다 이미
 * section aria-labelledby 가 있어서 여기에 이름을 붙이면 랜드마크가 이중이 되고, 스크린리더
 * 사용자에게는 "묶음"이라는 시각 장치가 두 번째 목차로 들린다.
 */
export const LandingGroup = styled.div`
  display: grid;
  gap: clamp(28px, 3.2vw, 52px);
  min-width: 0;
`;

/**
 * 🔴 **이 페이지의 h1 크기는 여기서 결정된다 — `PageHero.styled.ts` 의 `heroTitleFontSize` 가
 * 아니다.** 정본 파일만 읽는 사람은 30px 상한을 믿게 되므로 이 주석이 유일한 안내다.
 *
 * 왜 여기냐: `components/common/PageHero` 는 **앱의 유일 히어로**이고 이번 트랙 편집 금지다
 * (다른 트랙이 미커밋 수정 중). 랜딩에서만 제목을 키울 다른 경로가 없다. 영구 해법은 히어로
 * 자신이 크기 변형을 소유하는 것이고 그건 별도 트랙이다(스펙 §G-1).
 *
 * ⚠ 상한 44px 을 올리지 마라 — 1280 에서 46px 부터 제목이 2줄이 되고, 2줄이 되는 순간
 * `HeroTitleGroup` 의 `align-items: center` 때문에 아이콘 배지가 첫 줄 옆이 아니라 **두 줄의
 * 한가운데**로 내려앉는다.
 */
const LANDING_HERO_TITLE_FONT_SIZE = `clamp(${font.size['3xl']}, calc(0.6rem + 3.2vw), ${font.size['6xl']})`;

/**
 * 히어로 그림(`HippoCoinScene`) **무대 한 변**. 폭에 따라 연속으로 줄어든다.
 *
 * 🔴 헤더 로고와 같은 그림이 한 화면에 두 번 있는 것은 사실이므로, **크기가 역할을 갈라야 한다** —
 * 헤더는 44px 정체 표식(링크 안), 여기는 그 **6.3배**(실측 @1280 = 278px)인 지면의 삽화다.
 * 두 배 남짓이면 "같은 걸 두 번 그렸다"로 읽히고, 여기 값은 그 대역을 확실히 벗어난다.
 * ⚠ 280px 상한을 올리지 마라 — 아래 HERO_ART_INSET(무대폭의 20%)과 함께 카드 안쪽 폭을 먹는
 *   값이라, 키우면 제목이 3줄이 된다(실측 @1280: 제목 lane 761px · h1 실제 폭 759px · 2줄).
 * ⚠ 150px 하한은 **좁은 폭 전용 값**이다. 여기서는 그림이 카드 아래 흐름에 들어가므로 키우면
 *   접힘 예산을 그대로 먹는다(실측 @390: 180px 이면 검색 하단 617px, 150px 이면 587px).
 */
const HERO_ART_WIDTH = 'clamp(150px, 22vw, 280px)';

/**
 * 그림 무대의 **오른쪽 안쪽 여백**(카드 테두리로부터).
 *
 * 🔴 금화가 무대 오른쪽으로 13% 나가는 것이 `HippoCoinScene` 연출의 핵심이라(그 파일 주석),
 * 이 값이 그 돌출분보다 작으면 금화가 **카드 테두리를 가로지른다**.
 *
 * 필요한 여유 = `0.13 x 무대폭`(정지 돌출) + `약 0.034 x 무대폭`(±4° 회전이 만드는 추가 폭)
 * ≈ **무대폭의 16.4%**. 그래서 vw 가 아니라 **무대폭에 비례**시킨다 — vw 로 두면 두 값의 기울기가
 * 달라 어느 폭 구간에서만 여유가 사라진다(실측: 3.4vw 였을 때 1280 에서 정지 여유가 5px 뿐이었고,
 * 4vw 로 올려도 768 에서 다시 8px 로 좁아졌다).
 * 20% 는 그 16.4% 위에 3.6%p 의 여유를 둔 값이다.
 */
const HERO_ART_INSET = `calc(${HERO_ART_WIDTH} * 0.2)`;

/**
 * 히어로 + 그 **바로 아래** 덩어리(검색 · 조건부 "이어서 계산하기") 한 묶음.
 *
 * 간격이 섹션 리듬(위 `LandingStack`)이 아니라 카드 리듬(clamp 12~20px)인 것이 요점이다 —
 * 검색은 독립 섹션이 아니라 히어로에 딸린 줄로 읽혀야 하고, 다음 섹션과는 확실히 떨어져야 한다.
 *
 * 🔴 **아래 미디어 블록은 `PageHero` 의 내부 DOM 을 바깥에서 겨냥한다.** 그 대가를 알고 쓴다:
 *  ① `header > div:first-of-type > div + div` 는 "제목 줄의 자식이 정확히 둘(제목 그룹 · 액션)"을
 *     전제한다. 랜딩은 `titleAction` 을 넘기지 않으므로 지금은 참이지만, 히어로가 슬롯을 하나 더
 *     렌더하면 **에러 없이 조용히 어긋난다**.
 *  ② 특이도로 이긴다 — Emotion 이 이 블록을 0-1-2 로 컴파일하고 `HeroTitle` 자기 클래스는 0-1-0 이다.
 *     `!important` 를 쓰지 마라. 필요해졌다면 선택자가 틀렸다는 신호다.
 *  ③ 잉크 보정값 0.1 은 shared/styles/heroTitleRow.ts 의 INK_ABOVE_LINE_BOX.display 를 **손으로
 *     복제한 값**이다. 그 상수가 바뀌면 여기만 낡는다.
 */
export const HeroBlock = styled.div`
  display: grid;
  gap: clamp(12px, 2vw, 20px);
  min-width: 0;

  /*
   * 🔴 좁은 폭(≤640)의 순서는 **히어로 카드 → 그림 → 검색**이다 — 그림이 CTA 줄 *위*로 올라가면
   * 접힘 예산이 깨진다(뷰 머리말의 확정 규칙). 실측 @390x844: 그림을 넣기 전 검색 하단 425px,
   * 넣은 뒤 **587px** — iOS Safari 100svh 최소치 664px 안이다. 시뮬레이터 CTA 하단은 262px 로
   * 그림 도입 **전후가 같다**(그림이 CTA 아래에 있으므로).
   * 그래서 여기서는 area 를 배정하지 않는다. DOM 순서(header → art → extras)가 그대로 화면 순서다.
   */

  /* 🔴 이 경계를 내리지 마라 — 640 이하는 선언 자체가 없어야 390x664 접힘 예산(시뮬레이터 CTA
     하단 258px)이 바이트 단위로 보존된다. */
  ${media.up('mobileWide')} {
    /*
     * 넓은 폭에서는 그림이 **히어로 카드 위에 겹쳐** 선다 — 카드 오른쪽이 원래 비어 있던 자리다
     * (실측 @1280 before: 카드 폭 1160 인데 제목이 701, 나머지 ~400px 이 늘 공백이었다).
     * 두 자식(header·art)을 같은 그리드 셀에 넣어 겹치므로 **카드도 검색도 전폭을 유지한다** —
     * 2열로 갈랐다면 카드가 좁아지고 그 아래 검색 폼과 폭이 어긋났을 것이다.
     * 🔴 그림은 카드의 **형제**라 카드의 overflow:hidden 에 잘리지 않는다(금화가 살짝 나가는 연출이
     *   이 부품의 핵심이다 — HippoCoinScene.styled.ts 참고).
     */
    grid-template-areas:
      'hero'
      'extras';

    /* 0) 카드를 그림과 **같은 셀**에 놓고, 그림이 앉을 오른쪽 lane 을 패딩으로 비운다.
       🔴 이 규칙이 없으면 제목·리드가 그림 밑으로 흐른다 — 리드는 max-width 가 없어 카드 안쪽
       폭을 끝까지 쓰기 때문이다(실측 @1280 before: 리드 1078px = 카드 안쪽 전폭).
       ⚠ 절대배치를 쓰지 않는 이유도 같다. 겹침만 만들고 자리는 못 비운다.
       lane = 그림 폭 + 오른쪽 여백 + 글자와의 간격(clamp 16~28px). 실측 @1280 padding-right
       359.3px → 제목·리드가 쓰는 폭 761px, 리드 오른쪽 끝과 그림 사이 26px. */
    > header {
      grid-area: hero;
      padding-right: calc(${HERO_ART_WIDTH} + ${HERO_ART_INSET} + clamp(16px, 2vw, 28px));
    }

    /* 1) 제목 줄을 세운다 — 제목이 히어로 폭 전체를 쓰고 CTA 가 아래 줄로 내려온다.
       DOM 순서는 그대로다(제목 → CTA → 리드 → 그림 → 검색 → 이어서).

       🔴 세로 여백도 **여기서** 준다. 히어로가 짧아 보이는 원인은 제목 크기가 아니라 카드 높이였다
       (실측 @1280 before: 히어로 카드 총 높이 약 110px — 4955px 짜리 문서의 정문치고는 배너에 가깝다).
       세로 여백을 header 가 아니라 이 규칙 안에서 주는 이유: 히어로 override 선택자는 하나 늘 때마다
       landingHeroOverrideCoupling 의 목록과 DOM 단정을 함께 고쳐야 한다(그 파일이 목록을 통째로
       비교한다). 이미 잠긴 규칙으로 되는 일에 새 선택자를 만들지 않는다. */
    > header > div:first-of-type {
      flex-direction: column;
      align-items: stretch;
      gap: clamp(18px, 2.4vw, 30px);
      padding: clamp(10px, 2.2vw, 30px) 0 clamp(4px, 0.8vw, 10px);
    }

    /* 2) 제목 크기. 히어로 공용 규칙을 이 페이지에서만 덮는다. */
    > header h1 {
      font-size: ${LANDING_HERO_TITLE_FONT_SIZE};
    }

    /* 3) 🔴 CTA 줄 오버라이드 두 개를 걷었다(2026-08-17). 히어로 actions 슬롯이 비면서
          "> div + div" 가 겨냥할 대상이 사라졌다 — 남겨 두면 무엇을 고치는지 아무도 모르는 규칙이
          되고, 나중에 히어로가 div 를 하나 더 늘리는 순간 엉뚱한 요소를 잡는다.
          수준 4갈래는 히어로 밖 형제라 이 오버라이드가 필요 없다(자체 스타일이 왼쪽 정렬을 갖는다).
          ⚠ 이 주석에 백틱을 쓰지 마라 — 템플릿 리터럴이 그 자리에서 끊긴다(실제로 겪었다). */
  }
`;

/**
 * 검색 + 조건부 "이어서 계산하기".
 *
 * 🔴 **`PageHero` 의 `meta` 슬롯에 넣지 않는다 — 히어로의 형제다**(2026-08-01 교정). `meta` 는
 * "글자 폭만큼의 hue 밑줄"을 갖는 짧은 근거 줄 자리라, 여기에 이 덩어리를 넣으면
 *  ① 부모(`HeroMeta`)가 그린 `border-bottom`·`padding-bottom` 이 그대로 남는다. 자식에서
 *     `border-bottom: none` 을 선언해도 **상속 속성이 아니라** 부모의 선은 지워지지 않는다
 *     (실측: 첫 화면 검색 아래 2px · 254px 짜리 identity 밑줄. 같은 블록의 `font-family` 되돌리기가
 *     상속 속성이라 실제로 먹혔던 것이 "무력화됐다"는 착시를 만들었다).
 *  ② `HeroMeta` 는 `justify-self: start` 라 폭이 내용에 맞춰 줄어든다 — 검색 폼의 `min(520px, 100%)`
 *     가 무의미해져 1280 에서도 입력이 204px 로 쪼그라들어 있었다.
 *  ③ `p` 를 `div` 로 바꿔 얻은 콘텐츠 모델 정합을, 그 안에 다시 `span` 을 세워 되돌리고 있었다
 *     (`span` 안의 `div`/`form`/`ul`. 파서가 자동으로 닫지 않아 **무증상으로 영원히 남는** 종류다).
 *
 * 되돌림은 부모 쪽에서 — 즉 슬롯을 **쓰지 않는 것**이 정답이다. 랜딩 전용 prop 을 뚫으면 앱의
 * 유일 히어로가 그 prop 을 영원히 지고 간다.
 */
export const HeroExtras = styled.div`
  display: grid;
  gap: ${space[3]};
  min-width: 0;

  /* 히어로 카드가 그림과 셀을 나눠 쓰므로(HeroBlock 의 grid-template-areas) 이 줄도 자기 area 를
     명시해야 한다 — 안 그러면 자동 배치가 세 번째 행을 만들어 검색이 그림 아래로 한 칸 더 내려간다. */
  ${media.up('mobileWide')} {
    grid-area: extras;
  }
`;

/**
 * 히어로의 **삽화 자리** — 아기 하마와 금화(`HippoCoinScene`).
 *
 * ## 왜 히어로에 그림이 있나
 * 이 지면은 배당을 처음 접하는 방문자를 위한 안내서다. 정문이 제목·버튼·검색만 있는 동안에는
 * "무엇을 하는 곳인가"는 읽히지만 **누구의 지면인가**가 없었다. 이 그림이 그 몫을 지고,
 * 동시에 브랜드 서사(재투자로 키운다)를 글자 없이 한 번 더 말한다.
 *
 * ## 🔴 헤더 로고와 중복으로 읽히지 않게 하는 두 축
 * 같은 그림이 한 화면에 두 번 있는 것은 사실이다. 그래서 **크기와 자리**가 역할을 가른다:
 *  · 크기 — 헤더 44px(링크 안의 정체 표식) vs 여기 실측 @1280 **256px**(지면의 삽화). 5.8배.
 *  · 자리 — 헤더는 화면 왼쪽 위 고정 크롬, 여기는 히어로 카드 **오른쪽 안쪽**. 시선 경로가 겹치지 않는다.
 * ⚠ 이 값을 헤더 쪽으로 끌어내리면(예: 120px) 두 그림이 같은 급이 되어 그때부터는 정말 중복이다.
 *
 * ## 좁은 폭에서는 흐름 안, 카드 **아래**
 * 🔴 CTA 줄 위로 올릴 수 없다(뷰 머리말의 접힘 예산). 그래서 ≤640 에서는 카드 → 그림 → 검색 순서다.
 *
 * ## 장식이다
 * `HippoCoinScene` 은 label 없이 쓰면 `alt=""` 로 접근성 트리에서 빠진다. 여기 정보는 제목·리드가
 * 전부 말하므로 그 기본값이 맞다 — 브랜드 이름을 지는 자리는 헤더 워드마크다.
 */
export const HeroArt = styled.div`
  display: flex;
  justify-content: center;
  min-width: 0;

  /*
   * 🔴 무대 상자를 CSS 로 덮는다. HippoCoinScene 은 size(px 숫자) 하나로 정사각 무대를 잡는데,
   * 이 자리는 폭에 따라 연속으로 줄어야 해서 숫자 하나로는 표현되지 않는다(JS 로 폭을 재는 것은
   * 첫 페인트 뒤 크기가 바뀌는 결함을 만든다). 안전한 이유: 하마·금화는 무대 안에서 **전부 %로**
   * 배치되므로(HippoCoinScene.styled.ts) 상자만 바꾸면 구도·금화 각도가 그대로 보존된다.
   * ⚠ 그래서 호출부는 size 로 **상한값**을 넘긴다 — img width/height 속성이 그 값을 받아
   *   레이아웃 예약(CLS)에 쓰인다.
   *
   * 🔴 선택자를 && 로 두 번 겹치는 것은 장식이 아니다. 무대(SceneRoot)도 자기 Emotion 클래스로
   *   width/height 를 선언하므로 특이도가 0-1-0 으로 같고, 그때는 **주입 순서**가 승부를 가른다 —
   *   실측으로 졌다(390 에서 상자가 280px 로 남아 검색 하단이 717px, 접힘 예산 664px 초과).
   *   && 는 0-2-0 이라 순서와 무관하게 이긴다. 지우면 같은 결함이 조용히 돌아온다.
   */
  && > * {
    width: min(100%, ${HERO_ART_WIDTH});
    height: auto;
    aspect-ratio: 1 / 1;
  }

  ${media.up('mobileWide')} {
    /* 카드와 같은 셀에 겹쳐 선다. 아래로 붙이는 이유: 하마가 물에 앉아 있는 그림이라
       (HippoCoinScene 의 object-position: bottom center) 카드 바닥 선에 앉아야 떠 보이지 않는다. */
    grid-area: hero;
    align-self: end;
    justify-content: flex-end;
    /* 금화가 카드 테두리를 넘지 않는 거리. 근거는 HERO_ART_INSET 주석의 실측. */
    padding-right: ${HERO_ART_INSET};
    /* 카드 아래 테두리에 그림이 닿지 않게 — 카드 패딩(24~40px)의 절반 대역. */
    padding-bottom: clamp(12px, 1.6vw, 22px);
    /* 🔴 **이 한 줄이 없으면 히어로 CTA 가 눌리지 않는다.** 이 상자는 grid-area 를 채우느라 폭이
       카드 전폭(1160px @1280)이고 높이가 그림 높이(278px)라, 정확히 **CTA 줄과 리드 위**를 덮는다.
       그림은 읽는 것도 누르는 것도 아니므로 클릭을 통과시킨다.
       실측(elementFromPoint, 각 요소 중심): 시뮬레이터 CTA → BUTTON · 보유 종목 CTA → BUTTON ·
       리드 → P. 셋 다 그림이 아니라 원래 요소가 잡힌다. */
    pointer-events: none;
  }
`;

/**
 * 차례가 앉는 자리. 히어로 묶음 **안**이지만 검색보다 확실히 떨어져야 한다 — 검색은 히어로에
 * 딸린 줄이고(간격 12~20px), 차례는 "여기서부터 문서가 시작된다"를 여는 블록이다.
 *
 * 위쪽 1px 룰을 두지 않는다: 차례의 **각 항목이 이미 자기 상단 룰**을 갖고 있어서, 블록 전체에
 * 또 하나를 그으면 첫 줄만 이중선이 된다(실측 가능한 2px 계단).
 */
export const HeroIndexSlot = styled.div`
  margin-top: clamp(16px, 2.6vw, 32px);
  min-width: 0;
`;

/**
 * 재방문자용 줄. 마커가 없으면 렌더 자체를 하지 않는다 — 첫 방문자에게 없는 기능을 보여 주지 않는다.
 * 🔴 이 줄이 없어도 안전하다: 항상 보이는 시뮬레이터 CTA 가 같은 곳으로 간다.
 */
export const ResumeRow = styled.span`
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: ${space[2]};
`;

export const ResumeNotice = styled.span`
  font-size: ${font.size.xs};
  /* 히어로 밖(페이지 bg)으로 내려왔지만 계속 text-secondary 다 — 같은 줄의 고스트 버튼 라벨과
     위계가 맞고, 히어로 면 위로 되돌아가도 그대로 안전한 쪽이다(text-muted 는 velog 다크의
     gradient-hero 위에서 4.04:1 로 AA 미달이었다). */
  color: ${color.textSecondary};
`;
