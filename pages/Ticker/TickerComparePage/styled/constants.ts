/* ──────────────────────────────────────────────────────────────────────────
   이 화면만 쓰는 치수 상수 — 두 파일 이상이 함께 쓰기 때문에 한 자리에 모았다.
   🔴 여기 값은 **모듈 내부용**이다. `styled/index.ts` 는 이것을 다시 내보내지 않는다
     (원본 `TickerComparePage.styled.ts` 에서도 export 가 아니었다).
   ────────────────────────────────────────────────────────────────────────── */

/**
 * 이 화면이 새로 세우는 두 면의 패딩. `surface()` 가 이 값으로 안쪽 라운드까지 파생하므로
 * 손으로 두 번 적지 않는다(적으면 동심이 반드시 어긋난다 — `shared/styles/surfaces.ts` 머리말).
 */
export const VERDICT_PAD = 'clamp(18px, 2.2vw, 26px)';
export const EMPTY_PAD = 'clamp(22px, 3vw, 34px)';

/**
 * 항목(고정) 열의 폭.
 *
 * 🔴 **고정하지 않으면 좁은 폭에서 이 열이 화면의 절반을 먹는다.** 항목 이름 밑에 설명 한 줄이
 * 딸려 있어 브라우저가 그 문장을 안 접으려고 열을 계속 넓히기 때문이다(390px 실측: 약 180px).
 * 폭을 정해 두면 설명이 그 안에서 접히고, 남는 폭 전부가 **비교 대상**에게 간다.
 */
export const METRIC_COLUMN_WIDTH = 'clamp(150px, 30vw, 230px)';
