/**
 * 내 포트폴리오(`/dividend/portfolio`) 페이지 배럴.
 *
 * ⚠ 이 배럴은 **엔트리 번들에서 참조하지 않는다**(`pages/index.ts` 에 연결 금지). 라우터는
 * 페이지 컴포넌트를 `React.lazy` 로 직접 불러 별도 청크로 유지한다 — 배당 캘린더와 같은 규칙.
 * ⚠ 페이지 **안쪽** 코드는 이 배럴이 아니라 형제 폴더를 상대 경로로 부른다(`'../utils'`·`'../hooks'`).
 * 배럴을 경유하면 페이지 → 컴포넌트 → 페이지 순환이 생긴다(캘린더 선례).
 */
export * from './hooks';
export * from './utils';
