/**
 * 투자 성향 테스트의 두 화면.
 *
 * 🔴 둘은 같은 라우트를 쓰지만 **함께 보이는 일이 없다**(쿼리로 갈린다) — 그 이음매가 435줄짜리
 * 한 파일을 가른 절단면이다(2026-08-27). 둘 다 **순수 뷰**로, 저장·라우팅·계측은 컨테이너가 한다.
 */
export { QuizView } from './QuizView';
export { ResultView } from './ResultView';
