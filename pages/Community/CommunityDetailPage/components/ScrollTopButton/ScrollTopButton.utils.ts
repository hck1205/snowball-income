/**
 * "맨 위로" 버튼의 등장 판정 — 순수 함수라 레이아웃 없이도 검증된다.
 */

/**
 * 버튼을 띄우는가. 기준은 **뷰포트 높이 1개분**이다.
 *
 * 왜 px 상수(300 등)가 아니라 뷰포트 비율인가: 같은 300px 가 노트북(높이 800)에서는 "조금 내렸다",
 * 폰(높이 640)에서는 "화면 절반"이라 화면 크기마다 다른 UI 가 된다. 뷰포트 배수로 잡으면 어떤
 * 기기에서든 같은 사건("첫 화면이 완전히 사라졌다")에 버튼이 뜬다.
 *
 * 왜 1배인가: 짧은 글에서 방해가 되면 안 된다. 문서가 2 뷰포트보다 짧으면 스크롤 가능 거리
 * (문서 − 뷰포트)가 뷰포트보다 작아 **임계에 도달하지 못하고, 버튼은 아예 뜨지 않는다.**
 * 즉 "짧은 글에는 안 나온다"가 별도 분기가 아니라 이 한 줄에서 따라 나온다.
 */
export const isPastScrollTopThreshold = (scrollY: number, viewportHeight: number): boolean =>
  scrollY >= viewportHeight;
