export { default } from './ScenarioTabs';
export type * from './ScenarioTabs.types';
/** 탭 호버 툴팁 스타일 — 포털 렌더(마운트 순서 보존)를 위해 부모(MainRightPanel)가 직접 그린다. */
export { ScenarioTabTooltip } from './ScenarioTabs.styled';
