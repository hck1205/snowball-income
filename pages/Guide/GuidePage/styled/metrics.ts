/* -------------------------------------------------------------------------- */
/* 가이드 지면의 치수 상수 — 여러 관심사가 나눠 쓰는 값                            */
/* -------------------------------------------------------------------------- */

/** 얇은 액센트 레일 폭. 얇은 막대라 반경을 주지 않는다(radiusShape 가드 §②). */
export const RAIL = '4px';

/** 목차 레일 열 폭. 번호 + 두 줄 라벨이 접히지 않는 최소값(티커 상세와 같은 값). */
export const RAIL_COLUMN = '248px';

/**
 * 본문 문단의 읽기 폭. 68ch 를 넘기면 줄 끝에서 다음 줄 시작을 놓친다.
 * 🔴 티커 상세와 **같은 값**이다 — 두 지면은 같은 종류의 글이고, 읽는 폭이 다르면 사이트를
 * 옮겨 다닐 때 눈이 매번 다시 적응해야 한다.
 */
export const MEASURE = '68ch';
