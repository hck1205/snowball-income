/**
 * 채권 ETF (2026-08-18 신설 — 사용자 요청 HYEM).
 *
 * 🔴 **이 엔진은 주식 배당을 모델링한다**(고든 성장모형: 주가가 배당과 같은 속도로 자라고, 배당률이
 * 시간에 대해 불변). 채권은 그 전제가 성립하지 않으므로, 여기 넣은 가정이 무엇을 뜻하는지 반드시
 * 적어 둔다 — 숫자만 보고 주식 프리셋과 같은 것으로 읽으면 안 된다.
 *
 * ## 가정: 쿠폰은 받지만 가격은 제자리
 * 채권은 이자를 지급하지만 **원금이 자라지 않는다.** 그래서 `expectedTotalReturn` 을 실측 배당률과
 * **같게** 두어 `dividendGrowth = 0`(주가 성장 0)이 되게 한다. 이 모델에서 표현할 수 있는 가장 정직한
 * 근사다.
 * ⚠ 부도·금리 변동으로 **원금이 줄 수 있다**는 것은 표현하지 못한다(프리셋의 성장률 하한이 사실상 0 이다 —
 *   `expectedTotalReturn` 이 음수면 프리셋 필터 하한에 걸려 목록에서 사라진다). 그래서 이 프리셋은
 *   낙관 쪽으로 기울어 있다.
 *
 * ## 값의 출처 — 2026-08-18 실측(`ticker:refresh`, Yahoo chart API)
 * `initialPrice`·`dividendYield`·`frequency` 는 갱신 파이프라인이 채운다.
 * 🔴 **배당률이 갱신으로 움직이면 `expectedTotalReturn` 도 사람이 함께 옮겨야 한다**(그 값은 파이프라인이
 *    건드리지 않는다). 안 옮기면 성장률이 0 이 아닌 값으로 파생돼 "채권 가격이 자란다/줄어든다"는 가정이
 *    조용히 들어온다 — 실제로 2026-08-18 갱신에서 그 일이 났고 파이프라인의 "negative dividendGrowth"
 *    경고가 잡아 줬다(아래 HYEM 주석).
 *
 * ## 일부러 뺀 것
 * - **USO(United States Oil Fund, LP)** — 2026-08-18 에 넣었다가 **사용자 결정으로 뺐다.**
 *   원자재 선물 상품이라 ① 배당이 아예 없고 ② 선물 롤오버 비용 때문에 유가가 제자리여도 순자산이
 *   줄어든다. 이 앱에서 표현하려면 `expectedTotalReturn` 을 0 으로 둘 수밖에 없는데(음수는 필터 하한에
 *   걸려 사라진다), 그 0 은 "손실이 없다"가 아니라 **아무것도 말하지 않는 값**이다. 배당 재투자
 *   시뮬레이터에서 배당도 성장도 없는 프리셋은 사용자에게 줄 정보가 없다.
 *   ⇒ 원자재 선물형 상품은 프리셋에 넣지 않는다. 넣으려면 롤 손실을 모델링할 방법이 먼저 필요하다.
 */
export const BOND_ETFS = {
  /*
   * ⚠ `expectedTotalReturn` 이 **실측 배당률과 같은 6.76** 이다(위 머리말의 규칙: 채권은 성장률 0).
   *   처음에 6.5 로 뒀는데 갱신에서 배당률이 6.76 으로 들어와 성장률이 -0.26 으로 파생됐다 — 그 말은
   *   "채권 가격이 해마다 0.26% 씩 떨어진다"는 가정이 조용히 들어왔다는 뜻이라 ETR 을 배당률에 맞춰 옮겼다.
   */
  HYEM: {
    ticker: 'HYEM',
    name: 'VanEck Emerging Markets High Yield Bond ETF',
    initialPrice: 20.04,
    dividendYield: 6.76,
    dividendGrowth: 0,
    expectedTotalReturn: 6.76,
    frequency: 'monthly' as const
  }
} as const;
