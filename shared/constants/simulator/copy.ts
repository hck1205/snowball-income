/**
 * 시뮬레이터(`/simulator`) 화면의 공용 카피.
 *
 * 여기 모은 이유는 **같은 문장이 여러 파일에 흩어져 있었기 때문**이다 — 특히 빈 상태 안내는
 * 차트·월별 현금흐름·포트폴리오 구성·연도별 결과 4곳에 같은 문자열이 복제돼 있었고, 좌측 설정
 * 컬럼이 사라졌을 때 한 곳만 고치면 나머지가 조용히 거짓말을 계속했다.
 *
 * 어미는 격식체("~습니다")로 통일한다.
 */
export const SIMULATOR_COPY = {
  /**
   * 문서 메타 — `pages/Main/Main.tsx` 가 `useDocumentMeta` 로 싣는다.
   *
   * 🔴 **이 화면은 자기 메타를 반드시 스스로 선언해야 한다.** 2026-08-01 이전엔 `/` 가 시뮬레이터라
   * `index.html` 의 정적 메타가 곧 이 화면의 메타였는데, 랜딩이 `/` 를 가져가면서 그 자리가 랜딩
   * 문구로 바뀌었다. 선언이 없으면 ①`/simulator` 와 `/` 의 제목이 완전히 같아지고(중복 제목 +
   * 주력 키워드 소실) ②GA4 `page_view` 의 `page_title` 이 랜딩 제목으로 기록된다.
   * 문구는 `/` 가 시뮬레이터이던 시절 `index.html` 에서 회수했다 — 단 "(스노우볼)" 삽입구만
   * 뺐다. 🔴 "눈덩이/스노우볼/snowball" 비유는 **전 표면 완전 금지이고 브랜드 예외는 없다**
   * (구 "브랜드명 외" 조항은 2026-08-03 폐기 — 제품명이 Snowball Income 이던 시절의 규칙이다).
   * 제품명은 영문 "Hungry Hippo" 하나다(한글 음차 금지). 가드 `test/main/simulatorCopy.test.ts`.
   *
   * ⚠ 크롤러·SNS 스크래퍼는 JS 를 실행하지 않는다. 이 값은 **클라이언트 라우팅 진입·GA4·탭 제목**을
   * 고칠 뿐이고, 정적 셸(`index.html`)/서버 핸들러 쪽 문구 정합은 별개 소관이다.
   */
  meta: {
    title: '배당 재투자 시뮬레이터 — 배당 계산기',
    description:
      'SCHD·JEPI 같은 배당주·ETF로 포트폴리오를 구성하고 배당 재투자 효과를 시뮬레이션합니다. 목표 월 배당 도달 시점, 세후 현금흐름, 전량 매도 시 양도세까지 계산합니다. 가정을 그대로 계산해 보여주는 도구이며 투자 자문이 아닙니다.'
  },
  /** 포트폴리오가 비어 결과를 그릴 수 없을 때의 안내. 설정이 드로어 뒤에 있으므로 방향이 아니라 **행동**을 말한다. */
  emptyPortfolioHint: '투자 설정을 열어 종목을 추가하면 결과가 나타납니다.',
  /* 구 `settingsOpen`("설정 열기")은 헤더 진입 버튼과 함께 삭제됐다(2026-07-29) — 이제 설정을 여는
     라벨은 히어로의 `settingsTitle`("투자 설정") 하나다. 같은 동작에 두 이름이 있으면 사용자도
     테스트도 어느 쪽을 찾아야 할지 모른다. */
  settingsClose: '설정 닫기',
  settingsTitle: '투자 설정',
  editCondition: '조건 수정',
  heroTitle: '배당 시뮬레이터',
  heroLede: '포트폴리오와 투자 조건을 넣으면 장기 배당 현금흐름을 계산합니다.'
} as const;
