/**
 * 배당 도감 — 배당 목록 넷을 카드 한 벌로 다시 세운 것.
 *
 * 🔴 규칙·타입은 `dividendDex.types.ts`(순수), 데이터는 생성물이다. 손으로 고치지 마라 —
 *    도감 번호는 append-only 이고, 그 기록을 사람이 건드리면 사용자가 기억한 번호가 다른 회사가 된다.
 */
export * from './dividendDex.types';
