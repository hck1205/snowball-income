# 이 파일을 `.env`로 복사해서 쓴다. (`.env`는 .gitignore 대상 — 커밋되지 않는다)
# 값을 비워두면 vite.config.ts의 기본값이 쓰인다.

# 배포 도메인 — SEO 전체(canonical / og:url / og:image / sitemap.xml / robots.txt)의 단일 소스.
# ⚠ 기본값 https://snowball-income.example 은 RFC 2606이 예약한 테스트용 TLD라 실제로 해석되지 않는다.
#   실제 도메인으로 바꾸기 전에는 검색엔진 등록과 카카오톡/페이스북 미리보기가 동작하지 않는다.
#   CI/호스팅(Vercel, Netlify, GitHub Actions 등)에서는 이 이름의 환경변수로 주입하면 된다.
VITE_SITE_URL=https://hungry-hippo.xyz

# Google Analytics 4 측정 ID (없으면 GA를 아예 로드하지 않는다)
# 이 앱의 측정 ID. 측정 ID는 브라우저에 그대로 노출되는 공개 값이라 커밋해도 안전하다.
VITE_GA_MEASUREMENT_ID=G-VY837P1WK2

# ── 커뮤니티 (Supabase) — 선택 사항 ──────────────────────────────────────────
# 설정 절차: docs/supabase/README.md
#
# ⚠ 아래 두 값이 **둘 다** 있을 때만 커뮤니티(시나리오 갤러리·좋아요·댓글)가 켜진다.
#   비워 두면 커뮤니티만 꺼지고 앱은 지금과 100% 동일하게 동작한다
#   (IndexedDB 저장 + 공유 URL은 백엔드 없이 그대로 작동 — 이게 이 앱의 기본 배포 형태다).
#   하나만 채우면 "반쯤 켜진 상태"를 만들지 않고 꺼진 것으로 취급한다.
#
# anon 키는 **공개값**이다. 브라우저 번들에 그대로 실려 나가도록 설계된 키라 커밋해도 안전하다.
# 이 키로 할 수 있는 일은 전적으로 DB의 RLS 정책이 결정한다 (supabase/migrations/ 참고).
#
# 키 이름은 둘 중 하나를 쓴다 (PUBLISHABLE 이 우선):
#   - 신형 프로젝트: VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
#   - 구형 프로젝트: VITE_SUPABASE_ANON_KEY=eyJ...
#
# ⚠ 이 값들은 커뮤니티 전용이 아니다 — DB key 공유 미리보기(api/og.tsx, api/share-html.ts)도
#   같은 값을 process.env로 서버에서 재사용한다(anon 키만, service_role 불필요). 새 서버 변수 없음.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_ANON_KEY=

# 🚫 service_role 키에는 절대 VITE_ 접두사를 붙이지 마라.
#    VITE_ 접두사가 붙은 변수는 전부 번들에 인라인되어 공개된다.
#    service_role은 RLS를 통째로 우회하므로, 노출되는 순간 누구나 모든 사용자의 데이터를
#    읽고 지울 수 있다. 아래 SUPABASE_SERVICE_ROLE_KEY처럼 **서버 전용 변수로만** 쓴다
#    (Vercel 서버 함수 — api/account-delete.ts, api/naver-auth.ts).

# ── 네이버 로그인 (선택, 커뮤니티가 켜져 있을 때만 의미있다) ─────────────────────
# 설정 절차: docs/supabase/README.md §7
#
# 네이버는 Supabase 기본 소셜 프로바이더가 아니라서, 이 앱은 Vercel 서버 함수(api/naver-auth.ts)를
# 거쳐 세션을 발급한다. 위 VITE_SUPABASE_URL/키가 없으면(커뮤니티 꺼짐) 네이버도 함께 꺼진다
# (isNaverEnabled = isCommunityEnabled && VITE_NAVER_CLIENT_ID).
#
# 공개값 — 브라우저 번들에 인라인된다. 설정하면 네이버 버튼이 활성화되고, 비워두면 버튼은 사라지지
# 않되 "준비 중"으로 표시되고 클릭이 무시된다.
VITE_NAVER_CLIENT_ID=

# 🚫 서버 전용(Vercel 대시보드에만 등록). 절대 VITE_ 접두사를 붙이지 마라 — 붙는 순간 번들에
#    인라인되어 누구나 브라우저에서 읽을 수 있다. 로컬에서 실제 로그인을 테스트하려면(`npx vercel dev`)
#    이 값을 로컬 .env에 넣어도 되지만, 커밋은 금지.
NAVER_CLIENT_SECRET=

# 🚫 서버 전용. 네이버 로그인(api/naver-auth.ts)과 회원 탈퇴(api/account-delete.ts)가 함께 쓰는
#    Supabase service_role 키 — Project Settings → API에서 복사. RLS를 통째로 우회하므로
#    **VITE_ 접두사 절대 금지, 커밋 금지**.
SUPABASE_SERVICE_ROLE_KEY=

# (선택) 네이버는 이메일을 주지 않아서 서버가 결정론적 "합성 이메일"로 Supabase 계정을 식별한다.
# 비워두면 기본 도메인을 쓴다 — GoTrue 이메일 형식 검증만 통과하면 되므로 실제 소유 도메인일 필요는 없다.
NAVER_SYNTHETIC_EMAIL_DOMAIN=

# ⚠ 로컬 `npm run dev`(순수 Vite)에는 /api 라우트가 없어 위 서버 변수를 채워도 네이버 로그인은
#   항상 실패한다 — 실제 로그인 확인은 `npx vercel dev` 또는 Vercel Preview에서만 가능하다.

# ── 카카오 로그인 — 커스텀 플로우(계정 병합 회피) ──────────────────────────────
# 설정 절차: docs/supabase/README.md §7-4 ~ §7-6
#
# 카카오는 Supabase 기본 프로바이더지만, 기본 경로는 **같은 이메일의 구글 계정과 한 계정으로 병합**된다
# (GoTrue 자동 identity 링크 + 카카오 콘솔의 이메일 선택동의 — 코드로는 못 막는다). 그래서 네이버와
# 같은 구조로 카카오 회원번호 기반 합성 이메일(kakao_<id>@…)을 써서 계정을 분리한다.
# ⚠ 트레이드오프: 같은 사람이 구글/카카오로 각각 로그인하면 **별개 계정**이 된다(사용자 승인된 결정).
#
# 카카오 개발자 콘솔(https://developers.kakao.com/console/app)에서 할 일:
#   1) 앱 키 → **REST API 키** 복사(JavaScript 키 아님) → 아래 VITE_KAKAO_CLIENT_ID 에 넣는다
#   2) 카카오 로그인 → 활성화 ON → **Redirect URI 등록**(경로가 코드 상수라 정확히 일치해야 한다):
#        로컬  http://localhost:5173/community/auth/kakao/callback
#        배포  <VITE_SITE_URL>/community/auth/kakao/callback
#      (기존 Supabase 콜백 https://<project>.supabase.co/auth/v1/callback 은 폴백용으로 남겨둔다)
#   3) 동의항목 → **닉네임만** 켜고 프로필 사진·카카오계정(이메일)은 끈다
#   4) (선택) 보안 → Client Secret 을 "사용함"으로 켰다면 그 값을 아래 KAKAO_CLIENT_SECRET 에 넣는다
#
# 공개값 — 브라우저 번들에 인라인된다(authorize URL 에 실려 나가는 값이라 정상).
# **비워두면 커스텀 플로우를 타지 않고 기존 Supabase 카카오 로그인으로 폴백한다**(로그인이 죽지 않는다).
VITE_KAKAO_CLIENT_ID=

# 🚫 서버 전용. **선택** — 카카오 콘솔에서 Client Secret 을 "사용함"으로 켠 앱만 넣는다.
#    꺼져 있으면 비워둔다(빈 값을 보내면 오히려 토큰 교환이 거절된다). VITE_ 접두사 절대 금지.
KAKAO_CLIENT_SECRET=

# (선택) 카카오도 이메일을 쓰지 않으므로 서버가 결정론적 "합성 이메일"로 Supabase 계정을 식별한다.
# 비워두면 기본 도메인(kakao-oauth.snowball.invalid)을 쓴다 — GoTrue 형식 검증만 통과하면 된다.
KAKAO_SYNTHETIC_EMAIL_DOMAIN=

# ── 가계부 (구글 시트) — 선택 사항 ────────────────────────────────────────────
# 데이터 원본은 **사용자 본인의 구글 시트**다. 우리 서버·DB에는 가계부 행이 저장되지 않는다
# (로컬에 남는 것은 시트 ID·탭 ID·열 인덱스뿐).
#
# ⚠ 아래 세 값이 **전부** 있을 때만 가계부가 켜진다. 하나라도 비어 있으면 "반쯤 켜진 상태"를
#   만들지 않고 기능 전체가 꺼진다 — 앱의 나머지 동작에는 영향이 없다.
#
# 🔴 아래 값은 전부 **공개값**(브라우저 번들에 인라인된다). 그래도 이 파일에는 절대 실제 값을
#    적지 마라 — .env.example 은 커밋되는 파일이다. 실제 값은 .env / 호스팅 환경변수에만 넣는다.
#
# Google Cloud Console(https://console.cloud.google.com)에서 할 일:
#   1) 프로젝트 생성 → **API 및 서비스 → 라이브러리**에서 두 API를 사용 설정한다:
#        Google Sheets API · Google Picker API
#   2) **OAuth 동의 화면** 구성 → 범위(스코프)는 **`.../auth/drive.file` 하나만** 추가한다.
#      🚫 `.../auth/spreadsheets` 나 `.../auth/spreadsheets.readonly` 를 추가하지 마라 —
#         민감 스코프라 앱 검증(수 주 소요)에 걸린다. drive.file 은 "앱이 만든 파일 + 사용자가
#         피커로 직접 고른 파일"만 접근하므로 검증 없이 쓸 수 있다.
#   3) **사용자 인증 정보 → OAuth 클라이언트 ID**(웹 애플리케이션) 생성 →
#      승인된 자바스크립트 원본에 http://localhost:5173 과 <VITE_SITE_URL> 을 등록 →
#      발급된 클라이언트 ID를 VITE_GOOGLE_CLIENT_ID 에 넣는다.
#   4) **사용자 인증 정보 → API 키** 생성 → VITE_GOOGLE_API_KEY 에 넣는다(피커 developer key).
#      키 제한은 "HTTP 리퍼러" + "Google Picker API" 로 좁히는 것을 권장한다.
#   5) 대시보드 상단 **프로젝트 번호**(숫자, 프로젝트 ID가 아니다)를 VITE_GOOGLE_PROJECT_NUMBER 에 넣는다.
#      ⚠ 피커의 setAppId 에 쓰이는 값이라 누락·오기 시 피커가 조용히 뜨지 않는다(가장 흔한 실패 지점).
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
VITE_GOOGLE_PROJECT_NUMBER=

# ── 티커 데이터 갱신 스크립트 (앱 런타임과 무관 — 로컬/CI 배치 전용) ──────────────
# 외부 데이터 전체 지도: docs/external-data.md
#
# 🚫 둘 다 VITE_ 접두사 절대 금지 — 붙는 순간 브라우저 번들에 인라인되어 키가 공개된다.
#    (그래서 스크립트는 vite-node가 못 올리는 non-VITE_ 변수를 .env에서 직접 파싱한다:
#     scripts/tickerRefresh/snapshotIo.ts)

# 배당 **지급일** 갱신용 (npm run ticker:paydates). 무료 티어 25 req/day라 하루 25종목씩 회전한다.
# 발급: https://www.alphavantage.co/support/#api-key
# 비워두면 지급일 갱신만 실패한다 — 가격 갱신(Yahoo, 무키)과 앱 동작에는 영향 없다.
# CI에서는 GitHub Actions 시크릿으로 주입한다(.github/workflows/refresh-paydates.yml).
ALPHAVANTAGE_API_KEY=

# (선택) 가격·배당수익률 갱신의 **대체 공급자**(npm run ticker:refresh -- --provider=fmp).
# 기본 공급자는 Yahoo(무키)라 이 값 없이도 정상 동작한다.
# ⚠ FMP 무료 티어는 이 유니버스에 사용 불가로 실측됐다 — 유료 키 보유자만 의미가 있다.
FMP_API_KEY=
