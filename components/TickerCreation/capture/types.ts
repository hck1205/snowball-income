export type CaptureContext = {
  root: HTMLElement;
  captureTarget: HTMLElement;
  layoutWidth: number;
  captureWidth: number;
  captureHeight: number;
  outputScale: number;
  outputWidth: number;
  outputHeight: number;
  tileMaxHeight: number;
};

